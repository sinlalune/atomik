---
type: Atomik Session Record
title: CP-MVP-008 closing ceremony — recall, backlog rulings, next path confirmed (semantic graph)
timestamp: 2026-08-04T00:00:00Z
path: CP-MVP-008
ceremony: closing
---

# CP-MVP-008 closing ceremony (2026-08-04)

Run per 22 §Between paths, immediately after the acceptance
(`2026-08-04-cp-mvp-008-acceptance.md`). Recall was derived from the
register, ledgers, acceptance records, and log — never conversation
memory. Prompted exchange; owner answers recorded verbatim.

## Recall presented

- **Done:** 8 paths; milestones M0–M5 covered plus the M1 friction
  pass and the M2 completion — real Mistral generation with the full
  interaction pass. 635/55 green. Per the Q1 record, the definitive
  vault starts now.
- **Backlog:** leading candidate = semantic-graph path (front half:
  wikilink grammar + `[[target]]{label}` typed edges + nodes/edges
  index + typed backlinks; back half: Wikidata local-RAG slice whose
  entity index doubles as the verification router's layer B);
  packaged native Windows build (07-21 decision: opens after 008 —
  sequencing conflict surfaced); carried: full PDF viewer tooling,
  DnD/docking path, Git status/diff view, local llama.cpp seat,
  session-B small verification candidates; owner-gated doc patches
  (bedrock 03, 20, 22, 24 + edge-grammar ADR).
- **Challenges surfaced:** Windows-build vs semantic-graph ordering;
  broken frontmatter fence in
  `brainstorm/2026-07-06-creation-flows-and-dnd.md`; owner
  dogfooding test files uncommitted; the 2026-07-16
  one-surface-two-layers amendment pack never committed.

## Rulings (owner, verbatim options chosen)

1. **Next path** → **"Semantic graph path"** — confirmed as the
   post-008 path, front half before the Wikidata slice, exactly as
   amended by the 2026-08-03 session A. The **packaged Windows build
   stays reserved** (not chosen for this gap; re-offered at a later
   ceremony). DnD/docking stays a carried candidate. The path file
   is drafted just-in-time and activates only through its OPENING
   check.
2. **Doc patches** → **all four now, one docs unit**: bedrock 03
   (artifact lifecycle rule), bedrock 22 (session protocol: staging
   discipline, themed owner checklists, external-tool benches),
   bedrock 24 (bare gates), **and** bedrock 20 (notes-are-concepts
   doctrine) + the edge-grammar ADR — the owner chose to land the
   graph doctrine early rather than wait for the path opening.
3. **Session-B small candidates** (normalized containment; shallow
   judge pass over an operation's own selections) → **fold into
   M6** — they land with the Truth Lens opening, where the verdict
   vocabulary freezes.
4. **Hygiene** → **restore the broken fence** (done — `git checkout`
   of the one-character corruption) **and delete the test junk**
   (done: `atomik-project/test/`, `projects/test/test.md` +
   `test/`, `docs/projects/test`, the `Zone.Identifier` file;
   `projects/test/dfdf.md` content stays the owner's, left
   uncommitted). Not asked but resolved alongside: the 2026-07-16
   one-surface-two-layers amendment pack (a real proposal, "nothing
   applied") is committed as-is with this ceremony so it stops
   floating untracked; it remains a proposal awaiting owner review.

## Consequences applied in this work unit

- Register: leading-candidate paragraph marked CONFIRMED
  (2026-08-04); session-B backlog-pass candidates annotated "folded
  into M6"; Windows-build reservation annotated "not chosen at the
  008 gap".
- ACTIVE.md: no active path; next = semantic graph, pending its
  opening check.
- The four bedrock patches + ADR land as their own docs unit right
  after this note (same session, separate commit).

## Next

Draft the semantic-graph coding path from the register's recorded
inputs (sessions A/B/C notes), then run its OPENING check
feature-by-feature. Activation requires explicit owner acceptance.
