---
type: Atomik Session Note
title: S07 owner bench — round 1 feedback (CP-MVP-008)
timestamp: 2026-08-03T00:00:00Z
status: recorded
---

# S07 owner bench — round 1 (2026-08-03)

The owner started the S07 bench (Theme 2) after confirming the Mistral
key (Theme 1 go given). First real-vault pass: a PDF import. Six
findings, recorded verbatim below, triaged into ledger steps
S07b1–S07b6 in `../coding-paths/CP-MVP-008.md`.

## Owner feedback (verbatim)

> I tried to import a pdf, feedbacks: source view > source dossier tab
> not collapsable to get full pdf view (better if source are on the
> first panel and source dossier on the right no ? Also I cant interact
> with the pdf text, as I would be needing to do if I want to anchor
> highlighted passage (maybe in need of a proper pdf viewer with basic
> tooling). Another thing is that I still don't have access to all the
> request content sent to either highlighted ai generation or chat
> interaction, it misses hidden built-in blocks ( chat system, rules,
> guardrails etc...) you can create an tree hierarchy in
> /prompt/built-in with all of that but I need to be able to manage
> easily and completly every bit of token sent. Looking at input total
> of a chat I understand that we are hiding in sent messages the
> context sent, I need pills that retrace the context
> (system,prompts,notes,other text document, medias) with the
> associated amount of token in each pills. Chats files should be
> stored in per current date folder and without current date timestamp
> in the title. chat history picklist layout should be aligned on the
> left not veritcally centered.

## Triage

| # | Finding | Surface | Scope call |
|---|---------|---------|-----------|
| S07b1 | Chat history picklist left-aligned, not vertically centered | chat (008) | in-path, trivial |
| S07b2 | Chats persist as `chats/YYYY-MM-DD/<slug>.md` — per-date folder, no date in the title | chats/ pin (008) | in-path amendment, small |
| S07b3 | Built-in request blocks (chat system, rules, guardrails…) materialized as a manageable tree under `prompts/built-in/`; owner controls every token sent | prompts pin / 28 no-hidden-source (008 core) | in-path |
| S07b4 | Context pills on sent messages: system / prompts / notes / other documents / media, each with its token count | chat transparency (008) | in-path |
| S07b5 | Source view: dossier not collapsible → no full-PDF view; owner proposes source content on the FIRST panel, dossier on the right | source surface (M4-born) | in-path friction fix (blocks the bench) |
| S07b6 | PDF text not interactive — selection needed to anchor a highlighted passage and to feed the AI context menu; "maybe a proper pdf viewer with basic tooling" | PDF viewer (M4-born) | SPLIT — minimal pdf.js text layer + selection in-path (the S07 bench's "real selection over a real source" needs it on PDFs); full viewer tooling = post-008 backlog candidate, owner-gated |

## Sequencing note

The Theme-1 real-provider intents re-run is DEFERRED until S07b3/S07b4
land: those change what each request contains and shows, so running the
intents first would spend real tokens on a request shape about to
change. One re-run after the fixes covers both.

## Owner decisions (asked in-chat 2026-08-03, ANSWERED same day)

1. S07b6 split — minimal text-layer selection now, full PDF tooling as
   a post-008 backlog item: **CONFIRMED** ("i confirm the split").
   Full-viewer tooling joins the closing-ceremony backlog list.
2. S07b5 layout — source content left / dossier right + collapsible:
   **CONFIRMED** ("source left").

## Round 2 (same day)

**Owner (verbatim):** "if I close the last note tab beside a chat pane
and then click on a note in the tree panel, it open a tab in the chat
pane in stead of a note tab in a new panel"

Triaged as **S07b7**, fixed same day: a chat pane's tree is a browser,
never a tab feeder — notes route through `revealNote`, sources through
`revealSource` (new; same activate-anywhere-or-open-beside contract).
The S06c13 "notes opened from a chat pane's tree join it as ordinary
note tabs" behavior is AMENDED by this directive.
