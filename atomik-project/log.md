# Log — Atomik knowledge and execution plane

## 2026-07-05

- Plane created by v0.6 (ADR-009). Coding path CP-MVP-001 opened as the active path; execution rules (time-to-M2, thinness rule, mechanical labeling, experiential gate) promoted from the 2026-07-05 design conversation into the corpus.

## 2026-07-06

- CP-MVP-001 S01 executed (bootstrap per 22). Reconciliation result: dual-plane tree matches the ADR-009/35 layout; all 100 payload files verify against the upload guide's sha256 manifest; no nested Git repositories; `git init` + seed commit already done per the guide. Seed commit `4675233` (branch `master`) recorded as `base_commit` in the path frontmatter and Work Ledger. Observation, not a defect: the seeded `.gitignore` ignores all of `.atomik/`, stricter than 27's itemized default that leaves `.atomik/workspace.json` committable for shared layouts — revisit when S04 introduces workspace state. Next action: S02.
- CP-MVP-001 S02 executed. Code plane activated: root npm-workspaces manifest + `apps/desktop` (electron-main / electron-preload / renderer per 14, wired with electron-vite; CJS deliberately, sandboxed preload). Security contract of 13 pinned in code (`SECURE_WEB_PREFERENCES`) and enforced by tests; one narrow typed bridge method (`atomik:get-app-info`) declared in `shared/ipc-contract.ts`; 13 §IPC trigger honored. Verified end to end: typecheck clean, 9 tests / 3 suites green, build clean, real launch on WSLg prints `ATOMIK_SMOKE_OK` exit 0. Dependency alignment recorded in the module note (electron-vite 5 pairs with vite 7; plugin-react ^5 / vitest ^3 until vite 8 pairing). First module note: `docs/modules/atomik-desktop.md`. Next action: S03 (read 16 first).
- CP-MVP-001 S03 executed. Dev Docs tab (16 MVP slice): grouped docs tree + rendered Markdown over the real `docs/` files through two new validated read-only IPC channels (13 §IPC honored; traversal, extension-allowlist, and symlink-escape guards unit-tested). Bedrock diagrams inline as SVG data URIs; `#dev-docs:<relPath>` deep-links a page; generated artifacts (`docs_source.json`, `index.html`) excluded from the tree by design. Smoke now waits for the rendered view and can capture a screenshot (`ATOMIK_SMOKE_DOC` / `ATOMIK_SMOKE_SHOT`); verified visually on index.md and 22-agent-handoff with its D08 diagram; 8 groups / 70 files enumerated. Implementation lesson promoted to the module note: React-owned innerHTML must not be patched post-render — inline assets in the HTML string before rendering. Tests 9 → 28. Next action: S04 (read 03 first).
- UI naming convention set by the owner: the app displays its own name in lowercase ("atomik") across UI surfaces (window title, header brand, home heading); documentation prose keeps "Atomik". Recorded in the module note.
- Owner feedback promoted into the docs system: module notes alone don't teach a beginner the technologies/methodology/concepts behind what agents build — ownership requires a beginner layer. Added `docs/learning/` (17 gains the doc type + a maintenance trigger "first use of a new technology/framework/pattern"; 24 gains the template). First note `01-electron-shell-from-zero.md` covers S02–S03 with take-over exercises. Standing rule for the rest of CP-MVP-001: each step that mobilizes a new technology extends the learning layer in the same work unit.
- First-use owner reactions to S04 captured in `brainstorm/2026-07-06-panes-tabs-navigation.md`: tab-groups-per-pane is a loved differentiator (protect through future redesigns); per-view docs tree vs single workspace sidebar left open, to decide no earlier than S05 with experiential data.
- CP-MVP-001 S04 executed (03 read first). Workspace: recursive pane tree (leaf tabs / splits, drag-resize with clamped fraction), pure operations in `renderer/src/workspace/model.ts` (incubating workspace-core), zustand store with 500 ms debounced persistence, state in `.atomik/local-workspace.json` written atomically (temp+rename per 27) and validated with hard caps in main (renderer never names a path). Tab kinds are an open set: 'home', 'dev-docs' (per-tab docPath persists). Collapse rule: emptied child leaf folds into its sibling; the root leaf may stay empty. Corrupt/invalid state reads as null → default layout, never a crash. Smoke: fresh dir → panes=1; pre-seeded fixture → 38/62 split restored, panes=2, screenshot reviewed. Tests 28 → 46 / 8 suites. Deferred from 03, recorded: move-tab-between-panes, pin, focus mode. S01 .gitignore note resolved: `.atomik/` stays fully ignored; shared workspace.json deferred until collaboration. Learning note 02 (React state, immutability, recursion, atomic writes) added. Next action: S05.
