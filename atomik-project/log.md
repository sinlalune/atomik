# Log — Atomik knowledge and execution plane

## 2026-07-05

- Plane created by v0.6 (ADR-009). Coding path CP-MVP-001 opened as the active path; execution rules (time-to-M2, thinness rule, mechanical labeling, experiential gate) promoted from the 2026-07-05 design conversation into the corpus.

## 2026-07-06

- CP-MVP-001 S01 executed (bootstrap per 22). Reconciliation result: dual-plane tree matches the ADR-009/35 layout; all 100 payload files verify against the upload guide's sha256 manifest; no nested Git repositories; `git init` + seed commit already done per the guide. Seed commit `4675233` (branch `master`) recorded as `base_commit` in the path frontmatter and Work Ledger. Observation, not a defect: the seeded `.gitignore` ignores all of `.atomik/`, stricter than 27's itemized default that leaves `.atomik/workspace.json` committable for shared layouts — revisit when S04 introduces workspace state. Next action: S02.
- CP-MVP-001 S02 executed. Code plane activated: root npm-workspaces manifest + `apps/desktop` (electron-main / electron-preload / renderer per 14, wired with electron-vite; CJS deliberately, sandboxed preload). Security contract of 13 pinned in code (`SECURE_WEB_PREFERENCES`) and enforced by tests; one narrow typed bridge method (`atomik:get-app-info`) declared in `shared/ipc-contract.ts`; 13 §IPC trigger honored. Verified end to end: typecheck clean, 9 tests / 3 suites green, build clean, real launch on WSLg prints `ATOMIK_SMOKE_OK` exit 0. Dependency alignment recorded in the module note (electron-vite 5 pairs with vite 7; plugin-react ^5 / vitest ^3 until vite 8 pairing). First module note: `docs/modules/atomik-desktop.md`. Next action: S03 (read 16 first).
- CP-MVP-001 S03 executed. Dev Docs tab (16 MVP slice): grouped docs tree + rendered Markdown over the real `docs/` files through two new validated read-only IPC channels (13 §IPC honored; traversal, extension-allowlist, and symlink-escape guards unit-tested). Bedrock diagrams inline as SVG data URIs; `#dev-docs:<relPath>` deep-links a page; generated artifacts (`docs_source.json`, `index.html`) excluded from the tree by design. Smoke now waits for the rendered view and can capture a screenshot (`ATOMIK_SMOKE_DOC` / `ATOMIK_SMOKE_SHOT`); verified visually on index.md and 22-agent-handoff with its D08 diagram; 8 groups / 70 files enumerated. Implementation lesson promoted to the module note: React-owned innerHTML must not be patched post-render — inline assets in the HTML string before rendering. Tests 9 → 28. Next action: S04 (read 03 first).
- UI naming convention set by the owner: the app displays its own name in lowercase ("atomik") across UI surfaces (window title, header brand, home heading); documentation prose keeps "Atomik". Recorded in the module note.
- Owner feedback promoted into the docs system: module notes alone don't teach a beginner the technologies/methodology/concepts behind what agents build — ownership requires a beginner layer. Added `docs/learning/` (17 gains the doc type + a maintenance trigger "first use of a new technology/framework/pattern"; 24 gains the template). First note `01-electron-shell-from-zero.md` covers S02–S03 with take-over exercises. Standing rule for the rest of CP-MVP-001: each step that mobilizes a new technology extends the learning layer in the same work unit.
- First-use owner reactions to S04 captured in `brainstorm/2026-07-06-panes-tabs-navigation.md`: tab-groups-per-pane is a loved differentiator (protect through future redesigns); per-view docs tree vs single workspace sidebar left open, to decide no earlier than S05 with experiential data.
- Owner feature request: tree panels gained expand/collapse — Dev Docs groups are now collapsible (native details, like the vault/project folders already were), and every tree panel can collapse entirely (per-tab `tree` param, persisted with the workspace). Follow-up polish on owner feedback: the toggle glyphs read poorly and sized inconsistently (second CSS selector leak, `.devdocs-tree button` — same class of bug as the input fix; both now scoped to `li > button`) — replaced by a shared inline-SVG sidebar icon, uniform everywhere.
- Owner friction fix: the note/project creation inputs were unreadable (collapsed to ~20px) — a CSS selector leak: `.vault-tree button { width:100% }` meant for tree items also hit the form's "+" button, which then starved the flex input. Selector scoped to `li > button`, form buttons pinned `flex:none`, input given a visible focus ring. Lesson for the module note pile: style leaf semantics, not containers.
- Owner feedback on MVP-001 recorded (`briefs/feedback on  MVP-001.md`) and taken up as pre-S02 dogfooding micro-units (continuous-gate practice). Unit 1: `.md` is now implied in every displayed file name — vault/project trees, search results, tab titles, dev docs labels (`noteDisplayName` helper + display-side strip; relPaths, tooltips, and file bytes untouched) — and the collapsed→expanded tree toggle lost its pill background: floating icon, same hover as the expanded toggle. Tests 98 → 100.
- CP-MVP-002 S01 executed: reality reconciled (tree clean apart from the owner's untracked dogfooding files, kept by choice; 98 tests / 16 suites green), `base_commit 09e2e38` recorded in frontmatter and Work Ledger. Next action: S02 — the capture session server.
- CP-MVP-001 REVIEWED AND CLOSED (owner decision). The S12 two-week gate is reformulated into the standing continuous-dogfooding practice — rationale and audit in `sessions/2026-07-06-cp-mvp-001-review.md`. The gate's first genuine finding closed it: M2 without sources has thin daily value, while the flywheel had already produced and resolved 10+ owner frictions on day one. Priorities patched: M3 opens now (sources = the missing value), cross-note evidence flagged for early M8, folder-first creation as an M3-era micro-unit candidate, deferred items carried with re-entry points. CP-MVP-002 (capture sources + local speech baseline, M3) created per the register's opening rule — 07/08 read as required, coverage audit complete (every page 00–35 accounted), mock-first transcription, 33+trace-contract promoted to Required (transcription traces exceed the S09 minimum). ACTIVE.md and the register updated: one active parent path, as always.
- CP-MVP-001 S11 executed — the last build step + the acceptance ritual. Search slice (M1): `search.ts`, a deliberate no-embeddings lexical scan (filename/heading/text kinds, line numbers, hard caps, tree-panel UI with debounce) — ripgrep/FTS5 take over at M8 behind the same channel. ACCEPTANCE RUN recorded durably in `sessions/2026-07-06-s11-acceptance-run.md`: every M0/M1/M2 line evidenced PASS except two honest exceptions (remote-content line STRUCTURAL until M5's first remote view; budget/cancellation DEFERRED as the M7 provider's entry criterion — a synchronous zero-cost mock has nothing to enforce). Destructive proof passed: full state-dir wipe between launches, vault byte-intact, git clean. Thinness held across M2; the 28 and 33 conditional triggers never fired path-wide. Tests 93 → 98 / 16 suites. Learning note 09 (retrieval ladder; PASS/STRUCTURAL/DEFERRED honesty). THE PATH NOW WAITS ON S12 — the owner's two-week experiential gate; functional tests alone do not close it. Next action: S12 (owner-executed; agent supports).
- CP-MVP-001 S10 executed — the anti-confidence-theater brick (06 §mechanical labeling). `truth.ts` (truth-core validator seat: validators never call AI): providers submit ClaimCandidates that can assert FORM only — the type admits `interpretive`/`needs-citation` and nothing else; every label is COMPUTED: exact containment in a supplied selection → `source-backed` with hashed evidence (quote + sha256; reproducible forever), no fuzzy matching by design (a paraphrase stays model-only — tested), derivability outranks asserted forms, smuggled labels fall to model-only (adversarial-tested). The mock now exercises all four MVP labels on every response. Panel: truth chips per claim, [source] selects+scrolls the exact quoted range in the editor (citation opens its anchor), [challenge] qualifies the claim inside the editable proposal — the repair patch preview flows through the normal accept path (a doubt becomes a reviewable diff). Stayed strictly within the four MVP labels: the 28 trigger never fired. Tests 87 → 93 / 15 suites; smoke e2e shows the four labels through the full stack. Learning note 08 (derivability, quote hashes, form vs evidence, asymmetric trust). Next action: S11 — acceptance run + the search slice.
- CP-MVP-001 S09 executed — the execution-economics discipline starts at the mock (00/33): `ActionTraceLedger` (execution-core seat) appends ONE JSON line per operation to `.atomik/usage/private/actions.jsonl` at DECISION time (accepted/edited/rejected; failures append immediately; quit flushes undecided). Exactly the S09 minimum wearing 06's final ActionTrace shape — ids, honest `deterministic` location for the mock, tokens NAMED estimated (chars/4), wallMs, EUR 0 estimated external, contentRecorded=false — and a unit test greps the ledger for prompt/selection text so content can never silently leak into telemetry. Compact badge in the AI panel (location · €0 external · ms · ~tokens est) with the ledger path in its tooltip. The 33/trace-contract trigger never fired: no field beyond the minimum. Tests 81 → 87 / 14 suites; smoke e2e run→summary→resolve verified and the real line inspected. Learning note 07 (append-only ledgers, estimated vs actual, measuring without spying). Next action: S10 — mechanical truth labels.
- Owner dogfooding caught two S08 UX defects within the hour (the flywheel works). (1) AI new-note: the file WAS created but nothing refreshed or opened, and the typed path resolved from the vault root while the user thought from the current note — fix: destination path prefilled beside the source note (visible, editable), tree refresh + open on create. (2) Accepted replace/append lived only in the buffer: switching to Read showed the old file and the discard guard could silently eat the accepted patch — fix: accept applies AND saves immediately (the editable preview is the review; Ctrl+Z + save reverts). Semantics corrected in learning note 06 and the module note.
- CP-MVP-001 S08 executed — the first AI patch loop is alive, mock-first (06). Contracts shipped in `shared/ipc-contract.ts`: AiOperation (free instruction first-class, presets as scaffolding, three destinations), AiResponseBundle (open block kinds; claims/evidence/verification/uncertainties/actionTraceIds present-but-empty — S09/S10 seats), PatchProposal (pending/accepted/edited/rejected). The mock sits in MAIN (the provider seat, 12) behind `atomik:run-ai-operation`, pure compute with validated input — the AI channel has no filesystem path; accepted patches flow through the editor buffer (undoable, drift-guarded, proposal editable) + explicit save, or createNote (wx) for new notes: "one accepted operation = one clear diff" inherits S05/S07 wholesale. Tests 72 → 79 / 12 suites; smoke runs the channel e2e from the renderer world (ai=ok:2/1/0/0:append). Honest gap: panel UI flow validated by owner dogfooding + learning-note exercises. Learning note 06 (contracts-first, the provider seat, why the channel cannot write). M2's skeleton stands; S09 (trace) and S10 (labels) flesh it. Next action: S09.
- Owner friction fix (post-S07): a single Enter in the editor rendered as a space in the reader (CommonMark soft break). The writing surface now renders with `breaks: true` (note-taking expectation) while Dev Docs stay strict CommonMark — the corpus is hard-wrapped for editing comfort and must keep joining soft breaks. Render-only change; file bytes untouched.
- CP-MVP-001 S07 executed (11 read first). Editor: CodeMirror 6 over the RAW note (frontmatter visible, no template, no save-time normalization — 11/27 byte-fidelity chain intact), explicit save (button + Mod-s), dirty flag with a navigation guard, and optimistic concurrency: saves carry the read-mtime, `writeNote` refuses stale writes ('conflict'), returns the new mtime (handshake chains saves), conflict banner offers reload-from-disk or overwrite-anyway. Read/edit mode persists per tab. React×imperative-lib discipline recorded: one EditorView per mount, keyed by note path, refs for fresh closures. Dark theme follows the OS. Tests 71 → 72 (conflict matrix with utimesSync-forced mtimes). Smoke: edit-mode tab restored, CodeMirror rendering the note raw (screenshot). Honest gap logged: typing/save UI flow validated by owner dogfooding, not automated E2E. Learning note 05 (CodeMirror architecture, optimistic locking, the three data-loss nets). M1 is functionally complete except S11's search acceptance. Next action: S08 — the AI patch loop.
- CP-MVP-001 S06 executed. Project bundles (04): manifest-detected (`project.atomik-project.json`; scan skips denied dirs, never descends into a project), `createProject` as idempotent ENSURE — creates only missing manifest/index.md/log.md with `wx`, adoption of an existing folder proven byte-identical for pre-existing files, manifest identity wins on re-create. Two new channels only; ProjectView reads through the existing vault family and scopes the tree with a pure `findSubtree` helper (rule kept: derive in the renderer when data exists, add channels only for new authority). Third markdown-reading view triggered the reuse threshold: shared `useVaultNote` hook extracted. Deviation from 04's manifest example recorded: no `root` field. E2e: project created through the running app on a Git-tracked vault (git sees only the untracked folder), project tab restored from workspace state (screenshot). Tests 60 → 71 / 11 suites. Learning note 04 (conventions, idempotence, slugs/NFKD). Next action: S07 (read 11 first).
- CP-MVP-001 S05 executed. Vault IO (04/27): six-channel family behind the typed bridge — native-dialog vault opening (user-mediated capability; renderer never names roots), validated vault-relative `.md` paths with a denylist (`.git`, `.atomik`, `node_modules`, dot-dirs; symlinks neither listed nor readable), byte-exact atomic writes with split verbs (writeNote = edit-only, createNote = exclusive `wx`), last vault remembered in `.atomik/local-settings.json` (main-only). VaultView tab: tree + rendered read view + note creation (editing arrives S07). Verified e2e on a Git-tracked scratch vault: restore + render, renderer→disk write byte-exact (cmp), one edit = one-file diff, and the headline 27 guarantee proven mechanically — open + render leaves `git status` empty. Two implementation lessons kept: readdir doesn't follow symlinks (made policy: invisible AND unreadable) and `.`-segment tolerance vs hidden-dir rejection. Tests 46 → 60 / 9 suites. Learning note 03 (consent dialogs, wx/TOCTOU, byte fidelity, no-rewrite-on-open). Next action: S06.
- CP-MVP-001 S04 executed (03 read first). Workspace: recursive pane tree (leaf tabs / splits, drag-resize with clamped fraction), pure operations in `renderer/src/workspace/model.ts` (incubating workspace-core), zustand store with 500 ms debounced persistence, state in `.atomik/local-workspace.json` written atomically (temp+rename per 27) and validated with hard caps in main (renderer never names a path). Tab kinds are an open set: 'home', 'dev-docs' (per-tab docPath persists). Collapse rule: emptied child leaf folds into its sibling; the root leaf may stay empty. Corrupt/invalid state reads as null → default layout, never a crash. Smoke: fresh dir → panes=1; pre-seeded fixture → 38/62 split restored, panes=2, screenshot reviewed. Tests 28 → 46 / 8 suites. Deferred from 03, recorded: move-tab-between-panes, pin, focus mode. S01 .gitignore note resolved: `.atomik/` stays fully ignored; shared workspace.json deferred until collaboration. Learning note 02 (React state, immutability, recursion, atomic writes) added. Next action: S05.
