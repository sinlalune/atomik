# Log — Atomik knowledge and execution plane

## 2026-07-05

- Plane created by v0.6 (ADR-009). Coding path CP-MVP-001 opened as the active path; execution rules (time-to-M2, thinness rule, mechanical labeling, experiential gate) promoted from the 2026-07-05 design conversation into the corpus.

## 2026-07-06

- CP-MVP-001 S01 executed (bootstrap per 22). Reconciliation result: dual-plane tree matches the ADR-009/35 layout; all 100 payload files verify against the upload guide's sha256 manifest; no nested Git repositories; `git init` + seed commit already done per the guide. Seed commit `4675233` (branch `master`) recorded as `base_commit` in the path frontmatter and Work Ledger. Observation, not a defect: the seeded `.gitignore` ignores all of `.atomik/`, stricter than 27's itemized default that leaves `.atomik/workspace.json` committable for shared layouts — revisit when S04 introduces workspace state. Next action: S02.
- CP-MVP-001 S02 executed. Code plane activated: root npm-workspaces manifest + `apps/desktop` (electron-main / electron-preload / renderer per 14, wired with electron-vite; CJS deliberately, sandboxed preload). Security contract of 13 pinned in code (`SECURE_WEB_PREFERENCES`) and enforced by tests; one narrow typed bridge method (`atomik:get-app-info`) declared in `shared/ipc-contract.ts`; 13 §IPC trigger honored. Verified end to end: typecheck clean, 9 tests / 3 suites green, build clean, real launch on WSLg prints `ATOMIK_SMOKE_OK` exit 0. Dependency alignment recorded in the module note (electron-vite 5 pairs with vite 7; plugin-react ^5 / vitest ^3 until vite 8 pairing). First module note: `docs/modules/atomik-desktop.md`. Next action: S03 (read 16 first).
- CP-MVP-001 S03 executed. Dev Docs tab (16 MVP slice): grouped docs tree + rendered Markdown over the real `docs/` files through two new validated read-only IPC channels (13 §IPC honored; traversal, extension-allowlist, and symlink-escape guards unit-tested). Bedrock diagrams inline as SVG data URIs; `#dev-docs:<relPath>` deep-links a page; generated artifacts (`docs_source.json`, `index.html`) excluded from the tree by design. Smoke now waits for the rendered view and can capture a screenshot (`ATOMIK_SMOKE_DOC` / `ATOMIK_SMOKE_SHOT`); verified visually on index.md and 22-agent-handoff with its D08 diagram; 8 groups / 70 files enumerated. Implementation lesson promoted to the module note: React-owned innerHTML must not be patched post-render — inline assets in the HTML string before rendering. Tests 9 → 28. Next action: S04 (read 03 first).
- UI naming convention set by the owner: the app displays its own name in lowercase ("atomik") across UI surfaces (window title, header brand, home heading); documentation prose keeps "Atomik". Recorded in the module note.
- Owner feedback promoted into the docs system: module notes alone don't teach a beginner the technologies/methodology/concepts behind what agents build — ownership requires a beginner layer. Added `docs/learning/` (17 gains the doc type + a maintenance trigger "first use of a new technology/framework/pattern"; 24 gains the template). First note `01-electron-shell-from-zero.md` covers S02–S03 with take-over exercises. Standing rule for the rest of CP-MVP-001: each step that mobilizes a new technology extends the learning layer in the same work unit.
- First-use owner reactions to S04 captured in `brainstorm/2026-07-06-panes-tabs-navigation.md`: tab-groups-per-pane is a loved differentiator (protect through future redesigns); per-view docs tree vs single workspace sidebar left open, to decide no earlier than S05 with experiential data.
- Owner feature request: tree panels gained expand/collapse — Dev Docs groups are now collapsible (native details, like the vault/project folders already were), and every tree panel can collapse entirely (per-tab `tree` param, persisted with the workspace). Follow-up polish on owner feedback: the toggle glyphs read poorly and sized inconsistently (second CSS selector leak, `.devdocs-tree button` — same class of bug as the input fix; both now scoped to `li > button`) — replaced by a shared inline-SVG sidebar icon, uniform everywhere.
- Owner friction fix: the note/project creation inputs were unreadable (collapsed to ~20px) — a CSS selector leak: `.vault-tree button { width:100% }` meant for tree items also hit the form's "+" button, which then starved the flex input. Selector scoped to `li > button`, form buttons pinned `flex:none`, input given a visible focus ring. Lesson for the module note pile: style leaf semantics, not containers.
- Owner feedback on MVP-001 recorded (`briefs/feedback on  MVP-001.md`) and taken up as pre-S02 dogfooding micro-units (continuous-gate practice). Unit 1: `.md` is now implied in every displayed file name — vault/project trees, search results, tab titles, dev docs labels (`noteDisplayName` helper + display-side strip; relPaths, tooltips, and file bytes untouched) — and the collapsed→expanded tree toggle lost its pill background: floating icon, same hover as the expanded toggle. Tests 98 → 100.
- MVP-001 feedback unit 2: tree panels are drag-resizable — a handle on the panel's right edge (all three tree views), width clamped 160–520 px and persisted per tab (`treeW` param) like every piece of recoverable layout (03); `clampTreeWidth` falls back to the default on garbled params. Tests 100 → 102.
- MVP-001 feedback unit 3: the shell relict is gone — ShellHome, the 'home' tab kind, the +shell action, AND its whole IPC slice (`get-app-info` channel, preload method, `app-info.ts`, its test): the surface shrank to twelve channels (13's minimal-API direction). Default first launch is now vault + docs, vault active; saved layouts holding 'home' tabs migrate to vault tabs at load (`migrateRetiredViews`, unit-tested). Bonus defect found by the flywheel: plain `npm run smoke` had silently started depending on the owner's live layout (no pane had dev-docs active → marker never rendered → timeout); smoke now takes a scratch state dir unless an explicit `ATOMIK_STATE_DIR` fixture is passed — the deterministic check can never again be broken by dogfooding. Learning note 01 re-anchored its IPC walkthrough on `readDevDoc`. Tests stay 102 (app-info suite out, migration tests in), suites 16 → 15; build + smoke green.
- MVP-001 feedback unit 4 — the modern header: both "atomik" top rows are gone (brand row deleted, native title bar dropped via `frame: false`); the tabstrip IS the window top row. Empty strip area drags the window (CSS drag regions, every button opts out); min/max/close are custom buttons seated in the top-right pane's strip (`topRightLeafId`, pure + tested) behind ONE new allowlist-validated channel `window-control` (13 §IPC re-read; surface now thirteen channels). 13's posture untouched — the security suite additionally pins `frame: false`. Known limit recorded: OS-shortcut maximize refreshes the icon on the next click. Learning note 01 gains the chromeless-window section. Tests 102 → 105; smoke + screenshot reviewed (single top row: tab, tab actions, window controls).
- MVP-001 feedback unit 5 — auto-save by default, manual as the toggle: debounced 800 ms save after typing pauses + flush when the editor unmounts + save-then-switch on Read; no discard prompts in auto mode (the flush replaces them), and auto-save NEVER forces — a conflict banner pauses it until the human resolves (the mtime handshake is unchanged; automation must not out-privilege the manual path). `saveMode` lives in a new validated `settings` map on the workspace state (main caps keys/values like tab params; old files stay valid). Note-bar shows the quiet auto/manual toggle + Saved state. Learning note 05 §4 rewritten around the policy layer with the honest residual-loss window recorded. Tests 105 → 109.
- MVP-001 feedback unit 6 — search in every mode, each over its own perimeter: the project tree searches its bundle (existing `search-vault` channel + validated optional `scope` folder — `resolveSearchScope` rejects traversal/absolute/hidden/denied segments; vanished folder = empty perimeter), dev docs search their bundle (new `search-dev-docs` channel, the SAME scan bound to docsRoot — no second engine), vault unchanged. Third consumer triggered the reuse rule: the debounce hook (`useTreeSearch`) + result list (`SearchResultsList`) extracted and VaultView refactored onto them. Fourteen channels; 13 §IPC honored (contract + preload + routing test same unit). Tests 109 → 112; smoke's live search proof still green.
- MVP-001 feedback unit 7 — the index/log pills generalized: every folder in the vault/project trees that holds the 04 convention files gets a top pill row ([index] [log], active-aware) with those files hidden from its list; the right-docked eye per row reveals them (disposable per-folder state). The twin tree renderers (FolderView/ProjectTree) collapsed into ONE `NoteTree`; `splitPillNotes` is the pure seam (index before log, case-insensitive, near-misses stay listed); the project-shortcuts row is subsumed by its own generalization. Decision recorded: dev docs keep the grouped list — pills express the bundle convention, which the docs corpus doesn't follow. Tests 112 → 114; rebuilt screenshot verified root + nested pill rows on the live plane (and caught a stale-bundle trap: screenshots must follow a build).
- MVP-001 feedback unit 8 — seamless editing lands: note modes are now read / live / source with LIVE the default ("same functionality as obsidian"; source stays "for ide lovers", retired 'edit' param maps forward). `editor/live-preview.ts` decorates the RAW buffer from the syntax tree — headings sized with hidden `#`s, emphasis/inline-code/strikethrough marks folded, links collapsed to styled text, `-` bullets, quote/fence lines — and any line the selection touches reveals its full syntax (the reveal rule). Pure StateField, tested headless (12 new tests incl. the multi-line-selection reveal); decoration-only, so 11/27 byte fidelity is structurally untouched. live↔source swap through a compartment on ONE EditorView (buffer/undo survive); only the read switch passes the save-first gate. GFM base so strikethrough/tables parse. Screenshot review caught the frontmatter misparse (giant setext heading) → `frontmatterEnd` guard styles the block as one dim unit and suppresses markdown inside. Learning note 10 (decorations, trees, compartments, the reveal rule). Tests 114 → 126; both smokes green.
- MVP-001 follow-up (owner, same-day, with maximized-window screenshot) — unit 9: live mode loses the IDE gutters. `basicSetup` retired; the editor chrome is composed explicitly — shared writing essentials (history, search, brackets, selection match, keymaps) + SOURCE-only trimmings (line numbers, fold gutter, active-line highlight) living in the same compartment as the live-preview extension. The codemirror meta-package leaves the dependency list; commands/search/autocomplete + lang-{javascript,html,css} declared at their installed versions. Tests stay 126; typecheck green.
- MVP-001 follow-up unit 10 — blocks render in live mode: fenced code nests real language highlighting (installed packs js/ts/jsx/tsx/html/css via `codeLanguages`; registry package deferred), task markers become clickable checkboxes (the click dispatches an ordinary buffer transaction — dirty/auto-save/undo apply; dash folds away; done items struck), `---` draws as a rule, tables style mono with dimmed pipes + bold headers. Ctrl/Cmd+click follows internal links in live (pure `linkHrefAt`; plain click keeps placing the cursor; external schemes stay inert pending a vetted opener per 13). The owner's read-mode question recorded as a brainstorm note with the lean: keep read cheaply (faithful render, plain-click reading flow, no accidental edits), re-decide at M5 with usage evidence. Tests 126 → 130; screenshots verified (caught the leftover task dash before commit).
- MVP-001 follow-up unit 11 — the maximized-window bug (owner screenshot): two mechanical causes found. (1) The tabstrip's overflow SCROLLBAR sat inside the `-webkit-app-region: drag` surface — visible but unclickable ("can't interact properly"); the scrollbar is now hidden and the wheel scrolls overflowing tabs. (2) Dragging a maximized frameless window glitches under some WMs (WSLg/Weston): maximize state is now PUSHED over the first main→renderer event channel (`atomik:window-state-changed`, boolean payload; subscription + unsubscribe routing tested), mirrored to `<body data-maximized>`, and CSS drops every drag region while maximized — clicks land where they look. Side effect: the maximize icon now tracks OS-initiated maximize too (previous known limit resolved). Residual to verify in the owner's environment: WSLg input-offset under fractional scaling is an environment-level suspect if symptoms persist. Tests stay 130 (+push routing assertions inside the surface suite); smoke green.
- MVP-001 follow-up unit 12 — the maximized transparent-gap/right-offset bug (owner: only F11 rendered correctly): diagnosed as Chromium's client-side shadow on frameless Linux windows — its invisible frame margins are honored by WSLg's window integration even when maximized, while fullscreen drops them (hence F11 looking right). A local probe confirmed Electron's own logical bounds are correct — the defect is presentation-layer, so the fix removes the liability: `hasShadow: false` on Linux only (WSLg's OS side draws window shadows itself; macOS keeps its native shadow; the platform is injectable in `buildMainWindowOptions` for the new test matrix). Escalation ladder recorded in the module note: if symptoms persist, test native Wayland via `ELECTRON_OZONE_PLATFORM_HINT=auto` before promoting it to code. Tests 130 → 131; awaiting owner confirmation on the real desktop.
- MVP-001 follow-up unit 13 — maximize gap persisted after the shadow fix (owner retest), so rung 2 of the recorded ladder is promoted to code: main now sets `ozone-platform-hint=auto` on Linux before ready — native Wayland under WSLg instead of the XWayland path whose stale frame margins caused the gap/offset; 'auto' falls back to X11 on compositor-less Linux. Local probe (frameless window, maximize, bounds vs workArea) and the full smoke both pass on Wayland. Rung 3 stays reserved if the owner still sees it: map maximize to fullscreen on WSLg (their F11 observation) — owner-consented only. Tests stay 131.
- MVP-001 follow-up unit 14 — the maximize bug ROOT-CAUSED upstream on owner's suggestion to search the web: WSLg cannot maximize borderless windows at all (microsoft/wslg#1015, open since 2023, no fix — gap around the window AND clicks offset by the gap; reproduces with plain Chrome). The owner's F11 discovery was the correct answer: under WSLg, maximize now MEANS fullscreen — the □ verb toggles fullscreen, OS-initiated maximize (snap/Win+Up) converts through the maximize event, enter/leave-full-screen push the state, and `isWindowMaximized` (maximized ∨ fullscreen) keeps the icon and the drag-region CSS consistent. Rungs 1–2 kept (shadow off per wslg#1050; native Wayland). Probe-verified end to end: maximize request → fullscreen 1920×1080, never the broken state; restore returns prior bounds. Non-WSLg platforms keep real maximize. Tests 131; typecheck/build/smoke green.
- MVP-001 follow-up unit 15 — live mode adopts the READING geometry (owner: the text sat glued to the left edge, unlike Obsidian): `.cm-content` in live gets exactly `.note-scroll`/`.markdown-body`'s padding (1.5rem 2.5rem 4rem) and column width (46rem), and the default CodeMirror line indent is zeroed so read ↔ live never shifts the text. Source mode keeps its IDE layout untouched. Verified with a three-pane screenshot (same note in read and live side by side; identical left edge and top offset). CSS-only; tests stay 131.
- MVP-001 follow-up unit 16 — read/live rendering parity (owner screenshots: `[mock]`/`[mock free]` turned into green underlined links in live only): two causes. (1) The lezer parser emits Link nodes for bare `[text]` (unresolved reference shorthand) that the read pipeline renders literally — live now applies link treatment (style + bracket folding + Ctrl/Cmd-follow) ONLY when the Link has a URL child; bare brackets and image syntax stay literal. (2) CodeMirror's default highlight style underlines link/heading TOKENS regardless — live now neutralizes every default underline and the lp-* classes re-assert exactly what read shows (links underlined, strike/done struck). Parity verified on the owner's own note content side by side, plus a real link and strikethrough. Tests 132.
- MVP-001 follow-up unit 17 — quote spacing parity (owner: `>` blocks tighter in live): live's quote lines now carry the same 1rem inset as `.markdown-body blockquote`; the first attempt was silently defeated by unit 15's zeroed line padding (higher specificity) — the override is deliberate and commented. Verified side by side on the same note. CSS-only; tests stay 132.
- MVP-001 follow-up unit 18 — the owner asked the right architectural question ("can't you just make the two renderings correspond?"): full unification is structurally impossible (read is a PROJECTION — margins swallow blank lines, `> ` is consumed, tables become grids; live is DECORATED SOURCE — every character stays cursor-addressable), but every parity bug shipped so far shared one root: duplicated style VALUES. Fixed at the root: note-rendering tokens in `:root` (`--note-column/pad/font/h1-3/heading-weight/quote-inset/quote-border/code-size/code-pad`) now feed BOTH `.markdown-body` and the lp-* classes — a value changed once moves both modes together. Bonus parity gains: heading sizes finally identical (live h1 was 1.47rem vs read 1.7rem), read's `hr` styled like live's rule. Verified side by side. CSS-only; tests stay 132. Note: verification runs now isolate `--user-data-dir` — the owner's live `npm run dev` instance and smoke launches collided once.
- MVP-001 follow-up unit 19 — vault switching ships (the owner asked where it was, citing the brief; the committed brief never contained it — it was the CP-MVP-001 review's owner-deferred "when necessary" item, and it is necessary now): a change-vault button in the vault tree-bar reuses the S05 `open-vault` dialog, and a successful pick broadcasts the second push channel `atomik:vault-changed` (subscription + unsubscribe routing tested). Every mounted vault/project view drops previous-vault state (note buffer, tree, search, project list; `useVaultNote.reset()`); a `projectPath` absent from the new vault falls back to the project picker; a stale `notePath` shows the selection prompt. Stale auto-save flushes against same-named files are refused by the mtime handshake — no cross-vault writes possible. Tests stay 132 (routing assertions extend the existing surface test).
- MVP-001 follow-up unit 20 — first real vault switch surfaced a raw `ENOENT: lstat docs/brainstorm` (owner report): the invalidation's reset re-ARMED the tab-param restore effect, which re-requested the OLD vault's note against the NEW root, and `readNote`'s realpath containment threw before any existence check. Three fixes: (1) the vault-changed handler now POISONS the restore guard with the stale target (old-vault paths are never re-requested; a same-named path in the new vault would be a coincidence, not the same note); (2) `readNote` answers a missing note with a human message — "note not found in this vault — <path>" — never a raw ENOENT (tested); (3) the renderer strips Electron's "Error invoking remote method" wrapper before display, and ProjectView's misleading "loading…" placeholder becomes the selection prompt. Tests 132 → 133.
- MVP-001 follow-up unit 21 — the window buttons "disappeared" in maximized/fullscreen (owner report): not a fullscreen regression — the controls lived INSIDE the scrollable tabstrip, so once enough tabs accumulated they overflowed off-screen with them (and the hidden scrollbar removed the only clue). The strip is restructured: only a dedicated `.tabstrip-tabs` region scrolls (wheel, hidden scrollbar); +project/+vault/+docs, the splits, and min/max/close are pinned right and visible at ANY tab count. Verified with an 11-tab fixture. Tests stay 133.
- MVP-001 round-2 unit 22 — live mode stops showing what read strips (owner screenshot: a screenful of frontmatter + visible ``` fences "alterate the experience"): frontmatter now folds to a clickable `⋯ metadata` chip away from the cursor (any selection touching its lines reveals the raw dim block; the editor opens with the cursor placed AFTER the frontmatter — position 0 sat inside and held it open, caught on the first screenshot); fence lines fold their ``` + language into empty tinted lines that read as the code block's padding (active line shows them dimmed for editing — reverses the v1 "fences always visible" choice, owner feedback wins). Verified on the exact bedrock doc from the owner's capture: live now mirrors read. Tests 151 → 152 (net; fold matrix reworked).
- MVP-001 round-2 unit 23 — restored windows were unresizable (owner report): `hasShadow: false` (unit 12, aimed at the WSLg maximize gap) also removed the invisible frame margins where Chromium hosts a frameless window's edge-resize handles. Reverted: the shadow stays enabled everywhere — the maximize gap is cured independently by the maximize→fullscreen mapping (unit 14), so the shadow costs nothing under WSLg anymore. Security test updated to PIN the shadow on (with the why). Awaiting owner confirmation that edge-resize works again.
- MVP-001 round-2 unit 24 — expand-all / collapse-all land in the vault and project tree bars (double-chevron icons, next to change-vault and the panel toggle): a fold epoch remounts every `<details>` with the new default, then each folder toggles freely again (React leaves user DOM state alone while the `open` prop is stable — the epoch key is what forces the reset). Tests stay 152.
- MVP-001 round-2 unit 25 — themes ship (owner: "my second screen is very bright"): `theme` joins the validated workspace settings map — system (default) / light / dark / green / blue / orange / grey / pink, the five pastels being soft tinted light palettes. Selector seated in the top row next to the window controls; `<html data-theme>` forces color-scheme so every existing light-dark() token follows for light/dark, pastels override the base tokens (--bg/--code-bg/--border/--accent/--muted); the editor's oneDark now follows the APP theme through a compartment instead of the mount-time OS query. Verified with green + dark screenshots (trees, read, live, chrome all follow). Tests 152 → 154.
- MVP-001 round-3 unit 26 — live block parity, second pass (owner dark-theme screenshots): fenced blocks carry read's pre geometry (inner 1.1rem padding; the folded fence lines take the 8px rounded caps — lp-fence-first/last); TABLES render as real widgets away from the cursor (minimal GFM parse header/delimiter/rows, click puts the cursor in the raw block; touched = the mono/dim styling for editing); the syntax highlighter's markdown TOKEN colors are neutralized in live across ALL themes (they belong to source — reading comfort wins; nested fenced-code highlighting kept, lp-* re-assert exactly read's styles); and live's canvas now uses the app tokens (--bg/--fg) instead of oneDark's own in dark. Tests 154 → 155.
- MVP-001 round-3 unit 27 — the panel header row aligns with the file row (owner request): both bars share one height token (--bar-h), the tree-bar spans full panel width with a continuous bottom border, and the collapsed floating expand icon centers on that height — collapsed or expanded, the top rows read as one line. CSS-only; tests stay 155.
- MVP-001 round-3 unit 28 — the single «+» flow (owner request, design left to the agent: chosen the tab-space chooser over a dropdown for bigger targets and a native escape): «+» sits right after the last tab (browser-style) and opens a NEW-TAB tab whose content is the chooser — Project / Vault / Dev Docs with hints — that MORPHS into the pick (`setTabView`, params reset); it closes like any tab plus an explicit Close button. The three +project/+vault/+docs buttons are gone (splits stay). Every mistake now has an exit: the project picker screen gains Close tab (`onCloseTab`), and a split's empty pane shows the same chooser with Close pane (`closeEmptyPane`, pure + tested; the root leaf is never closable — the workspace can't disappear). Tests 155 → 158.
- CP-MVP-002 S01 executed: reality reconciled (tree clean apart from the owner's untracked dogfooding files, kept by choice; 98 tests / 16 suites green), `base_commit 09e2e38` recorded in frontmatter and Work Ledger. Next action: S02 — the capture session server.
- CP-MVP-001 REVIEWED AND CLOSED (owner decision). The S12 two-week gate is reformulated into the standing continuous-dogfooding practice — rationale and audit in `sessions/2026-07-06-cp-mvp-001-review.md`. The gate's first genuine finding closed it: M2 without sources has thin daily value, while the flywheel had already produced and resolved 10+ owner frictions on day one. Priorities patched: M3 opens now (sources = the missing value), cross-note evidence flagged for early M8, folder-first creation as an M3-era micro-unit candidate, deferred items carried with re-entry points. CP-MVP-002 (capture sources + local speech baseline, M3) created per the register's opening rule — 07/08 read as required, coverage audit complete (every page 00–35 accounted), mock-first transcription, 33+trace-contract promoted to Required (transcription traces exceed the S09 minimum). ACTIVE.md and the register updated: one active parent path, as always.
- CP-MVP-001 S11 executed — the last build step + the acceptance ritual. Search slice (M1): `search.ts`, a deliberate no-embeddings lexical scan (filename/heading/text kinds, line numbers, hard caps, tree-panel UI with debounce) — ripgrep/FTS5 take over at M8 behind the same channel. ACCEPTANCE RUN recorded durably in `sessions/2026-07-06-s11-acceptance-run.md`: every M0/M1/M2 line evidenced PASS except two honest exceptions (remote-content line STRUCTURAL until M5's first remote view; budget/cancellation DEFERRED as the M7 provider's entry criterion — a synchronous zero-cost mock has nothing to enforce). Destructive proof passed: full state-dir wipe between launches, vault byte-intact, git clean. Thinness held across M2; the 28 and 33 conditional triggers never fired path-wide. Tests 93 → 98 / 16 suites. Learning note 09 (retrieval ladder; PASS/STRUCTURAL/DEFERRED honesty). THE PATH NOW WAITS ON S12 — the owner's two-week experiential gate; functional tests alone do not close it. Next action: S12 (owner-executed; agent supports).
- CP-MVP-001 S10 executed — the anti-confidence-theater brick (06 §mechanical labeling). `truth.ts` (truth-core validator seat: validators never call AI): providers submit ClaimCandidates that can assert FORM only — the type admits `interpretive`/`needs-citation` and nothing else; every label is COMPUTED: exact containment in a supplied selection → `source-backed` with hashed evidence (quote + sha256; reproducible forever), no fuzzy matching by design (a paraphrase stays model-only — tested), derivability outranks asserted forms, smuggled labels fall to model-only (adversarial-tested). The mock now exercises all four MVP labels on every response. Panel: truth chips per claim, [source] selects+scrolls the exact quoted range in the editor (citation opens its anchor), [challenge] qualifies the claim inside the editable proposal — the repair patch preview flows through the normal accept path (a doubt becomes a reviewable diff). Stayed strictly within the four MVP labels: the 28 trigger never fired. Tests 87 → 93 / 15 suites; smoke e2e shows the four labels through the full stack. Learning note 08 (derivability, quote hashes, form vs evidence, asymmetric trust). Next action: S11 — acceptance run + the search slice.
- CP-MVP-001 S09 executed — the execution-economics discipline starts at the mock (00/33): `ActionTraceLedger` (execution-core seat) appends ONE JSON line per operation to `.atomik/usage/private/actions.jsonl` at DECISION time (accepted/edited/rejected; failures append immediately; quit flushes undecided). Exactly the S09 minimum wearing 06's final ActionTrace shape — ids, honest `deterministic` location for the mock, tokens NAMED estimated (chars/4), wallMs, EUR 0 estimated external, contentRecorded=false — and a unit test greps the ledger for prompt/selection text so content can never silently leak into telemetry. Compact badge in the AI panel (location · €0 external · ms · ~tokens est) with the ledger path in its tooltip. The 33/trace-contract trigger never fired: no field beyond the minimum. Tests 81 → 87 / 14 suites; smoke e2e run→summary→resolve verified and the real line inspected. Learning note 07 (append-only ledgers, estimated vs actual, measuring without spying). Next action: S10 — mechanical truth labels.
- Owner dogfooding caught two S08 UX defects within the hour (the flywheel works). (1) AI new-note: the file WAS created but nothing refreshed or opened, and the typed path resolved from the vault root while the user thought from the current note — fix: destination path prefilled beside the source note (visible, editable), tree refresh + open on create. (2) Accepted replace/append lived only in the buffer: switching to Read showed the old file and the discard guard could silently eat the accepted patch — fix: accept applies AND saves immediately (the editable preview is the review; Ctrl+Z + save reverts). Semantics corrected in learning note 06 and the module note.
- CP-MVP-001 S08 executed — the first AI patch loop is alive, mock-first (06). Contracts shipped in `shared/ipc-contract.ts`: AiOperation (free instruction first-class, presets as scaffolding, three destinations), AiResponseBundle (open block kinds; claims/evidence/verification/uncertainties/actionTraceIds present-but-empty — S09/S10 seats), PatchProposal (pending/accepted/edited/rejected). The mock sits in MAIN (the provider seat, 12) behind `atomik:run-ai-operation`, pure compute with validated input — the AI channel has no filesystem path; accepted patches flow through the editor buffer (undoable, drift-guarded, proposal editable) + explicit save, or createNote (wx) for new notes: "one accepted operation = one clear diff" inherits S05/S07 wholesale. Tests 72 → 79 / 12 suites; smoke runs the channel e2e from the renderer world (ai=ok:2/1/0/0:append). Honest gap: panel UI flow validated by owner dogfooding + learning-note exercises. Learning note 06 (contracts-first, the provider seat, why the channel cannot write). M2's skeleton stands; S09 (trace) and S10 (labels) flesh it. Next action: S09.
- Owner friction fix (post-S07): a single Enter in the editor rendered as a space in the reader (CommonMark soft break). The writing surface now renders with `breaks: true` (note-taking expectation) while Dev Docs stay strict CommonMark — the corpus is hard-wrapped for editing comfort and must keep joining soft breaks. Render-only change; file bytes untouched.
- CP-MVP-001 S07 executed (11 read first). Editor: CodeMirror 6 over the RAW note (frontmatter visible, no template, no save-time normalization — 11/27 byte-fidelity chain intact), explicit save (button + Mod-s), dirty flag with a navigation guard, and optimistic concurrency: saves carry the read-mtime, `writeNote` refuses stale writes ('conflict'), returns the new mtime (handshake chains saves), conflict banner offers reload-from-disk or overwrite-anyway. Read/edit mode persists per tab. React×imperative-lib discipline recorded: one EditorView per mount, keyed by note path, refs for fresh closures. Dark theme follows the OS. Tests 71 → 72 (conflict matrix with utimesSync-forced mtimes). Smoke: edit-mode tab restored, CodeMirror rendering the note raw (screenshot). Honest gap logged: typing/save UI flow validated by owner dogfooding, not automated E2E. Learning note 05 (CodeMirror architecture, optimistic locking, the three data-loss nets). M1 is functionally complete except S11's search acceptance. Next action: S08 — the AI patch loop.
- CP-MVP-001 S06 executed. Project bundles (04): manifest-detected (`project.atomik-project.json`; scan skips denied dirs, never descends into a project), `createProject` as idempotent ENSURE — creates only missing manifest/index.md/log.md with `wx`, adoption of an existing folder proven byte-identical for pre-existing files, manifest identity wins on re-create. Two new channels only; ProjectView reads through the existing vault family and scopes the tree with a pure `findSubtree` helper (rule kept: derive in the renderer when data exists, add channels only for new authority). Third markdown-reading view triggered the reuse threshold: shared `useVaultNote` hook extracted. Deviation from 04's manifest example recorded: no `root` field. E2e: project created through the running app on a Git-tracked vault (git sees only the untracked folder), project tab restored from workspace state (screenshot). Tests 60 → 71 / 11 suites. Learning note 04 (conventions, idempotence, slugs/NFKD). Next action: S07 (read 11 first).
- CP-MVP-001 S05 executed. Vault IO (04/27): six-channel family behind the typed bridge — native-dialog vault opening (user-mediated capability; renderer never names roots), validated vault-relative `.md` paths with a denylist (`.git`, `.atomik`, `node_modules`, dot-dirs; symlinks neither listed nor readable), byte-exact atomic writes with split verbs (writeNote = edit-only, createNote = exclusive `wx`), last vault remembered in `.atomik/local-settings.json` (main-only). VaultView tab: tree + rendered read view + note creation (editing arrives S07). Verified e2e on a Git-tracked scratch vault: restore + render, renderer→disk write byte-exact (cmp), one edit = one-file diff, and the headline 27 guarantee proven mechanically — open + render leaves `git status` empty. Two implementation lessons kept: readdir doesn't follow symlinks (made policy: invisible AND unreadable) and `.`-segment tolerance vs hidden-dir rejection. Tests 46 → 60 / 9 suites. Learning note 03 (consent dialogs, wx/TOCTOU, byte fidelity, no-rewrite-on-open). Next action: S06.
- CP-MVP-001 S04 executed (03 read first). Workspace: recursive pane tree (leaf tabs / splits, drag-resize with clamped fraction), pure operations in `renderer/src/workspace/model.ts` (incubating workspace-core), zustand store with 500 ms debounced persistence, state in `.atomik/local-workspace.json` written atomically (temp+rename per 27) and validated with hard caps in main (renderer never names a path). Tab kinds are an open set: 'home', 'dev-docs' (per-tab docPath persists). Collapse rule: emptied child leaf folds into its sibling; the root leaf may stay empty. Corrupt/invalid state reads as null → default layout, never a crash. Smoke: fresh dir → panes=1; pre-seeded fixture → 38/62 split restored, panes=2, screenshot reviewed. Tests 28 → 46 / 8 suites. Deferred from 03, recorded: move-tab-between-panes, pin, focus mode. S01 .gitignore note resolved: `.atomik/` stays fully ignored; shared workspace.json deferred until collaboration. Learning note 02 (React state, immutability, recursion, atomic writes) added. Next action: S05.

## 2026-07-07

- CP-MVP-002 re-entry reconciliation (22 step 4): the Work Ledger checkpoint had gone stale — it described dogfooding units 1–11 and 130 tests while the repository holds units 12–21 committed through `09fbe7a` and 133 tests / 16 suites green (reverified today). Checkpoint rewritten to match reality. Lesson restated: dogfooding micro-units must update the ledger checkpoint in the same work unit, not only log.md. Working-tree notes for the owner: dogfooding files stay untracked by choice (atomik-project/projects/test/*, atomik-project/test/, docs/projects/test/); the owner-edited brainstorm note `2026-07-06-creation-flows-and-dnd.md` has a broken frontmatter fence (`---` → `--`, line 1) — left untouched, flagged. Next action: S02.
- CP-MVP-002 S02 executed — the capture session server lands in main (08 §security, 13 §capture; §IPC re-read). `electron-main/capture-session.ts` (incubating capture-core, 14): a `node:http` endpoint — no framework, the trust boundary stays readable — bound to the first non-internal IPv4 (never 0.0.0.0; loopback offline) and listening ONLY while a session is active. One session at a time; `start` mints a crypto-random id + one-time token (the token IS the QR capability, carried in `uploadUrl`), compared timing-safe; stop/expiry (5 min)/quit/a new start all kill it; every auth failure is a uniform 403 — no id/token/expiry oracle. Uploads pass four gates below the renderer: token, size (Content-Length early + streamed count, 25 MB), declared-MIME allowlist (jpeg/png/webp/heic/heif), and magic-byte validation against the DECLARED type ("content validation after upload"). Files land in the temporary inbox `.atomik/capture-inbox/<sessionId>/` with server-chosen names + meta sidecars (client names are sanitized display metadata only); the vault is untouched by construction — the inbox→vault import is S04's explicitly confirmed step. Three typed channels: `start/stop/get-capture-session` (surface: 21 invoke + 2 push). Tests 133 → 151 / 17 suites, all against the REAL server over loopback (fetch): forged/expired/stopped tokens, one-time across restarts, both size gates, MIME + magic mismatches, upload cap, byte-exact inbox writes, endpoint closed outside sessions. Smoke proof added (`ATOMIK_SMOKE_CAPTURE=1`, green). Learning note 11 (http from zero, capability tokens, never trust the label on the box). Recorded for S03: WSL2 NATs the LAN address — phone reachability needs Windows port-forwarding or mirrored networking on the owner's setup. Next action: S03.
- CP-MVP-002 S03 executed — the QR + phone page make capture usable (08 §MVP flow, §Phone page input; 03 re-read for the new tab kind). The phone page is one self-contained HTML string served by the session endpoint: `<input type="file" accept="image/*" capture="environment">` — the capture hint opens the camera where supported and the SAME input degrades to the ordinary picker elsewhere; fetch-POSTs the raw file with the sanitized-name header; falls back to extension-derived MIME when a picker hands over an empty type (the server's magic-byte gate keeps the last word); answers each status with a human message; derives its upload URL from its own address (`/c/` → `/u/`), so the token exists in one place only. Desktop: `capture` joins the new-tab chooser — `renderer/src/capture/CaptureView.tsx` starts/stops sessions over the S02 channels, renders the QR renderer-side (`qrcode` ^1.5.4 + @types, new dependency: the upload URL is a display capability the user hands to their own phone, unlike provider keys), ticks the expiry countdown, polls the inbox every 2 s, and lists received uploads with the "import arrives at S04" note. No new IPC channels. Tests 151 → 161 (page contract: input/degrade/URL derivation/self-containment; formatting helpers); smoke's capture proof now also drives the REAL tab when a state fixture mounts one — verified qr-rendered plus screenshot review (the rendered URL shows the WSL2 NAT address: the owner-side reachability caveat stands — mirrored networking or a port forward). Learning note 11 §4b (the QR as user-facing capability; what the phone page deliberately cannot see). Next action: S04 — the confirmation flow that imports inbox items into sources/captures/ per 07/08.
- CP-MVP-002 S04 executed — the explicit confirmation closes the capture loop (08 "desktop confirmation before permanent import"; 13 §IPC re-read for the new pair). `electron-main/capture-import.ts` is the ONLY code path from the inbox into the vault: per-item, in main, on a renderer request that names a validated destination (same validator as project folders) and a title. What lands is 07's canonical bundle at `sources/captures/<date-slug>/`: `original.<ext>` byte-exact (the preserved evidence), `source.md` (Atomik Source frontmatter — resource link, capture method/mime/original-name/received-at, `status: captured`, and the deliberately empty "Extracted representations" section: S06's transcription seat), `index.md` (human directory map). Write discipline inherited whole: every file `wx`, a destination already holding any bundle file is refused BEFORE the first byte lands, a vanished inbox file (double-import race) fails with zero vault side effects, and a mid-write race cleans up what it wrote. The manager gained the decide-once lifecycle (`getUpload`/`resolveUpload`): import and discard both clear the inbox files — the same photo can never enter the vault twice; imports record `importedTo` for the UI. Channels `import-capture-upload` / `discard-capture-upload` (surface: 23 invoke + 2 push). CaptureView rows carry the decision: Import… opens a prefilled EDITABLE title + destination form (the S08 dogfooding lesson: visible, editable), Discard deletes; resolved rows show their outcome. Tests 161 → 171 / 18 suites incl. the composed FULL loop (phone POST over real HTTP → inbox → import → vault bundle, inbox cleared) and the no-clobber matrix; typecheck/build/smoke green. Deferred, recorded: inbox items orphaned by an app restart are invisible until a recovery listing exists (S04 scope was session-lifetime decisions); revisit if owner dogfooding hits it. Next action: S05 — the image source tab (original beside the rendered dossier).
- Dogfooding micro-unit (owner phone test, WSL2): mirrored networking alone was not enough — `detectLanHost` now correctly sees the shared Windows LAN address (verified 192.168.1.174 picked over a VPN interface, by enumeration order), but the Hyper-V firewall blocks inbound LAN connections to WSL listeners by default, and our random per-session port made a targeted rule impossible. Design change: the capture server's port is STABLE by default (`DEFAULT_CAPTURE_PORT` 41414, `ATOMIK_CAPTURE_PORT` override, ephemeral fallback on EADDRINUSE so a taken port degrades gracefully instead of failing the session). Tests 171 → 173 (fixed-port binding; fallback with the port deliberately occupied; suites pinned to ephemeral so they never race the owner's live app). Full WSL2 phone-reachability runbook (mirrored networking + `New-NetFirewallHyperVRule` on WSL's VM-creator GUID + same-Wi-Fi caveats) recorded in the module note; learning note 11 records the lesson: an environment constraint that reshapes code gets documented with the code. Awaiting owner retest after the firewall rule.
- OWNER VALIDATION: the capture loop ran end to end on real hardware — phone photo → QR page → upload → confirmation import → bundles in the live vault (sources/captures/Pascal, Pascal 2). Environment path that made it work, all recorded in the module note: WSL2 mirrored networking + the Hyper-V firewall rule on WSL's VM-creator GUID + the stable-port design change. M3's center of gravity ("make the owner's daily use REAL") is materially closer.
- CP-MVP-002 S05 executed — the image source tab (08 "image tab views the original beside the dossier"; 03 re-read, tab kind `source-image`). New read-only channel `read-source-asset` (13 §IPC; surface 24 invoke + 2 push): image originals only (extension allowlist matching what capture can produce), the full note-path discipline reused, 50 MB cap, base64 out because the sandboxed renderer has no file access — a data URL and nothing more; a missing asset answers with a human message. `SourceImageView` renders the ORIGINAL beside the rendered `source.md` (viewer ≠ extractor, 07): the dossier's relative .md links open in place; the original is found through the frontmatter `resource:` line (`source/dossier.ts`, pure + tested). Two entry points so bundles outlive the capture session: [View] on imported rows in the capture tab, and a "View original" pill in the vault read-mode note-bar whenever the open note declares an image resource. One CSS-specificity trap avoided consciously (the generic `.note-bar-actions button` rule vs the pill — same class of bug as the S06-era selector leaks, this time caught before shipping). Tests 173 → 180 / 20 suites; verified against the owner's real Pascal bundle with a screenshot (photo beside dossier, tab labeled by bundle folder). No new learning note: no new technology mobilized (patterns covered by notes 01/03/11); note 11 unchanged. Next action: S06 — the transcription adapter contract + deterministic mock; 33 + the operation trace contract are the promoted Required reads before it.
- Dogfooding micro-unit (owner: "can I use that photo on notes now?"): read-mode notes inline vault images. The sandboxed renderer cannot load files, so `useVaultNote` now paints the note immediately and swaps every relative `<img>` (allowlisted image extensions, resolved from the note, root-escape refused, percent-encoding decoded) for a data URL fetched through the S05 `read-source-asset` channel — same validation, same read-only boundary; failed fetches keep their src, visibly broken like a broken link. Pure collect/inline pair in `vault/note-images.ts`, tested (184 total / 21 suites). Verified with a screenshot: the owner's Pascal capture embedded in a note via `![…](../sources/captures/Pascal/original.jpg)`. Known gap recorded: LIVE mode (the default) does not render image embeds yet — CodeMirror widget work, follow-up candidate alongside the read-mode image-link → image-tab idea. CommonMark note for the owner: destinations with spaces need `<…>` (`![x](<Pascal 2/original.jpg>)`).
- Dogfooding micro-unit (owner screenshots: embed showed raw text in live; "@captures" quick-actions request). (1) LIVE mode renders image embeds: the markdown `Image` node is replaced by an ImageWidget away from the cursor (the touched line reveals raw syntax — the standing reveal rule; clicking the photo puts the cursor on the embed, the table-widget pattern). Async by design: a module-level data-URL cache, widgets fire the fetch on first render, and an `imageCacheBump` StateEffect recomputes the field when it settles; widget `eq` captures the cache state at build so settled fetches re-render exactly once. The note path reaches the pure field via `notePathFacet`; `resolveEmbedPath` mirrors the read pipeline (relative only, root-escape refused, `<…>` and percent-encoding accepted). (2) "@" quick actions in the editor (`quick-actions.ts`): the ONLY registered autocomplete source — typing `@` lists source bundles (any folder holding source.md, pure `sourceBundlesOf` walk), filtering as you type; picking inserts `![name](<relative path>)` computed by `relativePathBetween` (angle brackets always — spaces stay valid CommonMark), or a dossier link when no image resource. The `!`-vs-`[` confusion this closes: the owner linked (`[…]`) instead of embedding (`![…]`) — with @, the syntax is generated. Tests 184 → 194 / 22 suites (bundle walk, relative paths, insertion shapes, headless CompletionContext runs, image decorations incl. reveal + path matrix); typecheck/build green; live-mode embed verified with a screenshot on the Pascal capture. `window.atomik` reached via a call-time globalThis accessor in both modules (they are imported by headless node tests). Honest gap: the widget's async DOM path (cache→img swap) is exercised visually, not unit-tested.
- Dogfooding micro-unit (owner: a capture renders landscape; "we need to rotate the original"). Design held against the truth model: the ORIGINAL is evidence (07/08) and its bytes are never rewritten — rotation is a recorded human correction in the dossier frontmatter (`atomik.capture.rotation: 90`, quarter turns only; `rotationOf`/`withDossierRotation` pure and roundtrip-tested, body untouched, one line ever). The image tab gains ⟲/⟳ buttons that edit the dossier through the ordinary writeNote mtime handshake; `read-source-asset` now returns the sibling dossier's rotation (resource-name matched — a second image in the same folder is not covered), and all three viewers (image tab, read inline, live widget) redraw the pixels on a display-only canvas (`source/rotate.ts`; JPEG re-encodes as JPEG 0.92, others PNG; failures fall back to the unrotated URL). Tests 194 → 199 / 22 suites; typecheck/build green; rotation verified visually on the Pascal fixture (live embed rotated 90°). Honest gaps: the canvas redraw is visually verified, not unit-tested; the live widget's module cache keys by path only, so a rotation edited while the same note is open in another view refreshes on reopen, not instantly.
- Dogfooding micro-unit (owner: "collapse all trees by default in all modes and remember the state per pane/tab"). The epoch-remount fold mechanism (expand/collapse-all defaulting open) is replaced by CONTROLLED fold state: `vault/tree-fold.ts` (pure — parse/serialize/toggledFolder/allFolderPaths, tested incl. the param-cap clamp that drops deepest paths first and identity-stability against the details mount event) holds the open-folder set; NoteTree renders `<details open>` from it and reports toggles; VaultView/ProjectView/DevDocs receive it from TabContent, which persists it per tab as the `treeOpen` param (recoverable UI state, 03 — like treeW/mode). Default = empty set = all collapsed, in all three tree views; dev docs gained the expand/collapse-all buttons the other bars already had (group ids as keys). Tests 199 → 204 / 23 suites; typecheck/build/smoke green; fresh-state screenshot shows every group collapsed. Next: S06 for real.
- CP-MVP-002 S06 executed — the transcription seat, mock-first (07/08; 33 + the operation trace contract read first, the promoted Required trigger). `electron-main/transcription.ts`: a replaceable `TranscriptionAdapter` contract (job in; markdown body + full model/model-version/runtime/runtime-version/location identity out) with the deterministic MOCK in the seat — same bytes, same output, and the output SAYS no recognition ran (a fabricated transcript would be exactly the dishonesty 08 forbids; the mock proves the pipeline, a real local runtime arrives only through a dated 34 evaluation behind the same contract). The pipeline runs in main: validated dossier path (`source.md` only) → frontmatter resource → original under containment + image allowlist → adapter → `transcript.md` written `wx` and VISIBLY derived (Atomik Transcript frontmatter: `derived: true`, `correction_state: model-output`, transcription identity, `action_trace_id`; banner heading "model output, uncorrected") → dossier updated through the ordinary mtime handshake (status → transcribed, transcription block in frontmatter, the S04-seated "None yet" line becomes the transcript link; the transcript is removed again if the dossier write races). Re-running REFUSES an existing transcript — S07's human corrections live in that file. Every run appends exactly ONE ActionTrace line (id pre-generated so the files reference it): `action: 'transcribe'`, full runtime identity, input bytes + sha256 content hash — never content (grep-checked) —, `audioSeconds: null` until the S08 audio companion, completed or failed. Channel `transcribe-source` (surface 25 invoke + 2 push); the image tab's dossier bar gains the Transcribe button while no transcript exists. Tests 204 → 212 / 25 suites; typecheck/build/smoke green. No new learning note: the mock-first seat pattern is note 06's, the trace discipline note 07's. Honest gap: the button flow is visually verified; the pipeline beneath it is fully unit-covered. Next action: S07 — the human correction flow.
- CP-MVP-002 S07 executed — the human correction flow, and the flywheel called it first (owner corrected a transcript and asked why the dossier still said uncorrected). `recordTranscriptCorrection` (transcription.ts) hooks after `write-note` in main: when the saved file is a bundle's `transcript.md` and the sibling dossier still records model-output, the DOSSIER flips — `correction_state: human-corrected` + `corrected_at`, and the extracted-representations link text follows. Design line held: the transcript's bytes stay EXACTLY what the user saved (27 — no silent mutation of a file mid-save); correction state lives in the dossier (07/08: "model output and user correction remain distinguishable"). The flip happens exactly once (gated on model-output); bookkeeping can never fail the user's save — a dossier busy at flip time is retried by the next transcript save. "Original one click away" while correcting: the EDITOR note-bar now shows the View-original pill for any note declaring an image resource — the transcript carries `resource:` in its frontmatter precisely for this. Tests 212 → 216 / 25 suites; typecheck/build/smoke green. Honest note: an external edit of transcript.md (outside atomik) flips nothing until the next in-app save — the dossier records in-app corrections, not file forensics. Next action: S08 — the audio companion.
- CP-MVP-002 S08 executed — the audio companion (08 §audio capture companion path), and the design paid off: audio rides EVERY existing seam without a new one. Capture server: the allowlist gains audio/mp4+x-m4a (ISO BMFF), audio/webm (EBML), audio/ogg (OggS), audio/mpeg (ID3/frame-sync), audio/wav+x-wav (RIFF+WAVE — same container magic as WEBP, disambiguated by sub-brand, adversarially tested both directions); same size cap (capture means SHORT audio), same token/inbox gates. Phone page: a second input (`accept="audio/*" capture` — recorder where supported, picker elsewhere), one shared upload path. Import: links and anchors label by media kind (original-audio). Viewer: the source tab plays audio originals through the SAME read-only data-URL channel (<audio controls>; rotation tools hidden; View-original pill now gates on hasMediaResource). Transcription: the pipeline is byte-for-byte the same adapter contract — the mock produces the same honest non-transcript for a voice memo; per 33 ("do not manufacture precision") the trace's audioSeconds stays null until a real runtime that actually decodes audio reports a duration (the seat exists since S06). Tests 216 → 220 / 25 suites; typecheck/build/smoke green. Owner-side validation of the audio loop from a real phone pending (same QR flow). Next action: S09 — the M3 acceptance run and path close.
- CP-MVP-002 S09 (agent half) — the M3 acceptance run is recorded durably in `sessions/2026-07-07-m3-acceptance-run.md`: every 18 §M3 build intent and truth/execution intent PASS with evidence pointers, except the two honest entries — the optional timestamp sidecar DEFERRED (meaningless while the mock decodes nothing; re-entry is the 34 evaluation) and owner-device audio upload pending (same QR flow as the image loop the owner already validated on real hardware). Security posture re-checked line by line against 13 §capture. Gate at run time: 220 tests / 23 suites, typecheck/build/smoke green (smoke marker carries the AI proof and capture lifecycle), repo at 893f735. Priority observations for the review: (1) the mock transcript is now the daily-use ceiling — the 34-gated LOCAL RUNTIME EVALUATION (OCR for pages, speech for memos) is the highest-value next investment; (2) CP-MVP-003 (PDF, M4) reuses the capture seams by construction; (3) the continuous-gate practice absorbed 26 owner-friction units this path — keep it. THE CLOSE IS THE OWNER'S: this record is the agent-side half; the owner review seals CP-MVP-002 and decides what opens.
- Dogfooding micro-units (owner, post-acceptance-run — recorded for the S09 review). (1) m4a acceptance widened: several OS pickers declare m4a as the nonstandard `audio/m4a` — aliased to the same ISO-BMFF sniffer (audio/mp4 and audio/x-m4a were already in). (2) DESKTOP MIC capture ("why only remote devices?" — no reason but scope order): CaptureView gains ● Record here — MediaRecorder in the trusted renderer, bytes handed to main over the new `add-local-capture` channel (13 §IPC; surface 26 invoke + 2 push) where they pass the SAME gates (size/MIME/magic via the shared `storeUpload` seam) and land the SAME inbox, decided by the same explicit import. No endpoint opens: with no usable session a LOCAL record is created lazily (stopped, empty uploadUrl, unreachable by construction; a full session refuses rather than orphaning undecided items; an active phone session is joined, not replaced). Electron permission posture made explicit while at it: main now grants ONLY 'media' and only to app content — everything else denied outright (previously Electron's permissive default). Tests 220 → 224 / 23 suites incl. the local-upload gate matrix and the audio/m4a alias over real HTTP; typecheck/build/smoke green. Honest gap: the MediaRecorder→channel flow needs owner validation on real hardware (WSLg audio support permitting); the channel beneath it is fully covered.
- Dogfooding diagnosis (owner: desktop mic says "microphone unavailable or permission denied"): probe-driven root cause on the real machine. WSLg's audio bridge IS present (`RDPSource` on /mnt/wslg/PulseServer, default source) — but the machine lacks the `libpulse0` SYSTEM package, so Chromium's audio stack sees zero input devices and getUserMedia fails NotFoundError. Proven both ways with an Electron probe (file:// page — mediaDevices only exists in secure contexts; a data: URL probe red-herringed as a hang first): without libpulse `inputs=0 gum=fail:NotFoundError`; with the extracted lib on LD_LIBRARY_PATH `inputs=2 gum=ok:Default`. Fix is owner-side: `sudo apt-get install libpulse0` + app restart. Shipped: the recorder's error message now distinguishes NotFoundError (with the WSL hint) from NotAllowedError; dev-environment runbook extended in the module note. 
- Dogfooding fix (owner: audio player empty for both phone-imported and desktop-recorded originals): the renderer CSP allowed `img-src data:` but declared no `media-src`, so <audio> data URLs fell back to `default-src 'self'` and were silently blocked — images worked, sound never could. CSP gains `media-src 'self' data: blob:` (blob: ahead of a future object-URL path for large audio; data: is what the viewers build today). Verified with a real WAV fixture bundle in the source tab: the player loads and enables (screenshot); audible playback confirmed owner-side. Lesson for the module note pile: every NEW media surface must name its CSP directive — fallback to default-src is a silent kill.
- Dogfooding fix (owner phone screenshot: URecorder's .m4a refused with "not an accepted media type"): Android's legacy MIME table declares .m4a files as `audio/mpeg`, so the declared-type-keyed magic check compared ISO-BMFF bytes against the mp3 signature and refused a perfectly good file. Rule inverted: BYTES OUTRANK LABELS — the server sniffs content against the known-signature table and the declared MIME pins only the media FAMILY (image vs audio). Security posture unchanged or stronger: unknown content refused, cross-family smuggling refused (image bytes can't enter as audio and vice versa; ISO-BMFF disambiguated by ftyp brand so heic stays image and m4a stays audio; RIFF split by sub-brand), and what gets stored is canonical truth (sniffed extension + MIME; the client's declared label kept in the meta sidecar as provenance). Same rule in `addLocalUpload`. Tests 224 → 227 / 23 suites incl. the owner's exact case (ISO-BMFF declared audio/mpeg → stored as audio/mp4 .m4a) and the brand-split matrix; typecheck/build/smoke green.
- Dogfooding batch (owner: desktop recording plays silent — "which device records? we need a menu"; capture panel off-center; file names overflow). (1) The desktop recorder gains a MICROPHONE MENU (enumerateDevices, labels appearing after first grant), records from the chosen deviceId, names the active device while recording, and — the diagnostic that answers silence directly — a LIVE LEVEL METER (AnalyserNode peak at ~8 Hz): a flat bar during recording means a silent take BEFORE it is imported. (2) Centering root cause: `.capture-view` was `display:flex` with `margin:auto` on the panel — auto margins collapse to flex-start once the panel overflows the container, which the uploads list made happen; replaced by plain block + `margin: 0 auto`. (3) Long unbreakable file names now ellipsize with a full-name tooltip. Tests stay 227; typecheck/build/smoke green; layout verified by screenshot. Silence on WSLg is expected to be environmental (Windows Settings → Privacy → "Let desktop apps access your microphone"); the meter now shows it live either way.
- OWNER VALIDATION completes M3's surface: desktop mic recording is audible once the right microphone is set as the WINDOWS default input — confirming the WSLg single-bridge model (RDPSource mirrors whatever Windows defaults to; the physical device is chosen in Windows, not in-app). With phone photo, phone audio, desktop audio, import, playback, transcription, and correction all exercised on real hardware, every acceptance line of 18 §M3 is validated except the two deliberate deferrals. Addendum appended to the acceptance run; nothing blocks the S09 close.
- Dogfooding batch (owner round 2 on the capture surface). (1) Centering ROOT-caused this time: `.pane-content` is a flex row and `.capture-view` never declared `flex: 1`, so the whole view shrink-wrapped to content width at the left edge — the panel was centering correctly inside a narrow strip; the previous fix treated a symptom inside the view. `.capture-view` and `.source-image-view` now grow like every other view. (2) Navigation (owner: "capture and source pages feel awkward without a tree"): new `SourcesTreePanel` — the vault-tab tree mechanics reused wholesale (NoteTree, per-tab fold state via treeOpen, resize via treeW, hide via tree param), scoped to the vault's `sources/` subtree when it exists (whole vault otherwise, pure `sourcesSubtreeOf`), refreshed on vault switch. Seated in BOTH the capture tab (clicking a note opens it as a source-image tab beside) and the source tab (clicking navigates the dossier pane in place and the tab param follows via onDossierOpened, so restore works); the source tab without a dossierPath now shows a pick-from-the-tree hint instead of a dead end. Tests stay 227; typecheck/build/smoke green; three-pane screenshot verified.
- Dogfooding micro-fix (owner round 3, "centered in height also lol"): the capture panel now centers on BOTH axes — grid with `justify-content: center` + `align-content: safe center` (the `safe` keyword degrades to scroll-from-top when the panel outgrows the pane, so tall upload lists never clip). Screenshot-verified both ways.
- CP-MVP-002 REVIEWED AND CLOSED (owner decision, 2026-07-07). All nine steps executed in one day plus ten owner-friction units fixed on report; the full M3 surface owner-validated on real hardware (phone photo/audio, desktop mic, import, playback, rotation, transcription, correction). Acceptance record sealed in sessions/2026-07-07-m3-acceptance-run.md. The owner chose priority observation 1 as the next path: the 34-gated LOCAL SPEECH RUNTIME EVALUATION, opened as CP-MVP-004 (M3-completion row in the register; CP-MVP-003/PDF stays reserved for M4). Coverage audit done at open (every bedrock page 00–35 accounted; 13 §local-inference promoted to Required with a re-read trigger before S05). Standing note carried into S02: the repo is PUBLIC and sources/ is currently untracked — the fixture git policy is an explicit owner decision before any recording lands in history.
- CP-MVP-004 S01 executed (bootstrap per 22): reality reconciled — working tree carries only owner dogfooding files and the untracked sources/ captures (their git policy is S02's explicit owner decision); 227 tests / 23 suites green at the close commit. base_commit recorded in frontmatter and Work Ledger. Next action: S02 — evaluation protocol + fixtures.
- CP-MVP-004 S02 executed — evaluation protocol + fixtures. Owner decision recorded FIRST (the repo is public): `sources/captures/` git-ignored in full — media, dossiers, and transcripts stay local; the committed protocol carries sha256 hashes and metadata so the evaluation is verifiable without publishing a voice (per-bundle publication stays an explicit owner move later). Protocol in sessions/2026-07-07-speech-eval-protocol.md: four real fixtures manifested (Android m4a memo, two desktop-mic webm takes incl. the validated audible one, a 2020 WhatsApp mp3 as the long compressed-telephony case; the Pascal images are S07 OCR fixtures), the 33 §evaluation-gates metric sheet with CORRECTION EFFORT as the quality metric (02: the owner's editing time is the truth), durations measured by the harness at run time, and the selection rule — correction effort per usable transcript minute, RTF/memory/license as gates, ties to the smaller runtime (15). Gap flagged: one deliberately code-switched FR/EN-technical memo would strengthen S04. Next action: S03 — the dated candidate refresh.
- CP-MVP-004 S03 executed — dated candidate refresh (web-checked 2026-07-07, record in sessions/2026-07-07-speech-candidates.md). All three bedrock-34 candidates advance to the bench: whisper.cpp v1.8.6 (MIT; single sidecar binary — the cleanest Electron deployment story), faster-whisper on CTranslate2 int8 (MIT; python venv; PyAV decoding built in; must be measured before being rejected on deployment weight), sherpa-onnx-node 1.13.3 (Apache-2.0; prebuilt npm addon — the natural in-process integration if quality holds). Rejected with dated reasons: Vosk (outclassed generation), Parakeet (EN/GPU), Moonshine (EN-only), whisperX (unneeded alignment), reference whisper (the slow baseline the others reimplement). French rides whisper-family multilingual weights; base + small per candidate, the correction-effort metric arbitrates size. Harness decision: PyAV decodes fixtures once to 16 kHz WAV so all candidates see identical samples. Machine verified: Ryzen 7 8700F, 8 cores, 15 GB, full toolchain, no GPU assumed. Next action: S04 — the bench harness and mechanical runs; the correction-effort pass is the owner's half.
- CP-MVP-004 S04 (mechanical half) executed — 24 bench runs on the owner's machine, results in the dated record (sessions/2026-07-07-speech-candidates.md). Facts with dates: `base` multilingual FAILS the owner's French (wrong-language detection at ~0.4 confidence, gibberish text); `small` is the floor and all three candidates produce IDENTICAL correct text on short memos; the 181 s fixture discriminates — faster-whisper alone delivered a full coherent transcript, whisper.cpp emitted *Musique* suppression, sherpa-onnx's whisper path hard-stops near 30 s per stream; every candidate hallucinates (" you") on the silent take — the adapter to be seated should carry a no-speech threshold, and the capture level-meter guard is vindicated. RTF 0.03–1.5 (long audio ≈ 0.15), peak RSS ≤ ~1.1 GB — the Ryzen is comfortable. Bench flaw caught mid-run and rerun: whisper.cpp defaults to `-l en` and silently TRANSLATED the French (recorded — the seated sidecar must always pass language explicitly or auto). Transcripts converted to .md inside the git-ignored captures area so the owner corrects them IN atomik (correction effort = the deciding metric); harness reproducible under .atomik/speech-bench/. Awaiting the owner half; the winner decision seals at its end.
- CP-MVP-004 owner research lands (docs/research/model-research.md, committed 9b374c2): a dated market study across OCR, transcription, embeddings, DSL generation (GBNF as the validity lever), and cloud pricing with per-device recommendations — future paths (M9 local assistance, M12 DSL, provider work) inherit it. Immediate S03/S04 corrections: Parakeet v3 is multilingual now (the S03 rejection was stale), and whisper large-v3-turbo is the laptop-class ceiling — both added to the bench; Voxtral recorded as the FR quality reference (GPU-class, not benched). Owner flagged the fixtures as unplanned samples; new deliberate memos incoming before the winner decision.
- CP-MVP-004 S04 extension — the owner-research candidates ran (12 more rows in the dated record). Parakeet-TDT-0.6B-v3 (sherpa int8) takes the mechanical lead: perfect short-memo French at RTF 0.05 (~200 ms), full 181 s transcript in 14.7 s with no transducer length limit, 0.6B/CC-BY-4.0, in-process npm path. large-v3-turbo is measured OUT on this CPU (10–21 s per memo, 1.6–1.9 GB, no quality gain over small on speech, classic hallucination loop on the sung fixture) — the research's laptop-ceiling assumption meets dated on-machine evidence, which is exactly what 34 is for. Winner decision waits on the owner's deliberate memos (FR + EN-technical, 30–90 s); the contenders are parakeet-v3 vs faster-whisper-small, whisper.cpp-small as deployment fallback.
- Dogfooding batch (owner: can't listen to a recording before import; exported webm plays late and cuts ~12 s in VLC/Windows). (1) ROOT CAUSE of the broken playback: MediaRecorder streams WebM with NO Duration element — external players misjudge the file; we know the elapsed time, so the recorder now patches it into the container before upload (`fix-webm-duration`, MIT). (2) PRE-IMPORT PREVIEW: new read-only channel `get-capture-upload-data` (13 §IPC; undecided items only, via the same getUpload gate) — inbox rows gain Listen/Preview showing an <audio>/<img> from the bytes, so a bad take is re-recorded instead of imported blind. Tests 227 → 228; typecheck/build green. Note: already-imported webm files keep the missing-duration flaw (re-record or ffmpeg-remux if one matters).
- Dogfooding fix attempt (owner: recordings truncate ~20 s; meter flatlines then spikes at a fixed point; previews inaudible past it): the meter reads the RAW stream pre-encoder, so the degradation is the WSLg RDP audio bridge stalling under sustained capture, not app buffering. Mitigations shipped: `PULSE_LATENCY_MSEC=60` set in main on Linux (the known WSLg client-buffer fix, inherited by Chromium's audio service), audio processing disabled on the recorder constraints (echoCancellation/noiseSuppression/autoGainControl off — less resampling load on the bridge), and `recorder.start(1000)` so data flushes each second (a stall costs seconds, not the take). AWAITING owner retest with the meter; if the bridge still stalls, the fallback is recording Windows-side (the phone QR flow already works) — an environment limit to record, not an app defect.
- REGRESSION fixed same hour (owner: "no microphone found" after the WSLg mitigations): forcing `PULSE_LATENCY_MSEC=60` from main broke device discovery on this WSLg — libpulse rejects the requested stream config and Chromium sees zero inputs. Reverted (opt-in via shell env only); the two harmless mitigations stay (processing off, 1 s timeslices). Lesson logged: environment tuning knobs ship OFF by default and get tested on the owner's machine before defaulting — the probe verified libpulse presence but not this knob.
- ROOT CAUSE (owner: le m4a 30 s "marche nickel dans VLC" mais bugge en preview/lecture in-app): pas un buffer — un CODEC. Les originaux téléphone sont AAC (ftyp mp42) et Electron est livré SANS codecs propriétaires (AAC/H.264 — licence payée par Chrome, pas par Electron): le <audio> Chromium ne peut pas décoder l'AAC, VLC si. Les webm/opus desktop (codecs ouverts) se lisent, eux. Conséquence produit: la lecture in-app des originaux AAC nécessite soit un bouton "ouvrir avec le lecteur système" (shell.openPath validé côté main — nouveau canal, 13 §IPC), soit des codecs sous licence (castlabs), soit un décodage sidecar. Décision et implémentation au prochain unit; la transcription, elle, N'EST PAS affectée (les runtimes décodent l'AAC eux-mêmes — PyAV l'a déjà prouvé sur fx-urecorder). Les fixtures benchmark* du owner sont prêtes pour le round décisif du bench.
- CP-MVP-004 S04 SEALED — the owner's deliberate 30 s memo decided in one round: whisper-small transcribed it near-perfectly in both implementations (one homophone to correct), while Parakeet v3 — the mechanical leader on trivial clips — COLLAPSED to eight English nonsense words on real French (dated finding vs its leaderboard claims; the sherpa int8 export's language handling fails as-seated). WINNER: whisper-small via whisper.cpp (MIT single-binary sidecar, server mode for warm loads); faster-whisper recorded as the quality alternate. RTF 0.16–0.38 on the Ryzen, well inside daily use. Next action: S05 — seat whisper.cpp-small behind TranscriptionAdapter as an isolated sidecar (13 §local inference re-read first), mock demoted to fallback, audioSeconds + real runtime identity in traces.
- CP-MVP-004 S05 executed — the WINNER TAKES THE SEAT (13 §local-inference re-read): `whisper-adapter.ts` runs whisper.cpp-small as a bounded sidecar — execFile with hard timeouts and SIGKILL, tmp workdir wiped, media path in / text out, zero vault access; ffmpeg (system) decodes originals to 16 kHz WAV first since whisper-cli reads wav/mp3/flac only and Electron has no AAC. Identity (model/runtime/version, location local-model) and runtime-reported audioSeconds now flow into transcript frontmatter, dossier, and the trace line (33: reported, never estimated). The seat is chosen at startup — whisper when binary+model+ffmpeg exist (state-dir defaults, ATOMIK_WHISPER_BIN/MODEL/FFMPEG overrides), the honest mock otherwise: capture never blocks on a runtime. Binary+model installed to .atomik/speech/ on the owner's machine; the one activation step left is owner-side (`sudo apt install ffmpeg`). Tests 228 → 229 (fake-subprocess pipeline: decode→transcribe, identity, audioSeconds, readiness gate); typecheck/build green.
- OWNER VALIDATION S05: première vraie transcription whisper.cpp-small "perfect" sur machine réelle après installation de ffmpeg — le siège fonctionne de bout en bout. Le "player bug ~14 s" restant sur les webm desktop est réinterprété: le média encodé S'ARRÊTE réellement là (stall du pont audio WSLg pendant la prise), mais notre patch de durée tamponne le temps MURAL — le lecteur promet donc 30 s et "bugge" à la fin réelle (~14 s). Ce n'est pas le lecteur: c'est la prise, tronquée à la source, maquillée par la durée. Correctifs candidats au prochain unit: (1) avertissement de stall EN COURS de prise (meter à zéro >2 s → bannière "entrée morte — la prise sera tronquée"), (2) ne tamponner la durée qu'à hauteur du dernier chunk reçu, (3) recommandation UX: mémos longs par téléphone tant qu'on est sous WSLg (limite d'environnement, absente des builds natifs). 
- CORRECTION du diagnostic + fix (owner: c'est le m4a TÉLÉPHONE qui coupe ~14 s in-app, pas le webm desktop): puisque 14 s JOUENT, l'AAC décode bien dans cet Electron — le log précédent sur "pas de codec AAC" est corrigé; le vrai coupable est le chemin data:-URL des éléments <audio> Chromium, qui cale en milieu de fichier sur certains conteneurs volumineux. Fix: les originaux/previews AUDIO passent par des Blob URLs (`mediaObjectUrl`, URL.createObjectURL) — pipeline média normal avec accès aléatoire; les images restent en data URLs. Retest owner attendu sur le mémo 30 s.
- Hypothèse owner (probablement juste, lol inclus): le "stall desktop ~12-14 s" était peut-être LE MÊME bug data-URL que le m4a téléphone — la lecture in-app calait au même endroit pour la même raison, et la coupure VLC d'origine était la durée manquante (déjà corrigée par fix-webm-duration). Si c'est ça, la théorie du décrochage du pont WSLg est à rétrograder: seul indice restant, le comportement du meter (zéro puis spikes), possiblement un artefact. ARBITRE DÉFINITIF: enregistrer 30-60 s desktop et la TRANSCRIRE — whisper dit exactement combien d'audio réel existe dans le fichier, indépendamment de tout lecteur. Si le transcript couvre tout: l'enregistrement desktop est sain, tous les symptômes étaient côté lecture, et la limite d'environnement WSLg est levée du registre.
- BUG FAMILY CLOSED, probe-proven (le "12-14 s" qui a survécu à trois théories): une sonde Electron sur la machine réelle a joué le m4a 30,6 s (1) MUET → parfait jusqu'au bout, (2) SON ACTIVÉ → currentTime rame puis GÈLE à 15,0 s, (3) son activé + `--audio-buffer-size=8192` → parfait jusqu'au bout. Verdict: la SORTIE audio WSLg (RDPSink) underrun sous flux continu avec les buffers Chromium par défaut — ni fichier, ni codec, ni data/blob URL, ni lecteur; le switch (8× buffers) enjambe les hoquets du pont. Activé sous WSL uniquement (WSL_DISTRO_NAME). Théories précédentes: AAC manquant = fausse; data-URL = plausible mais secondaire; stall capture = à RETESTER — les buffers d'entrée profitent du même switch, le meter et l'enregistrement long sont peut-être guéris aussi. Owner: redémarrer, réécouter le mémo 30 s, puis tenter un enregistrement desktop long.
- Suite du duel avec le pont audio WSLg (owner: 8192 fait passer le gel de 15 à ~18 s): buffer monté à 16384, ET l'échappatoire garantie livrée — bouton "Ouvrir en externe" sur les originaux audio de l'onglet source: nouveau canal `open-source-externally` (13 §IPC; même validation que la lecture d'asset via `resolveSourceAssetAbs` extrait de readSourceAsset; shell.openPath côté main) → le lecteur système Windows lit sans passer par le pont. Si 16384 ne suffit toujours pas, la lecture longue in-app est actée limite d'environnement WSLg (sortie), le bouton externe est le chemin officiel, et les builds natifs n'auront rien de tout ça.
- Clôture du duel WSLg-sortie: 16384 gèle vers ~16 s (15/18/16 selon les runs = bruit) — la taille de buffer ne vaincra pas le pont; la lecture in-app LONGUE est actée LIMITE D'ENVIRONNEMENT WSLg (sortie), symétrique du soupçon côté capture. Le chemin officiel sous WSL: "Ouvrir en externe" — corrigé pour WSL (pas de xdg-open dans la distro): `wslpath -w` convertit le chemin et `explorer.exe` le confie au lecteur Windows par défaut, audio natif sans pont. Les builds natifs ignoreront tout ce paragraphe.
- CP-MVP-004 S06 executed — le sidecar de timestamps (04 relu: un sidecar machine ne cache jamais le savoir; transcript.md reste canonique). whisper-cli émet -oj; `parseWhisperSegments` (tolérant au drift de forme) → `segments.json` écrit wx à côté du transcript quand des ancres existent, lié depuis le corps du transcript, nettoyé si l'écriture du dossier échoue; la trace porte l'audioSeconds rapporté. Le volet "correction sur vrai contenu" était déjà validé par l'owner (mémo transcrit "perfect" + flip S07). Tests 229 → 231; typecheck/build verts. Next: S07 — OCR GO/NO-GO daté sur les pages Pascal.
- CP-MVP-004 S07 executed — OCR GO/NO-GO daté sur évidence réelle: RapidOCR (Apache-2.0, onnxruntime CPU) a lu la photo Pascal oblique — 20 lignes de français exploitable en 3,6 s (accents partiellement perdus, ordre des lignes brouillé par l'obliquité — appliquer la rotation du dossier avant reconnaissance est la première amélioration notée). VERDICT: GO sur la capacité, SIÈGE différé en décision propre — le blocage est le déploiement (python), la même question qui a départagé S05; options consignées (onnxruntime-node + pré/post Paddle, sidecar python, candidat VLM de la recherche owner). Le contrat d'adaptateur est prêt: les images monteront sur le même siège quand un choix sera fait. Next: S08 — acceptance + clôture (décision owner).
- INCIDENT PREUVE + FIX (owner: original.jpg de Pascal disparu, remplacé par original.jpg~RF23f98d33.TMP): le TMP ÉTAIT l'original intact — sha256 identique au manifest (de39201b150aa622), taille identique. Cause: le bouton "Ouvrir en externe" donnait le VRAI fichier à explorer.exe → Windows Photos a tenté un ReplaceFile (sauvegarde de rotation?) qui a avorté sur le pont 9P WSL, laissant le rename orphelin. Restauré par mv + hash-vérifié. FIX structurel: open-externally copie désormais l'asset dans un répertoire temporaire et ouvre LA COPIE — un lecteur externe ne touche plus jamais une pièce à conviction. Leçon 08 en action: les hashes du manifest ont permis de prouver l'intégrité en une commande.
- CP-MVP-004 S07 addendum 4 — le bench OCR se referme (2026-07-08), les deux « pannes » du handoff étaient des fantômes. (1) Qwen3-VL n'a jamais été cassé: l'encodage image de la photo 12 MP prend 105–107 s CPU et les observations d'hier (25/87 s) tombaient PENDANT — 2B: 291 s/page ≈ la référence qualité; 4B: 441 s = MEILLEURE qualité du banc (seul à lire le bloc marginal Husserl + l'appel de note ¹), Apache tous les deux. (2) Unlimited-OCR (MIT, arch DeepSeek-OCR): charge dès master+`--jinja` (#17400 mergé 2026-03-25) mais génère VIDE EN SILENCE (exit 0) sans le build PR#24975 (R-SWA, DRAFT, head 649864fc6) ET la recette exacte de l'auteur (`--chat-template deepseek-ocr`, « document parsing. », DRY) — la recette du README HF échoue silencieusement sur les deux builds, piège documenté. (3) Tier GPU complet (RTX 5070/WSL2; bug VMM `cuMemSetAccess: device not ready` → rebuild NO_VMM): Unlimited-OCR Q4 = 8,6 s/page COMPLÈTE et structurée (régions typées + folio; seul « obligeamment » correct du banc) — sa troncature CPU (tiers bas perdu à 25 s/page, ~700 tokens puis arrêt) disparaît sur GPU; qwen25vl-3b 12,9 s complet; gemma3 8,5 s mais EFFONDREMENT bas de page (« Vous Hilton lecouvrez… toujours million », boucles inventées) = le problème de confiance, prouvé; les tours vision qwen3vl/7B débordent les 12 Go à résolution native (spill mémoire unifiée → GPU PLUS LENT que CPU; hybride 7B `--no-mmproj-offload` 105 s mais perd le chapeau d'intro); IQ3_XXS d'unlim complet en 7,0 s mais dégradation de quant mesurée (« l'obze », « je le fais » contresens). (4) Question Unsloth de l'owner (« 2B parfait en secondes », UD-Q4_K_XL) décomposée par contrôles: la VITESSE = budget de pixels du runtime (capé 1024 tokens → 9,5 s, encode 0,27 s; les métadonnées du fichier unsloth n'embarquent AUCUN cap — diff cosmétique), mais le cap coûte des LETTRES à toute précision (Q8_0 compris: « je le fais » persiste) et l'artefact exact de l'owner à résolution native CPU sort un transcript OCTET-IDENTIQUE au Q4_K_M bartowski (334 s vs 291 s) — image de test différente probable côté owner (offre ouverte: la déposer dans captures/ pour la banc-tester). Récap siège (décision owner, options datées dans l'addendum §implications): unlim-GPU MIT 8,6 s structurel mais PR draft + floor CPU tronqué → garde-fou de complétude obligatoire; floor CPU qualité = qwen3vl-4b Apache; RapidOCR 3,6 s inchangé pour l'instantané; échelle 33 = les deux. Artefacts locaux sous sources/captures/speech-bench-2026-07-08/ (zone git-ignorée), harnais + runs bruts sous .atomik/speech-bench/.
- CP-MVP-004 S07 addendum 5 — tier « scan propre » (idée owner) + LE harnais Qwen correct + licence 3B vérifiée (2026-07-08). (1) Page Pascal 2 (Leibniz) scannée deux fois: scan-clean.py maison (20 lignes PIL: aplatissement d'illumination + contraste, 12 MP) et Adobe Scan owner (dewarp, 3,4 MP) — les deux marchent comme entrée VLM, différences marginales au même budget. (2) BUG llama.cpp isolé puis contourné: Qwen3-VL 4B stalle sur ces fichiers capé OU natif (encode ×40–100 puis ~2 tok/s, solo reproduit; familles d'issues #17345/#17012/#17172 — le web savait, le probing local seul non); LE HARNAIS CORRECT = pré-redimensionner soi-même au budget de tokens (≈2 500/page dense, multiples de 28, -c 8192), ne JAMAIS compter sur --image-max-tokens — c'est la mécanique des pipelines HF/Unsloth, la question Unsloth d'hier refermée par la racine. Résultat: 4B 9,5 s GPU / 154 s CPU (le floor passe de 441 s à 2 min 34). (3) SIÈGE rebattu: Qwen3-VL 4B Apache @2,5k × Adobe = MEILLEUR TRANSCRIPT DU BANC (TEXTE 5, notes a/b, ancres d'appels de note inline, 2 fautes de mot); la licence Qwen2.5-VL 3B est VÉRIFIÉE SUR TEXTE (owner): « research or evaluation purposes only » + juridiction Hangzhou → siège interdit, et le 4B Apache le remplace de toute façon. unlim COLLAPSE à 40 % sur le scan 3,4 MP (fragilité corrélée aux pixels bas — exactement ce qu'un pipeline PDF lui donnerait): jamais le nourrir en basse résolution. (4) Owner propose Mistral OCR 3.0 (API) comme référence plafond du banc (statut Voxtral) et option in-app opt-in pour scans douteux — consigné dans l'addendum, à bencher sur clé owner, intégration = coding path provider (13/28: clé hors renderer, sortie cloud-derived, opt-in par capture). Décision de siège: owner, options complètes dans l'addendum 5.
- Référence plafond API benchée (2026-07-08, clé owner via .env.local git-ignoré, jamais dans le dépôt ni la conversation): Mistral OCR — id résolu `mistral-ocr-4-0` (= `latest` prouvé octet-identique; la « 3.0 » essayée par l'owner a déjà un successeur serveur). Photo brute (page Pascal, folio 10): meilleur transcript brut du banc, 3 micro-fautes (« parier », « cogitationes », « pp. 16-19 »), 3,1 s API. Scans (page Pascal 2, folio 11 — Adobe ET scan-clean.py maison): LETTRE-PARFAIT sur toutes les sondes, ancres de notes en exposants Unicode, ~2,4–2,9 s, sorties de même taille sur les deux scanners — le scanner maison égale Adobe à travers le plafond. Écart local→plafond à entrée identique: ~2 fautes de mot (Qwen3-VL 4B @2,5k Apache) → ~0. Précision de méthode consignée sur rappel owner: les tiers photo et scan travaillent DEUX PAGES DIFFÉRENTES (seule Pascal 2 a un Adobe) — classements intra-tier à entrée identique, comparaisons inter-tiers = inter-pages. Statut: référence datée façon Voxtral, PAS un candidat au siège local; l'option in-app opt-in (scans douteux) reste un coding path provider à proposer (13/28). Artefact: OCR-mistral-reference.md; harnais mistral-ocr.py.
- CP-MVP-004 CLOS (2026-07-08) + CP-MVP-005 OUVERT sur directive owner — la décision de siège différée par S07 est tombée: « add CUDA to whisper; seat Qwen3-VL 4B CUDA with CPU fallback; only Mistral OCR API as fallback » (le « Qwen 3.5 » initial corrigé par l'owner en Qwen3 — noté au passage: Qwen3.5-4B (2026-03-02) est nativement multimodal et revendique de battre Qwen3-VL; pointeur daté pour un refresh ULTÉRIEUR, hors scope 005). Donnée manquante fournie avant décision (question owner): RapidOCR sur les DEUX scans propres (6,2 s / 5,8 s) — la perte est INTRINSÈQUE (accents, fusions, guillemets 《》, une proposition sautée même sur scan net) → exclusion du siège validée par la donnée; unlim exclu aussi (PR draft + fragilité basse résolution). CP-MVP-005 « Seats hardening »: S02 tier CUDA whisper (chaîne CUDA→CPU→mock, identité de tier dans les traces), S03 siège OCR Qwen3-VL 4B Q4_K_M Apache en sidecar borné avec LE harnais prouvé (pré-resize nativeImage ~2 500 tokens multiples de 28 — jamais --image-max-tokens), S04 passe de correction owner sur les dossiers réels, S05 échelon cloud Mistral STRICTEMENT opt-in par capture (clé main-process via .env.local, provenance cloud-derived 28, no-op explicite sans clé), S06 acceptance. Registre/ACTIVE.md/ledgers à jour; couverture 00–35 auditée à l'ouverture.
- CP-MVP-005 S01+S02 exécutés (2026-07-08). S01: base_commit 94559f7, 231/23 vérifiés, arbre de travail = dogfooding owner + .env.local ignoré. S02 — LE TIER CUDA PREND LE SIÈGE WHISPER: binaire CUDA v1.8.6 installé AUTO-CONTENU dans .atomik/speech/cuda/ (l'adaptateur exporte LD_LIBRARY_PATH=dirname(binaire) vers le sidecar — et l'audit a révélé que l'install CPU de S05 résolvait silencieusement ses .so depuis l'ARBRE DE BUILD DU BENCH via runpath: les deux installs portent leurs libs maintenant, nettoyer le bench ne tue plus le siège). Chaîne de repli AUTOMATIQUE CUDA→CPU→mock dans whisper-adapter.ts, démotion collante par session (un échec CUDA ne coûte qu'une fois), identité de tier dans les traces (runtimeVersion '+cuda'). Mesures machine owner: mémo 1,83 s CUDA avec transcript OCTET-ÉGAL au scellé S04 (-l auto), fixture 181 s en 5,7 s vs 14,7 s CPU (2,6×). Piège S04 recroisé en vérif manuelle (sans -l auto whisper TRADUIT le français) — l'adaptateur le passe toujours, consigné au module note. Tests 231→234, typecheck vert. Next: S03 — le siège OCR.
- CP-MVP-005 S03 exécuté (2026-07-08, 13 §local-inference + §IPC relus) — QWEN3-VL 4B PREND LE SIÈGE OCR. `ocr-adapter.ts` (sidecar borné llama-mtmd-cli: 600 s + SIGKILL, tmp workdir, image in / texte out, zéro accès vault) derrière le MÊME contrat TranscriptionAdapter; `routeByMedia(audio, ocr)` dans transcription.ts route par famille de média (l'identité vient toujours de l'adaptateur qui a répondu); `sidecar.ts` extrait l'exécution bornée partagée (LD_LIBRARY_PATH=dirname(binaire) → installs auto-contenus, whisper et OCR). LE HARNAIS PROUVÉ EST STRUCTUREL: main pré-redimensionne via nativeImage (≈2 500 tokens = pixels/784, dimensions multiples de 28, jamais d'upscale; resizer INJECTÉ — vitest n'a pas d'Electron; 15: aucune nouvelle dépendance image) — `--image-max-tokens` jamais utilisé (cassé pour ce modèle, refs datées). Tier CUDA d'abord avec démotion collante par session, floor CPU, mock en dernier; binaires = les builds BENCHÉS PR#24975 NO_VMM (requis sur ce WSL2), installés sous .atomik/ocr/{cpu,cuda,models} avec sha256 consignés. VÉRIFS du siège installé: 3,85 s CUDA sur la page Leibniz pré-dimensionnée; 96,6 s le tier CPU (variante 1k, transcript complet). Gap honnête: HEIC/HEIF probablement illisible par nativeImage sous Linux → échec propre tracé, jamais de fabrication. Tests 234→237 (budget de resize, identité, chaîne collante, routage), typecheck vert. Next: S04 — validation owner in-app (redémarrer, Transcribe sur un dossier image), puis S05 l'échelon cloud opt-in.
- CP-MVP-005 S05 exécuté (2026-07-08, 13 §IPC + clés relus) — L'ÉCHELON CLOUD OPT-IN. `mistral-ocr-adapter.ts` derrière le MÊME contrat, invoqué UNIQUEMENT par le nouveau canal typé `transcribe-source-cloud` (13 §IPC) derrière un bouton explicite « Cloud OCR » à côté de « Transcribe » — le title du bouton dit qu'il ENVOIE l'image; jamais de repli silencieux (13: « require explicit policy before any local failure falls back to cloud » — appliqué à la lettre). Modèle ÉPINGLÉ `mistral-ocr-4-0` (benché lettre-parfait sur les scans du banc; un upgrade = nouvelle décision datée, pas une dérive d'alias). Clé résolue côté main uniquement (env MISTRAL_API_KEY puis .env.local git-ignoré); sans clé = refus explicatif, rien ne quitte la machine. Le résultat passe par le pipeline ordinaire → `location: cloud-model` + identité fournisseur dans le frontmatter du transcript, le dossier et la trace; règle no-clobber conservée (supprimer le transcript local pour escalader — la même règle explicite de re-run que partout). fetch borné AbortController 180 s. Tests 237→238 (résolution de clé env>fichier>absent, forme de requête épinglée, identité cloud-derived, erreur HTTP explicite); typecheck/build/smoke verts. Les TROIS directives owner du jour sont exécutées; reste S04 (validation owner in-app des deux boutons) et S06 (acceptance + clôture).
- CP-MVP-005 S05b exécuté (2026-07-08, trois raffinements owner sur revue). (1) LE FILTRE SCAN DU BANC SIÉGÉ: scan-filter.ts — l'aplatissement d'illumination + étirement percentile de scan-clean.py réimplémenté sans dépendance sur les bitmaps bruts (image intégrale, agnostique à l'ordre des canaux BGRA), appliqué dans le pipeline OCR APRÈS le pré-resize — chaque photo entre désormais dans le régime « scan propre » où vivait la qualité du banc; testé sur page synthétique à éclairage inégal (papier blanchi des deux côtés, gradient supprimé, encre préservée). (2) SETTINGS → AI (fini le .env.local, directive owner): engrenage en barre supérieure → panneau AI settings → champ de saisie de la clé Mistral; stockage main-only dans le state dir (ai-settings.json mode 0600, jamais le vault, jamais git); le renderer ne reçoit que présence + hint masqué ••••1234 — la clé brute ne REDESCEND jamais (13); canaux typés get-ai-settings / set-mistral-api-key; la lecture de .env.local est SUPPRIMÉE de la résolution (env var conservée en override dev); message d'erreur du bouton Cloud OCR pointe Settings → AI. (3) DÉMOTION CUDA VISIBLE (trouvaille S04: le premier essai owner — >30 s puis mort sur reload dev — n'a laissé AUCUNE trace, indiagnosticable): les deux adaptateurs warnent désormais la cause de la démotion; le tier qui a répondu se lit dans runtime_version du transcript (+cuda) et dans chaque trace. Diagnostic préventif fait: le binaire CUDA installé tourne même sous env -i (le soupçon « pas de CUDA en app » ne tient pas à l'environnement); question 4-0 vs 3.0 clarifiée (la référence benchée EST 4-0, prouvée octet-identique à latest; la 3.0 essayée par l'owner n'a jamais été validée au banc). Tests 238→242 / 25 suites; typecheck/build/smoke verts. Retest owner: redémarrer, Transcribe sur Pascal 2, lire runtime_version.
- CP-MVP-005 S05c exécuté (2026-07-08, demande owner): LE SCAN NETTOYÉ ATTERRIT DANS LE DOSSIER — scan.jpg à côté du transcript, par la couture segments.json (l'adaptateur rapporte scanJpeg dans TranscriptionOutput, le PIPELINE reste le seul écrivain du vault, nettoyage si la mise à jour du dossier échoue). Lié depuis le corps du transcript (« Cleaned scan: … l'original reste la preuve ») et depuis les représentations extraites du dossier; écriture simple et non wx (dérivé machine régénérable — le wx du transcript reste LA porte des re-runs); l'ORIGINAL reste la preuve (leçon de l'incident). Tests 242→243; typecheck/smoke verts.
- CP-MVP-005 S05d — l'incident S04 diagnostiqué par la trace et corrigé (2026-07-08). La trace du premier essai owner dit tout: wallMs 652 428 (≈11 min), failed, bytes = l'original Pascal 2 exactement — la tentative CUDA a brûlé les 600 s de SON PROPRE timeout (la signature du stall du banc), démotion CPU, puis mort sur reload dev à ~52 s. L'erreur Chromium « GpuControl.CreateCommandBuffer kTransientFailure » rapportée par l'owner = le canal GPU du compositeur Chromium sous WSLg, vraisemblablement la contention pendant que le sidecar tenait la 5070 — pas notre siège. Corrections: (1) LE SIÈGE REDRESSE l'image selon la rotation du dossier avant reconnaissance (la première note d'amélioration de S07, enfin structurelle — rotateRgba pur en quarts de tour horaires, testé; scan.jpg atterrit droit aussi); Pascal 2 n'a AUCUNE rotation enregistrée — l'owner la redresse en deux clics dans l'app; la page couchée est l'hypothèse datée du déclencheur de stall. (2) La TENTATIVE CUDA plafonne à 90 s (une saine = 4–15 s): un stall coûte désormais des secondes avant que le floor CPU réponde, plus jamais 10 minutes. Tests 243→244; typecheck/smoke verts. Retest owner: redresser Pascal 2 (boutons rotation), supprimer le transcript s'il existe, Transcribe.
- S05d addendum — LE DÉCLENCHEUR PROUVÉ par reproduction (question owner « pourquoi CUDA a échoué, ça recommencera ? »): l'image de l'app (Pascal 2 pré-dimensionnée mais COUCHÉE, 1624×1204) passée au binaire CUDA installé → le modèle BOUCLE (« eu tantôt eu tantôt… » ×des centaines, 24 Ko de sortie pour une page de 3 Ko) à pleine vitesse GPU jusqu'au débordement de contexte (« failed to find a memory slot ») à 58 s — CUDA N'A JAMAIS DÉFAILLI, le modèle ne sait pas lire le français couché et dégénère. La chronologie des 652 s de l'incident se reconstruit exactement: load froid → boucle CUDA → débordement ctx (~1 min) → démotion invisible → la jambe CPU BOUCLE AUSSI → son timeout de 600 s → 652 s total, failed. Correctif résiduel: `-n 3000` sur les deux tiers (une vraie page ≈ 1–2k tokens) — une boucle coûte désormais ~30 s GPU / ~2–3 min CPU au pire, avec sortie visiblement dégénérée → l'utilisateur redresse et relance. Récidive: (a) dossier redressé = régime validé, boucle inatteignable par ce déclencheur; (b) page couchée oubliée = borné par -n + cap CUDA 90 s + warning console; (c) les bugs upstream llama.cpp gros-fichiers restent contournés structurellement (pré-resize, jamais les flags). Piste future notée: orientation EXIF/auto-détection en amélioration candidate — la rotation du dossier reste le mécanisme de vérité owner. Tests 244, typecheck vert.
- CP-MVP-005 S05e — LA VRAIE CAUSE RACINE, repérée par l'owner (« Pascal 2 est déjà en portrait ») : l'ORIENTATION EXIF. Le chemin d'affichage Chromium l'applique silencieusement (la page PARAÎT droite dans l'app), nativeImage d'Electron NON (le modèle recevait du paysage) — deux vérités pour le même fichier, et la « piste future » d'il y a une heure était en fait LE bug. Vérifié sur fichiers: Pascal 2 = EXIF 6 (90° CW, aucune rotation manuelle — l'owner avait raison), Pascal = EXIF 3 (180°) + rotation manuelle 270 = 90° CW total — la formule redressement effectif = EXIF + rotation dossier (mod 360) VALIDE sur les deux dossiers (la rotation manuelle de l'owner s'était faite PAR-DESSUS l'EXIF affiché, comme prévu). Fix: lecteur d'orientation EXIF sans dépendance (exif.ts — parse APP1/TIFF tolérant, tout inattendu → 0°, testé II et MM synthétiques), combiné dans l'adaptateur OCR; plus AUCUNE rotation utilisateur nécessaire pour les photos téléphone; scan.jpg atterrit droit. Tests 244→246; typecheck vert. Retest owner: redémarrer, supprimer le transcript partiel de Pascal 2 s'il existe, Transcribe — attendu ~5–15 s, runtime_version '+cuda', scan.jpg droit dans le dossier.
- CP-MVP-005 S05f — deux retours owner (2026-07-08). (1) « Cloud OCR sur un dossier déjà transcrit ? » — répondu depuis le code: le pipeline REFUSE avant toute lecture et tout appel réseau (rien ne part chez Mistral), et les deux boutons disparaissent dès que le dossier lie un transcript; l'escalade explicite = supprimer transcript.md puis Cloud OCR (les corrections vivent dans le transcript — la règle S07); une affordance « re-run » plus aimable est notée candidate. (2) BUG réel: les arbres ne se rafraîchissaient pas après transcription (transcript.md + scan.jpg invisibles sans re-switch) — nouveau push léger `vaultFilesChanged` (13 §IPC, sans payload) émis par les DEUX handlers de transcription après l'atterrissage des fichiers; SourcesTree ET l'arbre du vault s'abonnent — rafraîchissement de l'arbre SEUL, jamais le drop d'état d'un changement de vault (vaultChanged reste le signal lourd). Tests 246 (parité preload auto-couverte); typecheck/smoke verts.
- CP-MVP-005 S05g — la question owner « on n'a pas de visionneuse d'images ? » (2026-07-08). Constat: l'arbre est .md-SEULEMENT par conception (le plan de connaissance liste les notes; les médias s'atteignent PAR les dossiers — original.jpg/scan.jpg n'y apparaîtront jamais, ce n'est pas un bug de refresh) et la visionneuse EXISTE (l'onglet image du dossier + la pastille View-original de l'éditeur) — mais scan.jpg était lié depuis le transcript SANS aucun moyen de le voir. Fix: bascule « View scan / View original » dans l'onglet image quand le dossier lie un scan — le scan s'affiche SANS re-rotation (déjà droit), l'original reste la preuve; sert par readSourceAsset (même garde-fou containment+allowlist). Note posture: la vue transcript = l'éditeur du vault, son arbre est abonné au push vaultFilesChanged de S05f (redémarrage requis pour le voir). Tests 246; typecheck/smoke verts.
- CP-MVP-005 S04 — VALIDATION OWNER (2026-07-08, capture d'écran + trace): la transcription in-app de Pascal 2 (le fichier même qui avait brûlé 652 s le matin) COMPLÈTE en **12,6 s sur le tier CUDA** (trace trace_8cabc37d: Qwen3-VL-4B Q4_K_M, runtimeVersion PR#24975@649864fc6+cuda, completed) — toute la chaîne EXIF-redressement → filtre scan → pré-resize → CUDA validée en production; transcript de classe banc visible à l'écran, scan.jpg lié, arbre rafraîchi après redémarrage. AU PASSAGE, bug remonté par l'owner sur la même capture: les boutons Transcribe/Cloud OCR apparaissaient dans la VUE TRANSCRIPT (le prédicat de visibilité ne vérifiait pas que la note est le dossier) — corrigé: les deux boutons n'apparaissent que sur source.md sans transcript; « View scan » reste partout où un scan est lié. Clarification de vocabulaire consignée: « dossier » = la note source (source.md) de chaque capture — le terme UI visible est « source ». Tests 246; typecheck/smoke verts. Reste: validation owner du bouton Cloud OCR + flux de correction, puis S06 acceptance + clôture.
- CP-MVP-005 S05h — deux demandes owner (2026-07-08). (1) index.md (la carte humaine du bundle, créée à l'import) gagne les lignes transcript.md et scan.jpg quand la transcription les fait atterrir — idempotent, best-effort (la carte ne fait jamais échouer un run complété) — et les perd au reset. (2) L'AFFORDANCE DE RE-RUN qui manquait: bouton « Delete transcript… » sur la note source quand un transcript existe → confirmation renderer (les corrections vivent dans le transcript et seront perdues; l'original reste intouché) → resetTranscription supprime transcript/segments/scan, restaure le dossier à sa forme pré-transcription (bloc transcription retiré du frontmatter, status captured, ligne « None yet » de retour), élague l'index, émet vaultFilesChanged — Transcribe et Cloud OCR réapparaissent naturellement (le prédicat de visibilité suit le contenu du dossier). Aller-retour TESTÉ: transcribe → reset → transcribe ré-enregistre proprement (un seul bloc transcription, jamais deux); double-reset refuse explicitement. Tests 246→247; typecheck/smoke verts. L'escalade cloud complète devient: Delete transcript… → Cloud OCR — deux actes explicites, conformes 13/28.
- CP-MVP-005 S05i — retour owner « l'orientation de la page source ne se propage pas à la page transcript; l'état devrait être consommé » (2026-07-08): exact, et l'owner nomme la bonne règle — L'ORIENTATION EST L'ÉTAT DU DOSSIER, les consommateurs en héritent. La vue image lisait `rotation:` dans le frontmatter de la NOTE OUVERTE: sur le dossier ça marche, sur transcript.md (aucune rotation propre) l'image s'affichait sans la correction manuelle (l'EXIF seul survivait via le court-circuit rotation=0). Fix: pour toute note non-dossier, la rotation vient de l'ASSET (readSourceAsset renvoyait déjà `rotation` lue du source.md voisin — champ ignoré par le renderer jusqu'ici); le dossier garde sa lecture locale (rotation optimiste pendant l'édition). Piège désarmé au passage: les boutons ⟲⟳ n'apparaissent plus QUE sur le dossier — ailleurs ils auraient écrit `rotation:` dans le frontmatter de la MAUVAISE note (transcript pollué; vérifié: aucune pollution existante). Clé Mistral migrée de .env.local vers ai-settings.json (0600) au passage sur incident owner « no key configured » — le handler lit le store à chaque appel, aucun redémarrage requis. Tests 247; typecheck/smoke verts.
- CP-MVP-005 CLOS (2026-07-08, S06) + CP-MVP-003 (M4 PDF) OUVERT sur directive owner. Acceptance: sessions/2026-07-08-cp-mvp-005-acceptance.md — tous les items du DoD tenus: tier CUDA whisper (1,83 s mémo, chaîne testée), siège OCR Qwen3-VL 4B (VALIDATION PRODUCTION: Pascal 2 en 12,6 s +cuda, trace_8cabc37d — le même fichier échouait à 652 s le matin), échelon cloud Mistral épinglé 4-0 VALIDÉ OWNER, correction héritée-validée; sept trouvailles de dogfooding corrigées le jour même; gaps honnêtes consignés (HEIC, timeout whisper-CUDA non plafonné, chiffres GPU = WSL2-only, bug upstream à signaler, pointeur Qwen3.5 sans intention — coquille owner confirmée). ONZE commits sur la journée du banc au siège. CP-MVP-003 ouvert per directive owner (« ok for PDF page, as source and as a viewer and text extraction »): S02 décision moteur PDF (15, datée), S03 PDF-source (bundle standard par les mêmes gates), S04 viewer (nouveau view kind, 03/13), S05 extraction (text layer local + pages scannées via LE SIÈGE OCR prouvé — le dividende direct de 005), S06 ancres page/texte + retour de citation, S07 acceptance. Couverture 00–35 auditée; registre/ACTIVE à jour.
- CP-MVP-003 S01+S02+S03 exécutés (2026-07-08). S01: base 35ec031, 247/25 vérifiés, 10/03/05 relus (forme de bundle sources/pdf/<slug>/ avec extracted.md + ancres; « l'extraction n'est jamais le seul chemin »; les tabs sont des vues sur des ressources; fidélité de rendu ≠ fidélité d'extraction). S02 — DÉCISION MOTEUR DATÉE (15): pdfjs-dist 6.1.200 Apache-2.0, isEvalSupported:false TOUJOURS (CVE-2024-4367), worker local; LE SPLIT QUI IMPLÉMENTE 10 LITTÉRALEMENT: viewer = pdf.js dans le renderer (affichage seul), extraction = build legacy dans MAIN (vérité dérivée tracée, pipeline façon transcription); rejetés datés: mupdf AGPL, pdfium natif-lourd, react-pdf couche inutile, poppler noté alternative future (record: sessions/2026-07-08-pdf-engine-decision.md); installé, 0 vulnérabilité. S03 — PDF-SOURCE: pdf-import.ts par la discipline capture-import (magic %PDF- AVANT de toucher le vault — les octets priment sur l'étiquette; cap 200 Mo; slug normalisé dédupliqué; wx + nettoyage; sha256 + taille dans le dossier per 10 §evidence; « extraction arrives with S05 » dans les représentations), canal import-pdf-source (dialog main) + bouton ＋PDF dans l'arbre sources (ouvre le dossier créé), vaultFilesChanged émis. Tests 247→251/26; typecheck/smoke verts. Next: S04 viewer (nouveau view kind).
- CP-MVP-003 S04+S05 exécutés (2026-07-08). S04 — LE VIEWER: .pdf entre dans l'allowlist d'assets (mêmes gates de containment, cap 50 Mo — c'était le « vault: rejected asset type » de l'owner); PdfView.tsx: pdf.js 6.1.200 dans le renderer sandboxé, worker du bundle LOCAL, canvas ajusté au panneau, nav ‹ page N/M ›; BONUS structurel: pdf.js 6 a SUPPRIMÉ le chemin de police eval'd — CVE-2024-4367 fermée par construction, pas par option (l'API v6 a d'ailleurs retiré isEvalSupported et déplacé destroy() sur la loading task — dérive d'API absorbée); outils rotation + boutons transcribe hors-scope pour les PDF; restore page-dans-tab-param DIFFÉRÉ à S06 (porte le même mécanisme open-at-page que le retour de citation). S05 — L'EXTRACTION: pdf-extract.ts en pipeline façon transcription (lecteur + rasterizer + siège OCR INJECTÉS; no-clobber wx; dossier status→extracted + extracted_text + trace id; ligne index; UNE trace 'extract' — deterministic en pur text-layer, local-model quand des pages OCR ont tourné — le ledger apprend l'action 'extract'); pdf-text.ts: pdf.js legacy dans MAIN (les revendications d'extraction ne viennent jamais du chemin d'affichage) + rasterizer pdftoppm sur le précédent ffmpeg (outil système absent → placeholder honnête par page; poppler NON installé sur cette machine, l'installer active l'OCR-PDF sans changement de code); bouton Extract text sur les dossiers pdf. VÉRIFICATION RÉELLE: le devis JF_Quote de l'owner — 2 pages, ~5,1k chars/page, 125 ms, français intact (« Devis normalisé en optique médicale », L.165-9). Tests 251→253/28(?); typecheck/smoke verts. Note honnête: le flip de correction (S07-hook) est clé sur transcript.md — extension à extracted.md = item compagnon S06.
- CP-MVP-003 S05b — FEEDBACK OWNER STRUCTUREL (2026-07-08, deuxième occurrence du même trou en un jour): « je ne peux pas supprimer une extraction pour recommencer — il faut penser à l'usage quotidien en implémentant, même en MVP ». Leçon PROMUE EN MÉMOIRE PERSISTANTE (ship-lifecycle-with-artifact): toute feature qui atterrit un fichier dérivé livre créer ET supprimer/relancer DANS LA MÊME UNITÉ, avec test aller-retour — l'owner dogfoode immédiatement et la première chose qu'il fait après générer, c'est régénérer. Fix immédiat: resetExtraction (miroir de resetTranscription) — bouton « Delete extraction… » confirmé sur les dossiers pdf, extracted.md retiré, dossier restauré (status imported, lignes extraction retirées, ligne « None yet » de retour), index élagué, vaultFilesChanged émis; aller-retour testé extract→delete→extract (un seul extracted_text, jamais deux; double-delete refuse). Poppler installé par l'owner entre-temps → l'OCR-PDF pour pages scannées est ACTIF sur cette machine désormais. Tests 253→254; typecheck/smoke verts.
- CP-MVP-003 S06 exécuté (2026-07-08) — ANCRES + RETOUR DE CITATION, la tranche-vérité de M4 close. (1) « ⚓ anchor page N » écrit une ligne durable dans la table Useful-anchors du dossier (withPageAnchor, pure, idempotente — la citation EST une édition de fichier, file-first). (2) RETOUR DE CITATION intra-dossier ET inter-note: le gestionnaire de clic de lien partagé (useVaultNote) route tout `…/original.pdf#page=N` vers le dossier frère via un registre de page-en-attente (pdf-open.ts, la page ne peut pas voyager dans openNote générique), le PdfView saute à l'ouverture; requestedPage câblé dans PdfView (« return from citation to original page », 10). (3) COMPAGNON: le hook de flip de correction sert désormais DEUX fichiers dérivés (transcript.md ET extracted.md, un seul hook) — éditer extracted.md bascule le dossier en human-corrected (withExtractionCorrectionRecorded). Tests 254→257/30; typecheck/smoke verts. Reste S07: acceptance + validation owner sur le vrai devis + clôture M4.
- CP-MVP-003 S06-fixes + S06b (2026-07-09, captures d'écran + demande owner). TROIS bugs de retour de citation: (1) la cible de la table d'ancres était du TEXTE nu — aucun <a> à cliquer → withPageAnchor émet un vrai lien markdown [page N](./original.pdf#page=N), et les lignes DÉJÀ écrites dans le vault owner ont été MIGRÉES (le contenu de fichier ne se répare pas par du code); (2) un clic d'ancre DANS son propre dossier ne sautait pas (ré-ouvrir la même note ne re-déclenche pas l'effet) → interception same-dossier qui pilote le viewer directement, requestedPage devient un objet frais par clic (re-clic re-navigue); (3) une citation inter-note ouvrait le MARKDOWN du dossier, pas le viewer → useVaultNote route les liens #page=N vers onOpenSourceImage quand l'hôte le fournit (VaultView câblé). Diagnostic « nothing changed » après relance: le build était FRAIS — c'était la ligne d'ancre déjà écrite au vieux format dans source.md. S06b — l'AUTOCOMPLETE DE LIAISON DE SOURCE demandé: le menu @ existant insère désormais des CITATIONS PDF complètes (lien relatif correct + #page=1 avec le CHIFFRE PRÉ-SÉLECTIONNÉ à taper par-dessus; le label ne porte pas de numéro — pas de dérive) — plus jamais de chemin de bundle tapé à la main, la classe de liens morts (slug faux du test.md owner) meurt ici. Tests 257→258; typecheck/smoke verts.
- CP-MVP-003 S06d/e/f — trois retours owner de la même passe de dogfooding (2026-07-09). S06d: le PDF ne rendait pas au premier affichage (course de largeur-0 au mount d'un onglet frais — la première peinture visait un panneau non mesuré, un clic quelconque « réparait ») → ResizeObserver: le rendu attend la première mesure réelle et suit les redimensionnements de panneau. S06e: « cliquer un lien de note ouvre le markdown du dossier dans l'onglet vault, pas la vue source » — la règle que son attente nomme: LES SOURCES S'OUVRENT DANS LA VUE SOURCE; le gestionnaire de clic route désormais TROIS familles de cibles vers onOpenSourceView: citations #page=N (comme avant), liens original.pdf NUS (clic MORT avant — la ligne « Original PDF » du dossier marche enfin), et liens dossier source.md (avant: markdown dans l'éditeur). S06f: (1) l'échelon cloud ROUTE PAR MÉDIA comme le local — routeByMedia(voxtral, mistral-ocr): le bouton unique devient honnête deux fois (« Cloud transcribe » sur l'audio, « Cloud OCR » sur l'image; le bouton OCR-sur-audio que l'owner a repéré était bien un non-sens) — VOXTRAL SIÉGÉ épinglé voxtral-mini-2602 (vérifié live /v1/models sur clé owner; adapter multipart /v1/audio/transcriptions, audioSeconds rapporté par l'usage API quand présent); (2) le menu @ propose « extracted »/« transcript » → INSÈRE LE TEXTE DÉRIVÉ EN BLOC de citation (frontmatter dépouillé, corps cité ligne à ligne, lien source en tête; lu à l'APPLY, jamais au menu). Question de nommage owner consignée: transcript ≠ extracted est DÉLIBÉRÉ (28/10: transcription = interprétation modèle d'un média; extraction = parse déterministe d'un texte embarqué — le futur Truth Lens les traite différemment), l'UX doit gommer la différence (le menu @ dit juste le mot), pas les fichiers. AU PASSAGE: le store de clé était VIDE (Clear owner probable) — re-saisir la clé via ⚙ avant tout test cloud. Tests 258→260; typecheck/smoke verts.
- INCIDENT ÉVITÉ + FIX STRUCTUREL (2026-07-09): un `git add sources/pdf` de l'agent a balayé les DOCUMENTS PERSONNELS de l'owner (devis optique signé, certificat de travail signé) dans un commit — le dépôt est PUBLIC. Containment: RIEN n'était poussé (30 commits locaux d'avance, vérifié), commit défait (reset --soft), sources/ désindexé, zéro fichier personnel en staging. Cause racine double: (1) violation de la règle mémoire « stage explicitement, jamais de balayage » — le `git add sources/pdf` visait des fichiers d'ancres migrés et a tout pris; (2) TROU DE COUVERTURE: .gitignore ne couvrait que sources/captures/ (décision S02) — sources/pdf/ n'était pas protégé alors que c'est la même classe de médias personnels. Fix structurel: sources/pdf/ ajouté au .gitignore avec la même justification S02; les bundles PDF restent locaux, les records committés portent hashes et métadonnées. Leçon: toute NOUVELLE zone de médias utilisateur doit entrer au .gitignore À LA CRÉATION du type de bundle (S03 aurait dû le faire) — pas à l'incident.
- CP-MVP-003 RÉCONCILIATION (2026-07-13, 22 §4 à la ré-entrée): trois commits du 2026-07-09 avaient atterri SANS enregistrement — le ledger s'arrêtait à S06b, le log à S06d/e/f, et le frontmatter `current_step` était figé à S02 depuis l'ouverture du chemin; la règle « même unité de travail » a été manquée sur ces commits. Corrigé par backfill daté, pas par réécriture. S06c (6f322ff, feedback owner): le menu @ propose le CHOIX COMPLET par source — citation page libre (chiffre pré-sélectionné), CHAQUE ancre ⚓ enregistrée devient citation exacte (pageAnchorsOf parse la table Useful-anchors du dossier — une ancre posée devient citable depuis n'importe quelle note), lien dossier nu; bundles image: embed + lien; dossiers lus au moment du menu via lecteur INJECTÉ (cache 5 s; fakes en test). Tests 258→259. S06g (6015881): UN SEUL rendu à la fois — pdf.js refuse les render() superposés sur un même canvas et le flag `cancelled` ne sautait que le travail post-await (course vue par l'owner: flip de page pendant un resize) → la RenderTask précédente est ANNULÉE avant chaque rendu et au cleanup de l'effet; RenderingCancelledException = le mécanisme qui marche, pas une erreur; et les outils rotation ⟲⟳ disparaissent des vues PDF (correction PHOTO — auraient écrit dans le frontmatter d'un dossier pdf). S06h (3937fd2): UN SEUL scrollbar — `.source-image-original:has(.pdf-view)` cède le défilement (et le centrage image) à la vue PDF; la nav de page devient sticky (page N/M + bouton ancre restent atteignables en défilant). Tests 260 stables sur g/h (renderer/CSS purs). VÉRIFIÉ à la réconciliation: 260 tests / 28 suites verts, typecheck vert, base_commit 35ec031 inchangé, arbre sale = fichiers de dogfooding owner uniquement; current_step corrigé S02→S07; rappel S07 consigné au ledger: re-saisir la clé provider (store trouvé vide le 2026-07-09) avant les vérifications cloud. Reste S07: acceptance 18 §M4 + tranche-vérité, validation owner sur le vrai devis, clôture M4.
- CP-MVP-003 S07 — ACCEPTANCE RUN, balayage machine (2026-07-13; record: sessions/2026-07-13-cp-mvp-003-acceptance.md). Les sept intentions §M4 et les quatre lignes de tranche-vérité passées une à une contre le code et les tests — tout ce qui est vérifiable machine TIENT (la séparation fidélité-rendu/fidélité-extraction est STRUCTURELLE: build frais dans le renderer sandboxé, build legacy dans MAIN qui relit les octets de original.pdf; l'ancre de page est une ligne de table de dossier — une édition de fichier, pas un état de vue; UNE trace 'extract' par run, échec compris). L'acceptance a TROUVÉ ET CORRIGÉ deux manques du chemin dans la même unité: (1) la RESTAURATION page-dans-tab-param — S04 l'avait différée à S06, S06 n'a livré que le saut en session (registre pending) — les tours de page persistent désormais comme param `page` de l'onglet (03 état UI récupérable, comme mode/treeW/treeOpen: pdfPageOf pur + initialPage/onPageChange câblés Workspace→SourceImageView→PdfView; le retour de citation PRIME sur la page restaurée; rouvrir l'app rend la page); (2) la note de module n'avait AUCUNE couverture CP-MVP-003 — aucun fichier docs/ touché sur tout le chemin (CP-MVP-005 la tenait à chaque pas) — absorbée: quatre puces (import/viewer/extraction/ancres), le flux d'extraction, les quatre suites de test, pdfjs-dist 6.1.200 en fait daté + poppler-utils en note d'environnement, l'exemple « 28 tests, 6 suites » corrigé. Écarts honnêtes consignés: ancres de PAGE seulement (span/région pas « cheap » — renderer canvas sans couche texte); les entêtes ## Page N d'extracted.md ne sont pas des liens #page=N (le retour exact depuis un passage dérivé passe par la table d'ancres ou une citation @); la déviation .gitignore sources/pdf/ vs 10 §Git tient (incident 2026-07-09); pas de re-run par page (delete+extract = document entier, acceptable à 125 ms). Portes: 262 tests / 28 suites (260→262), typecheck, build, smoke ATOMIK_SMOKE_OK. RESTE LA PORTE OWNER: checklist de validation en 8 points dans le record (import, rendu, RESTAURATION DE PAGE — nouveau, extraction FR, ancre+citation @, retour de citation, flip de correction, aller-retour delete/extract); à son OK: status→done, pointeur ACTIVE.md, bedrock 10 planned→mvp dans le commit de clôture.
- CP-MVP-003 FERMÉ — M4 CLOS (2026-07-13): VALIDATION OWNER « all ok » sur la checklist d'acceptance en 8 points (vrai PDF, restauration de page comprise — la trouvaille du balayage du matin). Clôture dans une unité: CP-MVP-003 status→done (S07 coché, validation consignée dans le record d'acceptance); registre M4→done (2026-07-13); ACTIVE.md → AUCUN chemin actif — prochain jalon M5 (onglet source web + imports explicites), à OUVRIR comme NOUVEAU chemin per la règle d'ouverture (jamais élargir un chemin qui ferme; les chemins tardifs héritent des notes de module et de l'architecture telle que construite); bedrock 10 planned→mvp (17: la doc suit la réalité). Quatre jalons M0–M4 clos en huit jours de dogfooding continu. Rappel persistant: clé provider à re-saisir via ⚙ avant le prochain travail cloud.
- CP-MVP-006 PROPOSÉ (2026-07-13, sur directive owner à la clôture de M4) — onglet source WEB (M5), brouillon per la règle d'ouverture: semé de 18 §M5 + bedrock 09, audit de couverture complet (les 36 pages 00–35 comptées: 22 requises dont 29 pour la FRONTIÈRE import-durable/grounding-transitoire, 6 conditionnelles, 8 exclues avec raison — les trails vont avec 20-relations, PAS ce chemin). Sept pas: bootstrap → décisions datées 15 sous contrainte 13 (approche d'embed WebContentsView/webview/BrowserView + moteur reader + format snapshot) → vue web isolée (URL en tab param DÈS S03 — leçon S07) → Import as source EXPLICITE (bundle sources/web/<slug>/ avec métadonnées de preuve 09: accessed_at, hashes, note de licence; .gitignore DANS LE MÊME COMMIT que le type de bundle — leçon incident) → extraction reader.md (dérivé + UNE trace + delete/re-run MÊME UNITÉ — pratique permanente; le hook de flip gagne son troisième fichier dérivé) → citations @ + sélection→IA→note → acceptance. DoD verrouille aussi: notes de module + ledger + log À CHAQUE PAS (le manque trouvé par l'acceptance de CP-MVP-003). Statut draft; l'exécution n'ouvre qu'à l'acceptation owner (22: « reviewed and accepted like any other patch »).
- CP-MVP-006 ACCEPTÉ par l'owner (2026-07-13) AVEC DIRECTIVE D'ÉTABLI: « navigate through internet like a normal web nav », « enable a reader view or extract text and image as a source », et le scénario d'apprentissage — « a google collab page open on a panel and a pdf explaining the math on another, everything for learning ». Le brouillon a été RÉVISÉ avant activation pour porter la directive (pas d'élargissement silencieux — la portée est nommée dans le chemin): (1) navigation NORMALE exigée — liens, historique, états de chargement honnêtes, pages-application utilisables, GOOGLE COLAB nommé PAGE-BANC de la décision d'embed; (2) persistance de session/partition (état de connexion = décision de VIE PRIVÉE) et politique popup/window.open = décisions DATÉES S02 sous 13, pas des accidents; (3) l'extraction capture texte ET IMAGES — media/ hashé dans le bundle, références relatives depuis reader.md (une image nécessaire au texte est du matériau source, pas un hotlink qui pourrit), delete/re-run retire media/ avec; (4) l'onglet web est un citoyen ordinaire des panneaux — il se splitte à côté de l'onglet PDF de M4; le scénario Colab+PDF-de-maths devient une intention d'acceptance S07. ACTIVE.md pointe CP-MVP-006; registre M5→active. Prochain pas: S01 bootstrap.
- CP-MVP-006 S01 exécuté (2026-07-13) — BOOTSTRAP: base_commit 5381a12; 262 tests / 28 suites re-vérifiés au pas; arbre sale = fichiers de dogfooding owner uniquement (même ensemble que le matin). Relectures épinglées pour ce chemin: 03 — `source-web` est DÉJÀ un type d'onglet MVP nommé et les opérations de panneaux promettent déjà les vues source côte à côte → le scénario d'établi de l'owner (Colab + PDF de maths) est le système de panneaux fonctionnant comme conçu, AUCUNE machinerie de panneaux nouvelle à construire; 05 — `WebResource` et le ViewKind `'source-web'` existent dans l'esquisse de types, Selection 'text' + SourceAnchor.textRange couvrent les ancres du reader (le sidecar de coordonnées reste optionnel, comme pour le PDF); 13 (en entier) — les quatre réglages requis s'appliquent à l'EMBED, la vue live ne doit JAMAIS pouvoir muter les fichiers du vault (l'import passe par un canal main typé), chaque nouvelle WebContentsView exige frontière de confiance documentée + tests (règle de documentation sécurité), aucune clé près des vues distantes, le contenu distant ne déclenche jamais d'appel provider; 09 — deux chemins séparés (vue live / vue extraite), liste de métadonnées de preuve, pas de crawl automatique depuis les liens de grounding. Prochain pas S02: décisions datées 15 sous 13 — approche d'embed (WebContentsView/webview/BrowserView jugés contre le système de panneaux + le banc Colab), PERSISTANCE de partition + politique popup, moteur reader (capture d'images comprise), format snapshot; consigner les non-choisis; aucune installation sans le record.
- CP-MVP-006 S02 exécuté (2026-07-13) — QUATRE DÉCISIONS DATÉES (record: sessions/2026-07-13-web-engine-decision.md; faits vérifiés contre electron.d.ts 43.0.0 INSTALLÉ, le guide web-embeds officiel fetché ce jour, et le registre npm). (1) EMBED: WebContentsView — le tag webview est officiellement DÉCONSEILLÉ (« dramatic architectural changes »), BrowserView est déprécié dans les types mêmes, iframe meurt sur X-Frame-Options (Colab compris); coûts nommés et acceptés: synchronisation de bounds sous le rect du panneau (précédent ResizeObserver S06d) et MASQUAGE de la vue sous les modales (couture S03, pas une surprise). (2) POSTURE D'ISOLATION écrite avant tout code: les quatre réglages requis sur l'invité et AUCUN preload — surface de pont nulle pour le contenu distant; partition persist:web-sources PERSISTANTE par décision (le Colab quotidien de l'owner exige des connexions qui survivent; coût vie-privée énoncé: cookies Google sur disque sous userData/Partitions, session UI de confiance intouchée, clés Atomik jamais dans cette session — veto owner possible); permissions REFUS par défaut (fullscreen + clipboard-sanitized-write seuls autorisés — copier depuis les cellules Colab EST le flux d'apprentissage); popups refusés + navigation sur place, RISQUE NOMMÉ: le mur de connexion Google des navigateurs embarqués — normalisation d'UA en atténuation S03, fenêtre-enfant d'auth contrôlée en design de repli, déclencheur de re-examen = l'owner bute sur le banc Colab; téléchargements annulés en MVP. (3) READER: @mozilla/readability 0.6.0 (Apache-2.0) sur le DOM POST-JS CAPTURÉ (sérialisé à l'import, sha256 consigné), parsé dans MAIN via linkedom 0.18.13 — les revendications d'extraction ne viennent JAMAIS d'un re-fetch qui verrait un autre contenu (SPA/authentifié), ni du chemin d'affichage (le split de fidélité de 10, adapté); turndown 7.2.4 + plugin gfm pour le markdown; IMAGES téléchargées dans MAIN par les gates existants vers media/ (aucune dépendance nouvelle); article-extractor REJETÉ (orienté re-fetch), defuddle 0.19.1 NOTÉ alternative de re-banc 34 avec déclencheur nommé (Readability abîme une page de MATHS que l'owner utilise — son matériau central). (4) SNAPSHOT: MHTML par savePage intégré (vérifié dans les types) — UN fichier de preuve auto-contenu, zéro dépendance; déviation du croquis snapshot.html de 09 consignée; le DOM sérialisé est l'ENTRÉE du reader (hashée), pas un second snapshot. Installé: 4 paquets, 0 vulnérabilité; 262/28 + typecheck verts; note de module (faits de dépendances datés) dans la même unité. Prochain pas S03: l'onglet vue web.
- CP-MVP-006 S03 exécuté (2026-07-13) — L'ONGLET WEB EST RÉEL. Genre d'onglet `source-web` (le type que 03 nommait déjà): le renderer ne rend que le CHROME de navigateur de confiance (WebView.tsx — barre d'URL, ‹ › ⟳/✕, bandeau d'échec honnête avec retry, placeholder mesuré); la page vit dans UNE WebContentsView isolée par onglet, possédée par MAIN (registre dans index.ts, portes pures testées dans web-view.ts): les quatre réglages requis + partition persist:web-sources et AUCUN preload — surface de pont nulle, la page live ne peut pas atteindre le vault (13). http(s)-seulement imposé CÔTÉ MAIN sur ensure/navigate/will-navigate/popups, quoi que demande le renderer; UA normalisé Chrome (l'atténuation du mur de connexion, consignée); permissions refus-par-défaut (fullscreen + clipboard-sanitized-write); téléchargements annulés; popups refusés + navigation sur place. L'URL voyage dans le param `url` de l'onglet (étiquette = hostname; restauration = rechargement); CHANGER d'onglet ne fait que MASQUER la vue — une session Colab qui calcule SURVIT à la lecture d'un PDF à côté; FERMER l'onglet détruit la vue (les deux chemins de fermeture passent par destroyTabView). Bounds: ResizeObserver + resize fenêtre + vérification par rendu (les glissements de séparateur qui DÉPLACENT sans redimensionner); les canaux de géométrie tolèrent la course ensure/rapport; le panneau réglages prend la garde d'overlay (coût nommé S02: les vues natives peignent AU-DESSUS de l'UI). Sept canaux typés + un push, surface preload épinglée (test). Tests 262→272/30; typecheck/build/smoke verts. SONDE E2E: ATOMIK_SMOKE_WEB (opt-in, dépendant du réseau — jamais dans le run déterministe) + fixture d'état restaurant un onglet web scindé à côté des dev docs → `web=navigated(example.org) panes=2` — restauration → ensure → chargement réel → push typé → barre d'URL, la chaîne entière prouvée sur la vraie app. BANC OWNER EN ATTENTE: connexion Colab (le risque de mur nommé se résout sur SA machine), sensation de navigation quotidienne, scénario d'établi (web + vrai PDF côte à côte). Prochain pas S04: Import as source (bundle sources/web/<slug>/ + .gitignore DANS LE MÊME COMMIT — règle de l'incident).
- CP-MVP-006 S03b — première minute de banc owner (2026-07-13, capture d'écran): l'onglet web rendait en BANDE de 295px à gauche (page Google Connexion dans une colonne étroite, le reste du panneau vide). Cause: `.pane-content` est un flex ROW et `.web-view-tab` n'avait pas de `flex:1` — la colonne se dimensionnait à sa largeur INTRINSÈQUE (trois boutons + input min), et les bounds rapportés suivaient fidèlement ce mauvais rect (la plomberie marchait; le conteneur était faux). Fix une ligne + vérification par capture sur la vraie app: la barre d'URL s'étend sur tout le panneau (fixture split 50/50). Au passage, DEUX bonnes nouvelles du banc: l'owner a ATTEINT accounts.google.com dans la vue (la page de connexion REND — le mur Google ne tombe pas à l'étape sign-in; verdict final après le login complet), et sa QUESTION VIE PRIVÉE a reçu une réponse PROUVÉE SUR DISQUE: la session Google (Cookies, Cache…) vit dans ~/.config/atomik-desktop/Partitions/web-sources/ — HORS du dépôt; git n'en voit RIEN (git status vérifié zéro fichier de partition; .atomik/ ignoré par ailleurs); les cookies ne peuvent pas atteindre GitHub PAR CONSTRUCTION — l'arbre suivi ne les contient jamais. La vigilance réelle porte sur les SNAPSHOTS S04 (contenu perso d'une page connectée) — couverte par la règle .gitignore-à-la-création. Tests 272/30, typecheck, smoke verts.
- CP-MVP-006 S03c — LE MUR GOOGLE EST TOMBÉ, diagnostic aux preuves vives et parade SIÉGÉE (2026-07-13, capture owner: « Impossible de vous connecter — ce navigateur ou cette application ne sont peut-être pas sécurisés »). Le déclencheur de re-examen S02 a tiré. Diagnostic PAR CAPTURE SUR CETTE MACHINE (sonde executeJavaScript dans l'invité, retirée après): l'UA sort PROPRE (Chrome/150, zéro token Electron — la normalisation marche) mais les BRANDS des client hints disent « Not;A=Brand + Chromium » SANS « Google Chrome » — l'empreinte exacte d'un Chromium embarqué; l'UA seul ne suffisait pas, et le repli fenêtre-d'auth-contrôlée du record aurait reproduit le mur À L'IDENTIQUE (même moteur, mêmes hints) — repli NON pris, la raison est consignée. PARADE DATÉE (addendum au record de décision; lignée nativefier#831, re-confirmée par recherche ce jour; compatibilité pour un HUMAIN se connectant à SON compte — pas de l'automatisation): sur les hôtes d'auth EXACTS accounts.google.com + accounts.youtube.com SEULEMENT, l'invité se présente en FIREFOX — FIREFOX_UA épinglé (Firefox/140 Linux), en-têtes Sec-CH-UA* RETIRÉS (un Firefox qui enverrait des hints Chromium serait une nouvelle empreinte), et navigator.userAgent commuté par navigation main-frame pour que les scripts de page lisent la même histoire que le réseau (onBeforeSendHeaders + did-start-navigation); PARTOUT AILLEURS le Chrome normalisé reste — Colab veut du Chrome. Un domaine sosie (accounts.google.com.evil.example) reste dehors: correspondance de hostname EXACTE, testée. Helpers purs testés (isGoogleAuthUrl, authRequestHeaders, forme du FIREFOX_UA). Nouveaux déclencheurs: Google re-mure malgré la présentation Firefox (→ examiner le flux servi, considérer la fenêtre d'auth AVEC cette présentation); Google signale le Firefox épinglé comme obsolète (→ bump une ligne datée). Tests 272→274/30, typecheck, smoke verts. EN ATTENTE: nouvel essai de connexion owner.
- CP-MVP-006 S03c — VALIDÉ OWNER (2026-07-13): CONNEXION GOOGLE RÉUSSIE, COLAB TOURNE DANS LE PANNEAU — le risque nommé S02 est CLOS, le scénario d'établi M5 est VIVANT. Le premier réessai après la parade murait encore: la partition portait l'état FLAGGÉ de l'essai pré-parade (le verdict Google persiste dans les cookies de session; « Réessayer » le rejoue) — après purge de ~/.config/atomik-desktop/Partitions/web-sources et login frais via la présentation Firefox, succès. Note opérationnelle consignée au record: si le mur réapparaît, purger la partition AVANT de juger la parade morte — une session empoisonnée est indistinguable d'un contournement raté.
- Micro-unité de dogfooding UI (owner, à la validation Colab: « different tone of color theme to segment better the active/non active zone — tabs, header, pane, panel — and UI zone separation in general, most proven best practice »). SYSTÈME À DEUX SURFACES posé en tokens (lignée VS Code/Zed — la pratique éprouvée demandée): `--bg` devient le CHROME (coque: tabstrips, barres note/tree/pdf/web, panneaux d'arbre, scène média) et `--surface` naît comme ton de CONTENU (éditeurs, notes rendues, viewers, popovers) — appliqué en UN point structurel (.pane-content) + panneaux explicitement en retrait; échelle ordonnée dans les deux schémas (clair: chrome #ecece6 < code-bg < surface #fbfbf9; sombre: chrome #141418 < surface #1e1e23 < code-bg) et les CINQ pastels gagnent leur --surface teinté (le contenu ne blanchit pas — l'intention pastel est préservée). FOCUS: le signal migre de la bordure de strip vers L'ONGLET ACTIF — panneau focalisé: pilule couleur contenu + liseré d'accent inset 2px (le motif activeBorderTop de VS Code); panneau non focalisé: pilule chrome discrète; onglets inactifs en --muted, hover les remonte — un coup d'œil répond à « quel onglet » ET « quel panneau ». Au passage: pdf-nav et source-image-original perdaient leurs light-dark() codés en dur → var(--bg) (les PASTELS étaient FAUX sur ces zones — vrai fix, pas cosmétique); input d'URL web → tokens; popovers (réglages IA, menu @) sur --surface + ombre (élévation cohérente). Vérifié par CAPTURES sur la vraie app, clair ET sombre (fixture thème forcé), split web-focalisé/docs-non-focalisé — les deux états d'onglet visibles côte à côte. Incident de fixture au passage: l'état smoke avait été MUTÉ par une session interactive (onglet vault actif + recherche Google « Lancer le dé » 🎲 — la fenêtre smoke est une vraie app) → fixture réécrit; leçon: le réécrire avant chaque run de sonde. Tests 274/30, typecheck, build, smoke verts (CSS seul — aucun test ne bouge).
- CP-MVP-006 S04 exécuté (2026-07-13) — IMPORT AS SOURCE, l'action EXPLICITE (09: jamais automatique). web-import.ts + canal typé web-view-import-source derrière le bouton « Import as source » de la barre web. Le bundle sources/web/<slug>/: snapshot.mhtml = savePage MHTML — la page TELLE QUE RENDUE (sha256 + octets au dossier; la décision de format S02); source.md porte le jeu de preuves 09 (URL originale/canonique, accessed_at, author/publisher/published/updated quand les meta de la page les offrent, ligne licence « not reviewed » en attente d'un humain); index.md la carte. DISCIPLINE DE MÉFIANCE: toute chaîne contrôlée par la PAGE est hostile jusqu'à assainissement — cleanMetaText (une ligne, caractères de contrôle éliminés, | et backtick retirés, plafonné), titre YAML-quoté, canonical rejeté sauf http(s) (javascript: meurt au test) — l'injection de frontmatter/table markdown est VAINCUE par tests dédiés; slug du titre (repli URL), frères numérotés, wx + nettoyage de bundle COMPLET sur échec (un savePage raté ou vide ne laisse RIEN); sonde de métadonnées = executeJavaScript lecture seule sous course de 3 s (page hostile/suspendue → repli titre+URL). Le dossier s'ouvre dans un onglet vault au succès; sources/web/ entre au .gitignore DANS CE COMMIT (règle de l'incident — les snapshots de pages connectées sont personnels); SourceImageView affiche une ligne amicale pour les dossiers web (le routage du clic vers la vue web = S06). Tests 274→282/31; typecheck/build/smoke verts. PROUVÉ E2E sur la vraie app: échelon de sonde ATOMIK_SMOKE_WEB_IMPORT — attend le bouton ACTIVÉ, clique le VRAI bouton, attend le bundle sur disque → webImport=ok(example-domain), MHTML Blink réel, hashes cohérents (premier essai TIMEOUT: clic sur bouton désactivé pendant le chargement — la sonde attend désormais l'activation; leçon de sonde consignée). Prochain pas S05: extraction reader (texte + images depuis la page CAPTURÉE).
- CP-MVP-006 S04b — TROIS CLASSES DE CLICS MORTS tuées (2026-07-13, rapports owner à la première minute de dogfooding S04: « rien ne se passe sur Original URL / Local snapshot », puis « on a ça partout sur les dossiers, original audio ou original image »; « original pdf work » — S06e l'avait traité). Le routeur de liens partagé (useVaultNote) gagne les trois familles manquantes: (1) LIENS EXTERNES http(s) → ONGLET WEB (onOpenWebUrl, câblé Workspace→VaultView/ProjectView/SourceImageView — le web est à un onglet, pas à un navigateur externe; mailto reste inerte, jamais de navigation sur place); (2) SNAPSHOT .mhtml → ouverture EXTERNE (openSourceExternally — même trappe que les originaux audio WSLg; .mhtml entre dans l'allowlist d'assets pour ça); (3) ORIGINAUX image/audio → VUE SOURCE de leur bundle, exactement le motif PDF de S06e étendu à ses frères (isMediaFilePath pur testé — la ligne « Original photo/audio » de CHAQUE dossier capture était morte depuis toujours, le rapport web l'a révélé). Une partie du routage prévu S06 (« web dossier links route to the source view ») atterrit ici en avance sur rapport owner; S06 garde le routage des dossiers web VERS la vue web (lire le dossier au clic) et les citations @. Tests 282→283/31; typecheck/build/smoke verts.
- CP-MVP-006 S05 exécuté (2026-07-13) — EXTRACTION READER: la page importée devient du texte lisible/corrigeable/citable. web-reader.ts + mhtml.ts. DÉCISION DE SOURCE: l'extraction lit le snapshot.mhtml SUR DISQUE (jamais un re-fetch — 09; jamais le chemin d'affichage — le split de fidélité de 10), donc elle marche sur le dossier à tout moment comme l'extraction PDF lit original.pdf. Pipeline: mhtml.ts parse le multipart (décodage quoted-printable/base64, collecte des ressources image par Content-Location) → linkedom bâtit le DOM → Readability choisit l'article (charThreshold 200, REPLI sur le body complet si Readability rend vide — pages-app/éparses ne donnent jamais un reader.md vide) → turndown+gfm en Markdown; CHAQUE <img> de l'article (partie mhtml OU data: URI) atterrit dans media/ hashé, la référence markdown réécrite en relatif — TEXTE ET IMAGES, la demande owner. UNE trace 'extract' déterministe par run (échec tracé aussi); wx no-clobber; dossier bascule status→extracted + reader_text/reader_trace_id; « Delete reader… » (resetWebReader) retire reader.md ET media/, restaure la forme pré-extraction (règle permanente, aller-retour testé). Le hook de flip de correction sert désormais TROIS fichiers dérivés — reader.md rejoint transcript.md et extracted.md. UI: SourceImageView grandit un panneau web-source (boutons page live + snapshot, Extract/Delete reader); transcribe/OCR/rotation gatés hors web; les dossiers web s'ouvrent dans la vue source. Deux bugs d'intégration domptés: linkedom LÂCHE un <body> nu sans <html> → wrap complet; fixture Readability court → repli body + charThreshold. Deps: @types/turndown (dev) + turndown-plugin-gfm.d.ts local. Tests 283→292/33; typecheck/build/smoke verts. PROUVÉ sur une VRAIE capture Wikipedia (Backpropagation): 92 Ko de Markdown propre + 116 RENDUS SVG D'ÉQUATIONS dans media/ — le matériau maths de l'owner; re-banc defuddle non nécessaire. Prochain pas S06: sélection→IA→note + citations @ pour les sources web.
- CP-MVP-006 S06 (partiel, sur demande owner « add quick action on web source and their extracted representation ») — le menu @ offre désormais les SOURCES WEB comme il offrait déjà PDF et images: type de ressource 'web' ajouté (resourceInfoFrom détecte resource: http(s) — l'URL EST la ressource, aucun chemin vault à résoudre), et bundleCompletions offre pour un dossier web deux entrées: « @<nom> url » (citation vers la PAGE LIVE, URL absolue) et « @<nom> dossier » (lien vers source.md). Les REPRÉSENTATIONS DÉRIVÉES: reader.md rejoint extracted.md et transcript.md dans derivedTextEntries (troisième fichier dérivé du menu @ comme du hook de flip) — « @<nom> reader » insère le reader text en bloc de citation, corps lu à l'APPLY (jamais au menu), frontmatter dépouillé, lien source en tête. quoteBlockFor gagne le genre 'reader text'. Tests 292→295/33 (citation web absolue, choix web url+dossier, reader en bloc dérivé); typecheck/build/smoke verts. Reste de S06: sélection→IA→note portant la provenance URL, et le routage clic des liens de dossier web VERS la vue web (lire le dossier / ouvrir la page) — l'ouverture live-page + snapshot existe déjà via le panneau web-source de S05.
- CP-MVP-006 S05b + icône lien externe (2026-07-13, dogfooding owner sur de vraies pages Wikipedia). TROIS bugs d'extraction reader corrigés + la demande UX. (1) EEXIST qui BLOQUAIT un bundle: deux URLs d'images au CONTENU identique (une icône partagée) hashaient au même nom → le second write wx échouait; la déduplication passe de la clé-URL à la clé-CONTENU (Set de noms hashés) — deux URLs, mêmes octets = UN fichier media, les deux <img> pointent dessus. (2) NETTOYAGE: le catch d'échec ne retirait pas le media/ partiel, et un media/ résiduel d'un run raté (reader.md absent) coinçait le bundle — ni re-extraction (EEXIST) ni suppression (resetWebReader exige reader.md) ne passaient; désormais le catch retire reader.md+media/, ET l'extraction efface tout media/ pré-existant avant d'écrire (reader.md étant gardé absent en amont) → AUTO-RÉPARATION d'un bundle coincé. Le bundle « groupe-des-sept » de l'owner (media/ orphelin de 41 fichiers, pas de reader.md) se ré-extrait maintenant proprement. (3) SVG MATH INVISIBLES: les rendus SVG d'équations (le cœur du cas maths) étaient téléchargés dans media/ mais l'allowlist d'assets et INLINABLE excluaient .svg → readSourceAsset les rejetait; .svg + .gif ajoutés aux deux. Résolution de chemin vérifiée correcte sur le vrai reader.md sur disque (chemin complet → media/ du bundle). ICÔNE LIEN EXTERNE (demande owner, capture): les liens http du markdown rendu portent un petit ↗ (CSS pur sur a[href^="http"]::after — convention Obsidian/MediaWiki) pour ne pas les confondre avec les liens internes; liens .md et #ancres non marqués. Tests 295→297/33 (dedup contenu, auto-réparation bundle coincé); typecheck/build/smoke verts. À re-tester par l'owner après redémarrage: supprimer les bundles web coincés OU les ré-extraire (auto-réparation), et vérifier que les images + SVG maths s'affichent dans reader.md.
- CP-MVP-006 S05c — TABLES EN HTML BRUT (2026-07-13, capture owner: le reader.md du G7 affichait la soupe de balises <table about="#mwt2">…). Cause: turndown-gfm ne convertit en table markdown QUE les tables à ligne d'en-tête propre; les infobox Wikipedia (en-têtes de LIGNE <th scope="row">, colspan) sont GARDÉES en HTML brut, que le renderer vault (html:false) affiche littéralement. Deux défenses ajoutées à readerFromSnapshot: (1) flattenComplexTables — pré-passe DOM avant turndown: toute table SANS ligne d'en-tête tout-<th> devient une suite de <p>, une par ligne, cellules jointes par « — » (l'infobox G7 → « Création — 1975 », « Type — Conférence » lisibles); les vraies tables de données à en-tête gardent leur grille pipe via gfm. (2) stripLeftoverHtml — FILET DE SÉCURITÉ sur la sortie turndown: tout HTML résiduel (table rejetée, span, commentaire) réduit à son texte, entités décodées, espaces normalisés — GARANTIT qu'aucune balise littérale n'atteigne reader.md (un reader est lossy par nature, l'utilisateur corrige). Vérifié sur le VRAI snapshot G7 de l'owner: zéro HTML brut, données d'infobox préservées en texte. Tests 297→298/33 (filet de sécurité: table brute→texte, entités, tags inline; fast-path markdown pur intact); typecheck/build/smoke verts.
- CP-MVP-006 S05d — « pourquoi pas une table markdown ? » (question owner). Réponse: on PEUT, mais une table GFM EXIGE une ligne d'en-tête et des cellules mono-ligne — les infobox (en-têtes de LIGNE, colspan, <br>, cellules multi-blocs) violeraient les deux et donneraient une grille CASSÉE, pire que du texte. normalizeTables fait donc du TRIAGE en trois voies: (1) tables à ligne d'en-tête tout-<th> → laissées à gfm (converties fidèlement); (2) tables RÉGULIÈRES (colonnes de compte constant, sans colspan/rowspan, sans table nichée, sans enfant bloc) → PROMUES en vraie table pipe (thead vide préfixé, gfm convertit avec le markdown inline des cellules — liens/images préservés); (3) le reste (infobox) → APLATI en un paragraphe lisible par ligne, cellules jointes par « — », <br> en cellule → « · ». Bugs linkedom en chemin: replaceWith silencieusement ignoré → replaceChild; querySelectorAll('th,td') happait les cellules NICHÉES → enfants directs seulement. stripLeftoverHtml reste le filet final. Vérifié: table régulière → grille pipe; infobox synthétique → « Création — 1975 », « Ancien — G5 · G6 »; VRAI G7 → zéro HTML brut, « Création — 1975 » lisible. Tests 298→300/33; typecheck/build/smoke verts.
- CP-MVP-006 S05e — FIDÉLITÉ D'EXTRACTION, étape 1 du plan owner « les deux, dans cet ordre » (question: récupère-t-on la hiérarchie? répéter les en-têtes fusionnés?). DIAGNOSTIC AUX PREUVES: le reader.md du G7 (149 Ko) avait ZÉRO titre — le DOM brut a 8 h2/5 h3/4 h4 (« Historique et contexte », « Importance économique »…), Readability les RASE TOUS sur le HTML Parsoid de Wikipédia (h2:0 après). Correctif ARCHITECTURAL: EXTRACTION STRUCTURE-FIRST. (1) findContentRoot choisit le vrai conteneur de contenu (mw-parser-output / main / [role=main] / article / wrappers CMS; premier match avec ≥500 chars ET un titre — l'exigence de titre distingue un article structuré d'un wrapper de chrome), stripChrome retire nav/toc/navbox/mw-editsection/reflist NOUS-MÊMES en gardant titres/listes/tables; Readability reste le REPLI pour les pages-article génériques sans conteneur fort. Les titres, listes et structure survivent. (2) EXPANSION DES CELLULES FUSIONNÉES (idée owner « répéter les en-têtes »): expandCellSpans développe colspan (clone par colonne) et rowspan (reporté vers le bas via une matrice consciente des spans) → une table irrégulière seulement à cause des fusions devient RÉGULIÈRE et se promeut en vraie table pipe au lieu d'être aplatie (« Données » d'un colspan=2 → « | Données | Données | »; un rowspan « France » recopié sur chaque ligne couverte). Bugs linkedom domptés au passage (replaceWith→replaceChild, cellules directes vs descendantes). RÉSULTAT sur le VRAI G7: de 0 titre à 7 h2 + 5 h3 + 4 h4, 4 tables pipe, 34 listes, ZÉRO HTML brut — une note structurée. Tests 300→301/33; typecheck/build/smoke verts. Reste étape 2: reader mode live (toggle Obsidian-style sur le web viewer réutilisant ce moteur).
- CP-MVP-006 S06 — READER MODE LIVE (étape 2 du plan owner, façon Obsidian). Un toggle « Aa reader » dans la barre du web viewer rend la page COURANTE en texte propre SUR PLACE — sans fichier, sans import. Canal web-view-reader-text → main fait executeJavaScript pour le outerHTML post-JS de la vue live et le passe au MÊME moteur d'extraction que l'import snapshot (readerFromHtml, structure-first + tables), avec une map de ressources VIDE → les images distantes tombent naturellement (une lecture transitoire = texte+structure; la durabilité avec media local, c'est Import-as-source). Le renderer rend le markdown retourné via markdown-it (html:false — jamais le balisage de la page) dans un overlay .web-reader sur la surface de contenu, et MASQUE la vue native pendant la lecture (la règle de visibilité gagne: visible seulement si non-couvert ET pas en lecture). Le bouton se déverrouille sur vue-EXISTE, pas fin-de-chargement (des pages ne signalent jamais la fin — bug attrapé à la sonde: Wikipedia restait « loading », bouton désactivé). Une VRAIE navigation (origin+path change, jitter de hash ignoré) ferme le reader; un saut d'ancre in-page non (Wikipedia en émet, fermer serait pénible — bug attrapé à la sonde: le reader se fermait entre la mesure et la capture). Refacto: readerFromSnapshot délègue à readerFromHtml(html, url, resources) — un seul cœur pour l'import durable ET le reader live. PROUVÉ E2E sur Wikipedia live: webReader=ok(23h), persiste 3s (persist=23), bouton bascule « ✕ reader », overlay présent. La capture d'écran montrait blanc = artefact WSLg connu (capturePage ne composite pas l'overlay sous une vue native adjacente) — l'évidence DOM tranche. Tests 301/33; typecheck/build/smoke verts. Les DEUX étapes du plan owner livrées: fidélité d'extraction (S05e) + reader mode live (S06).
- CP-MVP-006 S05f — IMAGES LITTÉRALES DANS LES CELLULES APLATIES (2026-07-13, capture owner sur le reader.md du G7). Le chemin d'aplatissement des tables construisait les refs d'image en CHAÎNE (`![](src)`) puis les posait en `p.textContent` → turndown ÉCHAPPAIT les métacaractères → `\![\]\(…\)` littéral affiché au lieu de l'image; et l'expansion colspan dupliquait les cellules (« Groupe des sept (G7) — Groupe des sept (G7) »). Fix: l'aplatissement construit désormais le paragraphe avec de VRAIS nœuds — texte en text nodes, images en éléments `<img>` CLONÉS (turndown émet alors un vrai `![alt](./media/…)` que markdown-it rend en image et que vaultImageSources inline); les cellules identiques consécutives (dup de colspan) sont dédupliquées. Vérifié sur le vrai G7: 42 refs d'image propres (`![Drapeau du Canada](./media/…)`), zéro `\![`, zéro « X — X ». Tests 301→302/33; typecheck/build/smoke verts. RAPPEL GESTION MÉDIAS par mode (question owner): reader.md DURABLE = images téléchargées du snapshot dans sources/web/<slug>/media/ (hashées), référencées ./media/x.webp, inlinées à l'affichage via readSourceAsset (webp/svg/gif dans l'allowlist depuis S05b); reader mode LIVE = map de ressources vide → images distantes tombent (lecture transitoire texte+structure); snapshot.mhtml = les images sont EMBARQUÉES dans le fichier de preuve. À re-tester owner: Delete reader… + re-extraire le G7 pour appliquer le fix (l'ancien reader.md date d'avant).
- Micro-unité UI (owner, deux demandes). (1) INVERSION DES PANNEAUX de la vue source: l'ordre de lecture attendu est arbre → DOSSIER → aperçu média, or c'était arbre → aperçu → dossier. Fix par `order` flex (le DOM garde original-first pour ne pas toucher le code existant): .source-image-dossier order:1, .source-image-original order:2, la bordure passe de border-right à border-left. Le texte-savoir (dossier) colle désormais à l'arbre, la scène média (photo/PDF/panneau web) vient après. Vérifié par capture sur le vrai dossier web G7 (arbre | Source dossier + table de preuves | panneau fr.wikipedia.org). (2) HISTORIQUE DE NAVIGATION ‹ ›: boutons précédent/suivant chronologiques dans la note-bar des trois vues (vault, project, source). `vault/nav-history.ts` — registre PUR par onglet (clé = tab.id, comme le registre pending-page PDF), EN MÉMOIRE (état UI jetable, 03 — un redémarrage repart d'une piste vierge); recordVisit (no-op si c'est l'entrée courante — back/forward et refresh ne gonflent pas la piste; une visite en arrière élague la branche avant, règle navigateur); cap 100 entrées; useNavHistory câble le tout et rejoue par la MÊME porte (guardedOpen en vault/project — respecte le garde de buffer sale; openNote en source). Tests purs (piste linéaire, no-op courant, divergence élague le futur, pistes indépendantes par onglet, onglet inconnu vide). Tests 302→307/34; typecheck/build/smoke verts.
- Micro-unité UI (owner, 2026-07-14) — HEADER GLOBAL RÉTABLI + VRAI MAXIMIZE. (1) HEADER: retour à une entête d'app au-dessus de l'arbre de panneaux (App.tsx: <AppHeader/> puis <Workspace/>) — « atomik » à gauche, un bouton menu ☰ (AppMenu — dropdown unique portant la SÉLECTION DU THÈME en boutons + la clé Mistral: statut/champ/Save/Clear, la clé ne revient jamais au renderer, 13), puis les contrôles fenêtre; les ONGLETS descendent sur la ligne du dessous, par panneau. Le header est la surface de drag (les tabstrips ne portent plus le trio de contrôles). Inverse la disposition MVP-001 « la tabstrip EST la ligne du haut ». MenuIcon ajouté; AiSettings.tsx + ThemePicker.tsx SUPPRIMÉS (morts, remplacés par AppMenu); plomberie controlsPaneId/topRightLeafId retirée de Workspace. Vérifié par CAPTURES sur la vraie app: header (brand·menu·contrôles) + onglets en dessous, puis menu ouvert (8 thèmes, Light actif; section clé Mistral). (2) FENÊTRE: la BÉQUILLE fullscreen retirée — l'ancien contournement WSLg mappait maximize→fullscreen (wslg#1015: fenêtre borderless maximisée = gap transparent + clics décalés), mais ça masquait aussi la barre des tâches (la plainte owner « maximized window »). Cause racine du gap = les marges de l'OMBRE client; donc maximize est un VRAI maximize désormais, et l'ombre est retirée EN maximisé (setHasShadow(false) sur l'event 'maximize') puis remise en restauré (les poignées de redimensionnement vivent dans ces marges) — pas de gap maximisé, redimensionnement de retour restauré, barre des tâches préservée. Fullscreen (F11) redevient une chose séparée. FULLSCREEN_IS_MAXIMIZE + la conversion + isFullScreen dans isWindowMaximized supprimés. Tests 307/34; typecheck/build/smoke verts. À VÉRIFIER VISUELLEMENT PAR L'OWNER sur WSLg live (capturePage headless ne peut pas confirmer la géométrie de maximize) — déclencheur de re-examen consigné (setBounds vers la work area si un build WSLg garde un gap). CSS morte .ai-settings*/.theme-picker laissée (inoffensive, nettoyage ultérieur).
- FENÊTRE WSLg — LE DÉCALAGE PERSISTAIT (owner: « oui il y a toujours le problème de décalage »): setHasShadow(false) + maximize natif ne suffisait pas — le décalage est dans la GÉOMÉTRIE de maximize du gestionnaire de fenêtres WSLg lui-même (wslg#1015), pas dans l'ombre. FIX FINAL: sous WSLg (IS_WSLG), on n'utilise PLUS DU TOUT le maximize du WM — on positionne la fenêtre nous-mêmes sur screen.getDisplayMatching(bounds).workArea et on restaure les bounds sauvegardés (WeakMap wslgRestoreBounds fait aussi office de drapeau maximisé); un maximize OS-initié (snap/Win+Up) est capté sur l'event 'maximize', défait (unmaximize) puis refait en ajustement manuel work-area. PROUVÉ par sonde: setBounds(workArea) atterrit EXACTEMENT (getBounds après == workArea, MATCH true, zéro décalage) — le chemin du WM était bien le coupable. L'ombre reste retirée en maximisé. Tests 307/34; typecheck/build/smoke verts. À confirmer visuellement par l'owner (la sonde prouve les bounds, pas les pixels).
- FENÊTRE WSLg — RÉSOLU par sonde décisive (owner: « laggy + hides taskbar » après le setBounds manuel). L'EXPÉRIENCE qui tranche: window.maximize() natif atterrit {0,0,1920,1032} sur un écran 1080 — il RÉSERVE les 48px de la barre des tâches (le WM proxie vers Windows qui la connaît), alors que screen.workArea ment (1080 plein). Donc mon setBounds(workArea) couvrait la barre ET laggait; le maximize natif est le bon chemin (rapide, respecte la barre). FIX FINAL: window.maximize()/unmaximize() nu, PLUS de toggle setHasShadow (probable source du décalage de commit 1), plus de bounds manuels. isWindowMaximized = window.isMaximized(). import screen retiré (inutilisé). Tests 307/34; typecheck/build/smoke verts. Si un décalage VISIBLE persiste sur une build WSLg = vrai wslg#1015, options honnêtes consignées (vivre avec / frame natif qui perd le header custom / fullscreen-maximize qui masque la barre). À confirmer visuellement par l'owner.

## 2026-07-15

- FENÊTRE WSLg — LE DÉCALAGE ROOT-CAUSÉ ET CORRIGÉ, PAR SONDE CÔTÉ WINDOWS. La percée méthodo qui débloque tout: depuis WSL, powershell.exe capture le VRAI bureau Windows composé (CopyFromScreen), lit le rect de la fenêtre hôte RAIL (FindWindow/EnumWindows + GetWindowRect/DWM) et INJECTE des clics (SetCursorPos + mouse_event) — ce que capturePage ne peut structurellement pas voir, on le mesure désormais au pixel. MESURÉ (2 écrans): fenêtre restaurée = pixel-exacte (hôte = contenu + marges d'ombre ~32-36px, correctement décalé); fenêtre WM-MAXIMISÉE = bounds LOGIQUES corrects {0,0,1920,1032} (voilà pourquoi toutes les sondes bounds-level disaient « réparé ») mais l'hôte grandit de 32px et le CONTENU se présente +32px droite/bas avec l'INPUT non décalé → tout ce qui est visible clique 32px à côté; bande transparente gauche/haut, contenu rogné droite/bas — wslg#1015 enfin chiffré. Un setBounds manuel vers la VRAIE work area (sans état WM) = parfait pixels ET clics, ombre INTACTE, en 0-1 ms (le « lag » d'hier = le toggle setHasShadow, pas setBounds). Deux pièges de plus: screen.workArea MENT sous WSLg (1080 plein, les DEUX écrans), et la conversion snap naïve du 0070b6a (unmaximize + setBounds dans l'event 'maximize') PERD la course contre le restore asynchrone du WM — fenêtre finie 4px en retrait, mesuré. FIX (index.ts + wslg-workarea.ts nouveau): sous IS_WSLG on n'entre JAMAIS dans l'état WM-maximisé — maximize = setBounds vers la work area Windows de l'écran courant (requête powershell WINDOWS_SCREENS_PS_COMMAND au démarrage + re-query debounced sur changement d'affichage; mapping Linux = Windows − origine du virtual screen, vérifié par sonde; parse/matching PURS et testés; fallback = écran − 48px en bas), restore = bounds STABLES sauvegardés (enregistrement debounced — la danse WM émet des rects poubelle), conversion snap/Win+Up APRÈS que 'unmaximize' retombe + une ré-assertion gardée à 250 ms. Quirks assumés (dev-env seulement): Win+Down sur une fenêtre WSLg-maximisée minimise; un edge-resize en maximisé garde le drapeau jusqu'au prochain toggle. RÉALITÉ PLATEFORME: malgré ozone-platform-hint=auto et un socket wayland-0 vivant, l'app tourne en XWAYLAND ici — wayland forcé CRASHE (pas de DRM render node) — la mention « Wayland natif » du module note est retirée; toutes les mesures = le chemin X11, celui que l'owner utilise réellement. VÉRIFIÉ SUR LA VRAIE APP en cliquant son propre bouton □ côté Windows: hôte atterrit exactement à contenu = work area (barre des tâches visible), restore = rect d'origine EXACT, le menu ☰ s'ouvre là où il est dessiné. Tests 307→324/35; typecheck/build/smoke verts. Captures de preuve laissées dans C:\Users\toure\AppData\Local\Temp\wslg-probe\. Dernier verrou: l'œil de l'owner sur la vraie session. → OWNER CONFIRME le lendemain: « it works perfectly ».
- AUDIT DE PERFORMANCE (demande owner, audit seul — AUCUN changement de code): mesures réelles (startup, bundle avec attribution par sourcemap, mémoire/CPU par process, plancher IPC, micro-benchs des vrais modules sur corpus synthétiques 1000 notes/1 Mo riche/650 Ko HTML) + lecture de perf de CHAQUE composant (3 passes parallèles: shell React, éditeur/pipeline contenu, main process). Rapport complet: `sessions/2026-07-15-perf-audit.md`. TL;DR: app SAINE à l'échelle actuelle (CPU idle = 0 tick mesuré, démarrage 1,7-1,8 s, frappe sub-frame, écritures 0,2 ms) mais cinq motifs structurels qui grandiront mal: (1) slabs CPU SYNCHRONES dans le main qui gèlent toute l'UI — readerFromHtml MESURÉ à 834 ms pour 650 Ko (toggle « Aa reader » et import web), pré-resize OCR par page, import PDF 200 Mo lu d'un bloc; (2) bundle renderer de 3 Mo TOUT eager, zéro code-splitting — pdf.js 24 %, stack éditeur 38 %, code app 7 %; (3) médias en base64 partout sur l'IPC (×1,33 + 3 copies simultanées par asset); (4) caches d'images NON bornés jamais invalidés (fuite + 2 bugs de justesse: vault switch et rotation servent des octets périmés en live); (5) re-render du workspace ENTIER par dispatch, drags non throttlés (~60 renders/s), pas de memo/virtualisation — OK à 300 notes, mur à 2000+. Env: WSLg rend TOUT en logiciel (gpu/webgl/canvas « software » — taxe dev-only, les builds réels ne l'héritent pas). Gotcha de bench consigné: ensureSyntaxTree headless cale à ~3000 chars — parser les gros docs via markdownLanguage.parser.parse(). 10 actions classées impact×effort dans le rapport; l'owner choisit ce qui devient micro-unités ou étapes de path. → OWNER: « Correct what you feel necessary before adding more features » — quatre micro-unités correctives lancées (ci-dessous).
- CORRECTIF PERF 1/4 — L'EXTRACTION READER QUITTE LE MAIN PROCESS (l'action n°1 de l'audit, 834 ms de gel MESURÉS par toggle « Aa reader »/import pour 650 Ko). `reader-worker.ts` = nouvelle entrée de build (utilityProcess): le slab CPU (parse mhtml + 3 builds DOM + Readability + turndown) tourne dans le worker; validation, écritures wx, handshake dossier et trace RESTENT dans main. `runReaderJob` (index.ts): un fork par job (extraction rare et user-initiée — la mémoire retombe à zéro), timeout 120 s + kill, repli in-process honnête si le fork échoue. `extractWebReaderAsync` prend le compute INJECTÉ — testé avec des stubs: parité de résultat avec le chemin sync (conservé tel quel), échec = zéro bundle partiel + trace failed, et garde anti-concurrence par bundle (le sync ne pouvait pas s'entrelacer; l'async oui — un double-clic Extract aurait fait nettoyer les fichiers frais du premier run par l'échec wx du second). PROUVÉ par sonde Electron sur le vrai worker buildé: page 650 Ko convertie (608 Ko de markdown), PIRE gel du main = 13 ms (vs 834 ms avant) — l'app reste vivante pendant l'extraction. Tests 324→327/35; typecheck/build/smoke verts.
- CORRECTIF PERF 2/4 — UN SEUL CACHE D'IMAGES, BORNÉ ET INVALIDÉ (l'action n°3; corrige AUSSI deux bugs de justesse trouvés par l'audit). Avant: le live-preview gardait chaque data URL pleine résolution POUR TOUJOURS dans une Map de module (fuite: ~270 Mo après 50 notes-photos; et il servait les octets de l'ANCIEN vault après un changement de vault, et l'image d'AVANT rotation après un ⟲/⟳ — jusqu'au redémarrage), pendant que le mode read ne cachait RIEN (chaque autosave re-transférait toutes les images inline en base64 sur l'IPC — ~34 Mo par pause de frappe sur une note à 5 photos). Maintenant: `vault/image-cache.ts` — LRU borné 64 Mo (budget testable), rotation cuite dans la valeur, marqueurs loading/failed gratuits, partagé par le widget live ET l'inliner read (hit = zéro IPC); `clearImageCache()` sur atomik:vault-changed (abonnement global dans App.tsx — le shell survit à toutes les vues) et `invalidateImage(assetRel)` quand une rotation atterrit (SourceImageView.rotate) → le prochain rendu re-fetch avec la bonne rotation. primeImageCache (seam de test inutilisé) supprimé. Tests 327→335/36 (8 tests purs: round-trip, éviction oldest-first qui s'arrête à temps, recence rafraîchie par lecture, entrée surdimensionnée utilisable puis évincée, invalidation ciblée, clear). Typecheck/build/smoke verts.
- CORRECTIF PERF 3/4 — CODE-SPLITTING DU RENDERER (l'action n°2; zéro import dynamique avant — tout parsait à CHAQUE lancement). React.lazy dans Workspace.tsx pour les DEUX vues lourdes: SourceImageView (entraîne pdf.js → chunk de ~850 Ko) et CaptureView (qrcode + fix-webm-duration → ~100 Ko); leurs chunks se chargent au premier onglet source/capture (fichier local — le fallback Suspense est un clignement). MESURÉ après: bundle eager 2,98 → 2,0 Mo (−33 %), et lancement→contenu 1,70-1,82 s → 0,71-0,72 s (5 runs; le coût app au-dessus du plancher Electron passe de ~1,2 s à ~0,25 s). Garde-fou consigné dans le module note: tout nouveau dep lourd doit rester derrière une vue lazy — un import statique depuis un module eager défait le split en silence. Tests 335/36, typecheck/build/smoke verts.
- CORRECTIF PERF 4/4 — LOT DE PETITES CORRECTIONS (l'action n°10 + morceaux de n°6/8 de l'audit, chacune locale et sans changement de comportement visible): (1) import PDF: le CAP se vérifie par statSync AVANT readFileSync — l'ancien ordre lisait jusqu'à 200 Mo en mémoire pour les refuser (le check octets reste: le fichier peut changer entre stat et read); (2) EditorPane: le dirty-check par frappe passe de doc.toString() !== savedString (matérialisation du document ENTIER par frappe — 1-2 Mo d'alloc sur une grosse note) à doc.eq(savedText) sur le Text CM (partage structurel), baseline posée depuis le doc réel de la vue au mount; (3) fuites blob: — URL.revokeObjectURL sur changement/démontage pour l'audio de SourceImageView et les previews de CaptureView (chaque écoute retenait ses octets pour la session; revoke absent du renderer entier, vérifié par grep à l'audit); (4) recherche: garde latest-wins dans useTreeSearch (deux scans lents pouvaient se doubler et afficher des résultats périmés); (5) webViewEnsure: une entrée de vue DÉTRUITE (fermeture de fenêtre) est purgée et recréée au lieu de faire planter stateOf; (6) AppMenu s'abonne au THÈME (string) au lieu du store entier (il re-rendait à chaque dispatch, drags compris); (7) `base64ToBytes` (source/bytes.ts, testé) remplace Uint8Array.from(atob, cb) — un appel JS par octet, des secondes sur un gros PDF — dans PdfView et mediaObjectUrl; (8) PdfView: la largeur observée se stabilise 120 ms avant re-rasterisation (un drag de divider re-rasterisait la page pleine résolution par pointermove; la PREMIÈRE mesure reste immédiate — le bug blank-until-poked ne revient pas). Idiomes consignés dans Common mistakes du module note. Tests 335→337/37; typecheck/build/smoke verts.
- CP-MVP-006 S06 SE FERME — LA PROVENANCE URL (le dernier morceau; les @ citations, le routage dossier↔web et le live reader avaient pré-atterri aux S04/S05). `web-provenance.ts`: une sélection dans `sources/web/<slug>/reader.md` remonte à l'identité de son dossier (original_url, accessed_at, titre dé-quoté) — résolue CÔTÉ APPELANT dans le handler run-ai-operation, truth.ts/ai-mock.ts restent sans fs; `EvidenceRecord.source` gagne la tranche optionnelle url/dossierPath/accessedAt/title du croquis de 28 (jamais affirmée par le renderer); match strict du relPath (slug sans point ni « / » — impossible de sortir de sources/web/), résolution best-effort (dossier cassé → évidence sans URL, jamais une opération échouée). En aval: une proposition new-note/append depuis un reader porte « Source: [titre](url) — accessed date · [dossier](lien RELATIF — l'absolu-racine est un clic mort dans le routeur) » (09: « create note with URL/provenance »), et le chip de claim source-backed gagne [page ↗] (onOpenWebUrl enfilé VaultView/ProjectView → EditorPane → AiPanel). Tests 337→347/38 (suite web-provenance; cas provenance dans truth + ai-mock, y compris les formes INCHANGÉES sans provenance). E2E PROUVÉ sur la vraie app: nouveau rung ATOMIK_SMOKE_AI_WEB=1 sème un bundle fixture → aiWeb=ok:url=…:dossier=… (renderer → canal typé → lecture dossier → URL dans l'évidence + ligne Source dans la note). Typecheck/build/smoke verts. Prochaine étape: S07 — run d'acceptation 18 §M5 + validation owner (scénario workbench Colab+PDF).
- CP-MVP-006 S06b (rapport owner, premières minutes de dogfooding S06 — capture à l'appui): sélectionner un BLOC @ DÉJÀ CITÉ (« @document transcript ») et lancer l'IA re-citait le markdown cité — le collapse d'espaces d'`excerpt` transformait les « > » de début de ligne en « > » littéraux en pleine phrase (« haïssent > l'injustice »). Corrigé LÀ où l'owner l'avait deviné: dans la CITATION côté note, jamais dans le prompt — `dequote`/`quoteBlock` (ai-mock.ts) composent la citation affichée (dé-citation puis re-citation ligne à ligne, blancs préservés dans le bloc), tandis que la sélection, les candidats de claim et l'évidence gardent les OCTETS BRUTS: le check de containment et les ancres 05 pointent le buffer tel quel — source-backed prouvé préservé sur une sélection citée dans le test. Le format d'import en bloc de citation @ est confirmé et conservé (il marque visiblement le matériau dérivé; le bug n'était que la re-citation). Idiome consigné dans Common mistakes. Tests 347→348/38; typecheck/build/smoke verts.
- CP-MVP-007 PROPOSÉ (directive owner en dogfooding: « une passe de file management dans le tree panel — aujourd'hui on ne peut que collapse/expand et ouvrir en cliquant »). État des lieux consigné dans le chemin: AUCUN verbe rename/move/delete (ni canal, ni UI, ni corbeille, ni watcher — vérifié par grep le 2026-07-16); la création se cache dans l'astuce slash du champ new-note; le push vaultFilesChanged n'arrive qu'après les landings main-side. QUATRE décisions de scope prises par l'owner le jour même (à consigner en addendum du brainstorm 2026-07-06 au S01): dossier = option D (naît avec index.md — dévie du penchant B du brainstorm; dividende: jamais vide, l'invariant d'élagage tient), suppression = CORBEILLE OS (shell.trashItem; rmSync reste aux dérivés), rename = le REFACTOR sanctionné par 27 (backlinks réparés dans le même geste atomique, PREVIEW avant écriture, jamais silencieux, jamais mêlé à des éditions de contenu), et move + DRAG-AND-DROP INCLUS (dévie de la barrière post-MVP du brainstorm — assumé parce que move réutilise la machinerie rename; le chemin séquence la machinerie d'abord, le DnD en dernier). Garde-fous nommés: bundles sources déplacés/supprimés comme UNITÉS (liens internes = contrat), fichiers internes de bundle non renommables, les onglets ouverts SUIVENT (leçon CP-MVP-003 S07), zéro nouvelle dépendance par défaut. Statut proposed; activation à la clôture de CP-MVP-006 (règle: un seul chemin parent actif — le bench S07 owner reste ouvert). Registre index.md mis à jour.
- CP-MVP-006 FERMÉ (owner: « Clore CP-MVP-006 validé », 2026-07-16). Balayage machine des intentions 18 §M5 + frontière vérité/provider dans sessions/2026-07-16-cp-mvp-006-acceptance.md: chaque intention ✓ (vue isolée owner-validée jusqu'à Colab-dans-le-panneau; extraction reader texte+images prouvée sur Wikipedia réel; dossier+snapshot via import EXPLICITE; métadonnées URL/date d'accès jusque dans l'ÉVIDENCE depuis S06; sélection→IA→note owner-validée le 2026-07-16 après S06b), frontière ✓ (aucun provider joignable depuis le chemin web, aucun crawler n'existe, rendu ≠ extraction structurel). Écarts honnêtes consignés: ligne licence en attente humaine (par design); les liens dossier depuis ProjectView s'ouvrent en markdown (préexistant, toutes sources — micro-fix candidat). La validation owner s'est ACCUMULÉE sur trois jours de dogfooding (S03b/c, S04b, S05b-f, S06b — le flywheel a fonctionné). Tests 348/38 à la clôture. — CP-MVP-007 ACCEPTÉ le même message et ACTIVÉ (ACTIVE.md + registre basculés): prochaine étape S01.
- CP-MVP-007 S01 (bootstrap): doctrine épinglée depuis les lectures pleines — 04 (une opération = un diff relisible; supprimer un conteneur ne supprime jamais des notes utilisateur), 27 (pas de réécritures de masse; remplacement de liens CIBLÉ; rename+backlinks = refactor atomique étiqueté, JAMAIS mêlé à du contenu), 20 (la détection de liens cassés est un diagnostic, jamais une auto-réparation silencieuse — l'APERÇU est la porte d'acceptation), 13 §IPC (canaux étroits; la sortie modèle ne choisit jamais un chemin fichier; chaque canal documenté avec sa frontière de confiance). Addendum de décisions écrit en note SŒUR (brainstorm/2026-07-16-tree-file-management-decisions.md) — l'original du 06-07 porte des éditions owner non committées, l'éditer bloquerait son prochain merge. TROUVAILLE d'inventaire qui change la conception: le marqueur de racine de bundle est « dossier contenant directement source.md » — INDÉPENDANT de l'emplacement (les bundles de capture atterrissent où l'utilisateur veut) — les gardes s'ancrent sur le marqueur, pas sur un préfixe sources/. Base 9de5f01, 348/38. Prochaine étape: S02 (création de dossier D de bout en bout + squelette de menu contextuel + push vaultFilesChanged généralisé).
- CP-MVP-007 S02 — LA CRÉATION DE DOSSIER (OPTION D) DE BOUT EN BOUT + LE SQUELETTE DU MENU CONTEXTUEL. `createFolder` dans project.ts (porte resolveProjectDirPath; mkdir + index.md en wx « Atomik Folder Index », titre YAML-quoté depuis le segment; adopte un dossier existant sans index, REFUSE un index existant — le contenu est sacré); canal typé atomik:create-folder + preload + test de surface. Correctif au passage: les TROIS handlers créateurs (createNote/createFolder/createProject) poussent maintenant vaultFilesChanged — avant, seuls les atterrissages main-side poussaient et les AUTRES arbres ouverts restaient périmés après une création simple. UI: popup TreeMenu artisanal (zéro dépendance) sur LES TROIS arbres (vault, projet, sources) — clic droit ou Shift+F10 sur un nœud dossier OU le fond (= racine du scope); « New note here »/« New folder… » bascule le popup en saisie de nom sur place; pré-contrôle renderer childRelPath (UN segment, sans séparateur ni point de tête) mais les validateurs main restent LA porte (13); les erreurs s'affichent DANS le popup (comportement uniforme pour les trois hôtes gratuitement); le dossier créé ouvre son index et rejoint l'état de pliage. Tests 348→357/39 (createFolder: matrice adoption/refus/traversée; childRelPath: matrice de segments). E2E PROUVÉ sur la vraie app: rung vault-write étendu → vaultWrite=ok+folder (canal → mkdir+index → readNote aller-retour). Typecheck/build/smoke verts. Prochaine étape: S03 (suppression vers la corbeille OS).
- CP-MVP-007 S03 — LA SUPPRESSION VERS LA CORBEILLE OS, DE BOUT EN BOUT. `file-manage.ts`: deleteNote/deleteFolder derrière un SEAM TrashFn (prod = shell.trashItem; une corbeille qui échoue REMONTE l'erreur, jamais de repli silencieux en suppression dure — rmSync reste aux dérivés). Règle de bundle appliquée CÔTÉ MAIN: toute note dont le dossier contient directement source.md refuse la suppression individuelle (source.md compris) — la racine de bundle part À LA CORBEILLE COMME UNE UNITÉ. Canaux delete-note/delete-folder + preload + test de surface + push. UI: Delete note…/Delete folder… dans TreeMenu (masqué sur la racine du vault); le texte de confirmation vient de folderDeleteSummary (compte de notes + ESCALADE si des bundles sources partent avec — calculé côté renderer depuis l'arbre déjà chargé, zéro canal en plus); la note ouverte se reset() quand elle part avec la cible; l'état de pliage est élagué (prunedOpenFolders). Les pilules index/log ne portent pas de delete — les fichiers de convention partent avec leur dossier. Écart consigné: « l'état manquant d'onglet » livré comme reset-dans-la-vue-initiatrice; les AUTRES panneaux gardent le message humanisé not-found existant à la prochaine lecture (point de contrôle S07, pas un abandon silencieux). Tests 357→364/40 (seam de corbeille: aller-retours, refus bundle-internal y compris source.md, rejets racine/traversée/absent, échec-de-corbeille-ne-supprime-jamais). E2E PROUVÉ SUR CETTE MACHINE WSL: vaultWrite=ok+folder+trash et le fichier retrouvé dans ~/.local/share/Trash/files/ — le risque nommé « corbeille sous WSL » se ferme avec une preuve locale. Typecheck/build/smoke verts. Prochaine étape: S04 (le verbe refactor relocateNote — rename ET move partagés).
- CP-MVP-007 S04 — LE VERBE REFACTOR: RENAME/MOVE AVEC RÉPARATION DES BACKLINKS DERRIÈRE UN APERÇU. relocatePreview/relocateApply (file-manage.ts) partagent UN computeRelocate: balayage de tous les .md scannables (symlinks ignorés), liens inline [t](x) et [t](<x>) (schémas et #-seuls ignorés, #hash préservé), résolution depuis l'emplacement de CHAQUE note, réécriture des SEULES cibles qui résolvent vers la note déplacée; les liens sortants de la note déplacée se re-pointent depuis son nouveau foyer — SAUF au rename même-dossier (base de résolution inchangée → zéro octet cosmétique, la fidélité de 27). L'aperçu est LA porte d'acceptation (fichiers + comptes, rien d'écrit); l'application = rename d'abord, puis écritures de liens, ROLLBACK des deux sur échec en cours. Gardes: fichiers de convention (index/log), fichiers de bundle, dossier-bundle comme cible, collisions, même-chemin. Canaux relocate-preview/relocate-apply + push note-relocated → relocateTabPaths dans le modèle workspace (CHAQUE panneau suit; la forme préfixe attend les déplacements de dossiers de S05); un éditeur ouvert sale bloque son propre rename; « Rename… » dans TreeMenu (prérempli). Écarts consignés: l'aperçu est une liste de confirmation (le modal de diff viendra avec l'index de liens de 20); liens référence/wikilinks non balayés (l'app n'en produit pas); réécriture treeOpen différée à S05. Tests 364→371/40 (matrice relocate incl. rollback par chmod). E2E PROUVÉ: rung → vaultWrite=…+reloc (l'aperçu compte le lien, la note citante suit sur disque). Typecheck/build/smoke verts. Prochaine étape: S05 (Move to… + déplacements de DOSSIERS).
- CP-MVP-007 S05 — MOVE TO… + LE RELOCATE DE DOSSIERS (le même refactor, à l'échelle du préfixe). relocateFolderPreview/Apply: toute cible résolvant sous le dossier déplacé suit (mapPath préfixe); les notes QUI VOYAGENT re-émettent leurs liens sortants depuis leur nouveau foyer (les liens entre voisines gardent leurs octets); rollback des deux côtés. LES RACINES DE BUNDLE SE DÉPLACENT COMME UNITÉS (la voie sanctionnée pour déplacer un bundle); un dossier À L'INTÉRIEUR d'un bundle refuse (media/ reste en place), une cible dans un bundle refuse, l'auto-imbrication refuse. UI: « Move to… » sur notes ET dossiers (destination TAPÉE, '' = racine; moveTargetRelPath valide les segments, le main revalide) — écart consigné vs le croquis « folder picker », à revisiter sur retour owner; un move CONFIRME toujours. Le push note-relocated sert aussi les dossiers: relocateTabPaths réécrit notePath ET les params de pliage treeOpen, préfixe-large, dans chaque panneau. Tests 371→378/40. E2E PROUVÉ: rung → vaultWrite=…+fmove (la note pointeuse suit un déplacement de dossier sur disque). Typecheck/build/smoke verts. Prochaine étape: S06 (drag-and-drop par-dessus le flux S05 éprouvé).
- CP-MVP-007 S06 — LE DRAG-AND-DROP, UNE LIAISON D'ENTRÉE PAR-DESSUS LE FLUX MOVE ÉPROUVÉ (jamais un raccourci autour de la porte). Notes et dossiers se DRAGGENT (payload JSON TREE_DRAG_MIME, parseTreeDrag valide); les résumés de dossiers ET le fond de l'arbre (= racine du scope) sont cibles de drop (surbrillance .drop-target); dropMoveTarget calcule la destination S05 (drop même-parent / propre-sous-arbre = no-op) et le drop exécute EXACTEMENT le flux Move — aperçu, confirmation, verbe, gardes. Le chemin clavier reste le menu contextuel. Écarts consignés: pas d'auto-expansion au survol (ouvrir le dossier d'abord, ou Move to… — à revisiter sur retour owner); pas de rung smoke DnD (les événements synthétiques ne portent pas un vrai geste — les helpers sont testés unitairement, la chaîne sous le geste EST le flux S05 prouvé, le geste lui-même est un point de bench S07). Tests 378→380/40; typecheck/build/smoke verts. LES SIX ÉTAPES IMPLÉMENTABLES DE CP-MVP-007 SONT FERMÉES EN UNE SESSION — reste S07, le bench d'acceptation owner (dossier créé au menu, rename avec backlinks et son aperçu, bundle déplacé en unité, note draggée, suppression récupérée de la corbeille).
- CP-MVP-007 S06b (rapport owner: APP BLANCHE au lancement après merge). Cause: DEUX props ajoutées aux TYPES mais oubliées de la DESTRUCTURATION — onDropNode (NoteTree, commit S06) et onMove (TreeMenu, commit S05) → ReferenceError au chargement du bundle → renderer blanc. Pourquoi c'est passé: (1) ma « vérification » d'avant-commit pipait tsc dans un grep et en AVALAIT LE CODE D'ÉCHEC — un grep vide lu comme un succès (les deux erreurs TS2304 étaient bien là); (2) la smoke monte la vue dev-docs, jamais l'arbre du vault — la vue qui plantait; (3) la suite node ne rend pas les composants. Reproduit en local avec ELECTRON_ENABLE_LOGGING=1 (Uncaught ReferenceError: onDropNode is not defined), corrigé, re-prouvé: typecheck AVEC code de sortie regardé, 380/40, build, lancement réel = zéro Uncaught. Leçon durcie: JAMAIS de gate filtré par pipe — le code de sortie EST le verdict; troisième occurrence de la même classe d'oubli (S04: onNoteMenu/onRename attrapés à temps) → le pattern « type + destructuration » se vérifie en UNE fois à chaque prop ajoutée.
- CP-MVP-007 S07a (rapport owner: FLASHS de « d'autres notes » pendant les créations note/dossier imbriquées). Cause racine: l'effet « suivre le param notePath de l'onglet » se rejouait à CHAQUE re-render (openNote change d'identité — son onNoteOpened est une arrow inline de Workspace) alors que le param traîne quelques ticks de dispatch derrière les ouvertures réelles → l'effet ROUVRAIT l'ancienne note (flash), dont le onNoteOpened réécrivait le param périmé — ping-pong visible sous la tempête de re-renders des créations S02+ (push de rafraîchissement + dispatch treeOpen + dispatch notePath). Dormant AVANT cette passe, rendu visible par elle. Fix racine: noteFollowTarget (vault/note-follow.ts, pur, testé) — on ne suit le param que sur une VRAIE transition de sa valeur, et jamais vers ce qu'on vient de demander; câblé dans VaultView ET ProjectView (même effet). Le scénario exact du flash est un test (« a STALE prop re-render never re-opens the old note »). Tests 380→383/41; typecheck (code de sortie regardé), build, lancement réel zéro Uncaught, smoke verts.
- CP-MVP-007 S07b (rapport owner: l'arbre du MODE PROJET ne se rafraîchit pas à la création de note). Cause: ProjectView n'a JAMAIS été abonné au push vaultFilesChanged — VaultView et SourcesTree le sont; les créations au menu (S02) comptent sur le push, donc l'arbre projet restait figé. Fix d'alignement: la même souscription que les deux autres hôtes (chaque atterrissage main-side rafraîchit cet arbre aussi, quel que soit le panneau initiateur). Typecheck/tests/build verts (383/41).
- CP-MVP-007 S07c (directive owner: « make the tree panel go all the way up under atomik row, and start tabs at its right »). L'arbre devient du CHROME DE PANNEAU sans déplacer les composants: .pane passe en GRILLE [colonne-arbre | (onglets / contenu)] — la colonne vient inline des params du tab ACTIF (mêmes tree/treeW que la vue, défaut 240), la tabstrip occupe (rangée 1, col 2) avec hauteur --bar-h (alignée sur le rythme des barres), .pane-content s'étend sur tout le panneau avec padding-top --bar-h, et les arbres (.vault-tree ET .devdocs-tree) se hissent dans la rangée des onglets par marge négative. Vues sans arbre → colonne zéro, onglets au bord comme avant; la vue web garde ses mesures de bounds (rien ne change dans son flux). VÉRIFIÉ PAR CAPTURE de la fenêtre elle-même (rung ATOMIK_SMOKE_SHOT): arbre sous la ligne atomik, onglet à sa droite. Bord connu consigné: panneau < ~400px sans param treeW → désalignement par le minmax par défaut des vues. Typecheck/build/smoke verts (383/41).
- CP-MVP-007 S07d (directive owner: « one tree panel per pane — lié au TYPE du panneau (vault ou project), les onglets ne sont que des vues de cet arbre; la web view est libre mais garde le panneau; changer d'onglet ne change plus l'arbre; bouton hide/show en bas à droite du panneau »). L'arbre devient de l'ÉTAT DE PANNEAU: chaque leaf porte une map `tree` validée côté main comme les params d'onglet (kind vault|project, projectPath/Title, off/w/open); migration au chargement (migratePaneTrees) — le panneau se dérive de l'onglet ACTIF et ses tree/treeW/treeOpen survivent; splitPane hérite l'arbre. Les TROIS arbres de vues (VaultView, ProjectView, SourcesTree — supprimé) se consolident dans workspace/PaneTreePanel.tsx avec TOUT le jeu de verbes S02–S06 (créations, rename/move derrière l'aperçu, corbeille, DnD sur le flux Move, recherche scopée, ＋PDF déplacé dans la barre du panneau); la barre et les champs restent fixes, seule la liste défile, le toggle hide est ÉPINGLÉ EN BAS À DROITE (directive), le show flotte en bas à gauche du contenu. Routage arbre→onglets: clic note → param notePath de l'onglet actif compatible (discipline noteFollowTarget de S07a) sinon nouvel onglet du type du panneau; source.md → onglet source; ouvrir un projet TYPE le panneau (setPaneTreeScope; bouton retour-vault dans la barre projet; projet disparu → repli sur l'arbre vault). Les gardes d'éditeur sale passent à la porte du panneau (pont PaneNoteGuard: navigation confirmée en mode manuel, rename/move/delete refusés sur la note sale — mêmes messages). Une suppression initiée du panneau FERME les onglets du panneau sous le chemin supprimé (closeTabsWithin — jamais les onglets web, aucune vue native orpheline); les autres panneaux gardent le not-found humanisé de S03. Le push note-relocated réécrit maintenant AUSSI dossierPath/projectPath (les onglets dossier ne suivaient pas un déplacement de bundle — trou S05 comblé) et le scope + pliage de l'arbre de panneau. Dev-docs garde son arbre interne (corpus de l'APP, pas le vault) sous la tabstrip; la marge négative S07c est retirée (.pane-tree = vrai enfant de grille sur les deux rangées). Écarts consignés: la vue web native peint par-dessus le toggle show (changer d'onglet pour l'atteindre); le scoping sources/ du SourcesTree retiré (l'arbre vault sert les sources). Tests 383→396/41 (matrice pane-tree: accesseurs/migration/scope/héritage-split/relocate/closeTabsWithin + validation main); typecheck (codes de sortie regardés)/build/smoke verts; e2e vaultWrite=ok+folder+trash+reloc+fmove re-prouvé; lancement réel (layout vault restauré, chemin de migration) = zéro Uncaught; CAPTURE de la fenêtre: panneau pleine hauteur, onglets à sa droite, arbre vault constant sous un onglet dev-docs, toggle en bas à droite.
- CP-MVP-007 S07e (bench owner sur S07d: « dans un panneau typé, le (+) ne doit proposer QUE des onglets du panneau; à la création d'un split, un écran New Pane => Vault / Projects / Docs; une croix ferme le panneau à droite des boutons split; "Capture" devient "Import" avec TOUTES les options d'import; plus de ＋PDF sur l'arbre; docs disparaît du chooser d'onglets — il ne s'ouvre que par New Pane; le type de panneau choisi, l'arbre reste, caché ou montré, sous tous les types d'onglets »). DEUX surfaces de choix désormais: un split frais (ou la racine après son ✕) est NON TYPÉ et présente New Pane — Vault / Projects / Docs, le choix = le type PERMANENT du panneau (Projects reste non typé jusqu'à l'ouverture d'un vrai bundle — onProjectOpened porte le projectPath); le (+) d'un panneau typé n'offre que Note / Import / Web (note = onglet du type du panneau; tabForPick porte les params projet dans le morph setTabView). LES DOCS DEVIENNENT UN TYPE DE PANNEAU: l'arbre de documentation quitte la vue DevDocs pour PaneTreePanel (branche DocsTreePanel — même chrome, corpus docs au lieu du vault; clic doc routé comme une note: onglet dev-docs actif sinon nouveau); DevDocs se réduit à UN doc rendu suivant son param docPath; layout par défaut = UN panneau vault (les docs sont à un New Pane); le hash #dev-docs construit un panneau typé docs; la migration type 'docs' les leaves à onglet dev-docs actif et laisse les panneaux VIDES non typés. ✕ FERME LE PANNEAU (closePane, à droite de ◫⬓): non-racine s'effondre dans son voisin avec ses onglets (vues web natives détruites par l'appelant); la racine se vide ET se dé-type — retour au New Pane chooser, le workspace ne disparaît jamais. « Capture » devient la PAGE IMPORT (id de vue 'capture' conservé pour les layouts sauvés): Import PDF… (le bouton ＋PDF de l'arbre retiré), la voie web (ouvre un onglet Web isolé), la session QR téléphone, l'enregistreur desktop — une surface, mêmes portes, même inbox, même confirmation explicite. Tests 396→401/41 (scope docs, closePane, params du morph, splits non typés, migration docs; les fixtures du layout par défaut ré-ancrées); typecheck/build verts; smoke vert avec vaultWrite=ok+folder+trash+reloc+fmove; lancements réels (défaut frais ET legacy migré) = zéro Uncaught; CAPTURE du panneau docs: arbre docs en chrome de panneau, ✕ dans la tabstrip, toggle en bas à droite.
- CP-MVP-007 S07e-b (bench owner: la section Web de la page Import doublonnait le choix Web du (+) — « un champ URL au lieu d'ouvrir un onglet web vide »). La carte Web page porte un CHAMP D'ADRESSE: Entrée ou Open ouvre l'onglet Web isolé DÉJÀ SUR la page tapée (l'import s'y fait, portes CP-MVP-006 inchangées). webImportUrl pur dans import/format.ts (http(s) gardé, https:// supposé pour un hôte nu, tout autre schéma REFUSÉ — file:/javascript: ne deviennent jamais une navigation), testé. Tests 401→403/41; typecheck/build/smoke verts.
- CP-MVP-007 S07e-c (bench owner: « import = coller une URL, cliquer Import, ça importe DIRECTEMENT comme source, ouvre le dossier — et le snapshot en aperçu, comme les autres sources »). Deux canaux typés. importWebUrl: le main valide l'URL (isAllowedWebUrl), la charge dans un invité CACHÉ — même partition, mêmes portes de session (UA normalisé, profil Firefox sur les hôtes d'auth, popups refusés, navigation http(s) seulement), timeout 45 s + 1,5 s de stabilisation — puis EXACTEMENT le chemin importWebSource existant (sanitizers, wx, provenance) pose le bundle; l'invité meurt dans tous les cas; toujours EXPLICITE (une URL tapée, un clic, un bundle). webViewShowSnapshot: le snapshot.mhtml du bundle se rend LÀ OÙ les autres sources montrent leur original — portes pures testées dans web-view.ts (isSnapshotRelPath: chemin relatif propre, basename snapshot.mhtml, realpath-check assertInsideVault en plus; snapshotWebPreferences: les quatre réglages requis + partition ÉPHÉMÈRE snapshot-preview — zéro cookie, rien de partagé avec la session web vive, permissions refusées, téléchargements annulés, TOUTE navigation empêchée: la preuve est statique); la vue rejoint le registre partagé (bounds/visibilité/destroy comme un onglet web). Renderer: SnapshotView.tsx reprend la discipline géométrique de WebView (rapports de rect, garde d'overlay, destroy à l'unmount); le panneau web du dossier = barre d'identité fine (hôte, Live ↗, External) au-dessus de l'aperçu qui REMPLIT; la carte Import appelle importWebUrl (normalisation webImportUrl pure). E2E PROUVÉ: rung ATOMIK_SMOKE_WEB_URL_IMPORT sur Wikipedia réel → webUrlImport=ok(sources/web/mitochondria-wikipedia/source.md), snapshot non vide sur disque; rung ATOMIK_SMOKE_SNAPSHOT sur fixture → snapshotPreview=ok(bar=en.wikipedia.org, dossier=554chars, host=358x697) — capturePage ne voit pas les vues natives, les PIXELS de l'aperçu restent un point de bench owner. Tests 403→405/41 (matrice isSnapshotRelPath + prefs snapshot); typecheck/build/smoke verts; surface preload étendue de deux méthodes documentées (test de surface vert).
- CP-MVP-007 S07e-d (rapport owner + capture: les boutons de ROTATION flottaient PAR-DESSUS l'aperçu snapshot d'un dossier web). La rotation est une correction de PHOTO — la condition excluait audio et PDF mais jamais le web (bug préexistant, rendu visible par l'aperçu S07e-c). Fix une ligne: !isWeb rejoint la garde des outils de rotation. Typecheck/tests (405/41)/build verts — gates re-exécutés APRÈS un premier commit parti sans eux (chaîne && cassée par un heredoc sous le mauvais cwd; le commit est amendé avec cette entrée).
- CP-MVP-007 S07e-e (rapport owner: « delete-note: bundle file — delete the bundle folder instead » en supprimant une note GÉNÉRÉE PAR L'IA dans sources/web/<slug>/). La garde S03 protégeait TOUT le dossier de bundle; or la règle protège le CONTRAT du bundle, pas son emplacement. La garde se rescope sur les FICHIERS DE CONTRAT (source.md, index.md, transcript.md, extracted.md, reader.md — chacun a son verbe de cycle de vie): une note ordinaire qui VIT dans un dossier de bundle se supprime, se renomme et se déplace comme n'importe quelle note (delete ET relocate — la même sur-portée bloquait les deux). Les NOMS de contrat restent réservés comme cibles dans un bundle (renommer une note en reader.md masquerait les verbes du dérivé). Changement de comportement assumé: déplacer une note VERS un dossier de bundle est maintenant permis (cohérent — « New note here » y crée déjà des notes). Tests 405→407/41 (note ordinaire supprimée/déplacée dans-hors bundle; contrats toujours refusés; nom de contrat refusé comme cible); typecheck/build/smoke verts (vaultWrite=ok+folder+trash+reloc+fmove).

## 2026-07-16

- Décision promue en fichiers (unité docs-seule, règle de migration de contexte du doc 17): « One surface, two layers » — une seule surface d'édition, deux genres d'entités (entités LIÉES adossées au Scene IR, forme canonique = les lignes DSL du bloc fenced, éditées par opérations sémantiques + pins de projection réécrites en patches de lignes; ENCRE LIBRE sans sémantique, forme canonique = fichier de dessin sidecar, sans claim ni obligation Truth-Lens), reliées par un gradient de promotion (encre → énoncés, en proposition de patch) et un chemin de détachement (scène → copie encre, lossy par design). Le DSL est re-défini comme la SÉRIALISATION de la couche liée, atteinte par trois chemins convergents (taper, générer, dessiner-puis-promouvoir). [ADR-010](../docs/adr/ADR-010-one-surface-two-layers.md) posé Status: accepted (décision owner); amendements D/E/F du pack ([sessions/2026-07-16-one-surface-two-layers/](./sessions/2026-07-16-one-surface-two-layers/one_surface_two_layers_amendment_pack.md)) appliqués verbatim: doc 19 gagne « One surface, two layers (reserved) », doc 21 gagne l'invariant encre-libre + « Free-ink layer (reserved) ». Non touchés, par contrainte: 18 (roadmap, owner), `atomik_dsl_reserved_spec_v0_1.json` (sync possédée par CP-DSL-004 S06), tout code, le chemin actif CP-MVP-007. Les amendements A/B/C visent le repo atomik-dsl (hors de cette unité).

## 2026-07-20

- CP-MVP-008 PROPOSÉ (unité docs-seule): génération de connaissance RÉELLE + passe d'interaction AI. Question owner: « je ne lis nulle part quand on implémente la génération LLM — cloud ou local ? » — TROU CONFIRMÉ par survey: la boucle ask-AI tourne toujours sur le mock déterministe de CP-MVP-001 S08 (ai-mock.ts, branché dans index.ts), aucune milestone M6–M13 ne possède le swap (M7 = provider de VÉRIFICATION, M9 = autocomplete). Trois décisions owner: (1) séquençage — le chemin s'ouvre JUSTE APRÈS la clôture de CP-MVP-007, avant M6 (motif M2-completion, comme M3 avec 004/005; challenge/repair M6 n'a de sens que sur de la vraie sortie); (2) cloud d'abord, seat local llama.cpp EN RÉSERVE sous les portes d'évaluation de 33 (preuves datées dans 34, précédent speech); (3) Mistral Small premier adaptateur (seam de clé ai-settings.ts + patron mistral-ocr-adapter.ts déjà dans l'arbre depuis CP-MVP-005; labo français, résidence EU; prix datés dans docs/research/model-research.md). Trois directives UX owner même session (« ai generation »): dossier prompts/ à la racine du vault (prompts système/message = fichiers vault ordinaires, zéro nouvel IPC), déclenchement AI par MENU CONTEXTUEL sur la sélection (clic droit → actions rapides / saisie custom au point de clic + pills de prompts système / ouvrir un chat — le bouton AI QUITTE la note-bar; clarification owner: rien à voir avec le menu citations `@` de quick-actions.ts), et aperçu INLINE dans la note pour les requêtes rapides (widget CM, le buffer ne change qu'à l'accept — discipline 06 inchangée) + panneau de CHAT latéral droit (colonne de chrome de panneau, motif pane-tree; l'AiPanel docké se retire). Posé: coding-paths/CP-MVP-008.md (status: draft, base_commit non épinglé), ligne de registre M2 (completion) + note de réserve seat local, sessions/2026-07-20-generation-swap-decisions.md (survey + décisions + directives verbatim + pins ouverts pour S01). Activation DOUBLEMENT gardée: acceptation owner ET clôture de CP-MVP-007 S07 (règle un-seul-chemin-parent-actif, 35). Non touchés: 18 (roadmap, owner — le registre porte le séquençage, précédent 004/005), ACTIVE.md, tout code.
- CP-MVP-007 S07f (rapport owner: « maj lock ne s'enregistre pas dans l'app, les actions ctrl+ non plus, genre copier/coller »). CAUSE RACINE HORS APP, mesurée sur la machine avec une instance ISOLÉE (vault/état/XDG scratch — l'instance de l'owner intouchée), injection Windows-side (méthode wslg#1015) et lectures d'état X via ctypes: le pont d'état verrous de WSLg est DÉFECTUEUX — la bascule caps Windows→X passe dans UN SEUL SENS (ON traverse, OFF jamais), le focus-in RDP IMPOSE un état de base inversé (« abc » tapé caps OFF arrive « ABC », la bascule caps elle-même traverse et inverse correctement RELATIVEMENT à cette base fausse), et le sync écrase même les bascules XTEST côté X (deux écrivains sur un bit, le cache client msrdc gagne — rdp.c xf_input_synchronize_event, storms via wslg#894). Les CHORDS CTRL FONCTIONNENT dans une instance fraîche (Ctrl+A/C/V injectés → buffer dupliqué sur disque, zéro lettre fuitée) — le symptôme copier/coller de l'owner relève du même combat d'état (ou du canal clipboard wslg#15), pas de touches mortes. AUCUN changement de code: l'app est victime; compensation in-app rejetée (deviner l'état réel = course contre le même sync). Preuves + récupération owner (wsl --shutdown = vrai reset; un appui caps DANS l'app réaligne la casse relative) dans sessions/2026-07-20-wslg-capslock-probe.md. ÉTAT MACHINE à la clôture: parité DÉSACCORDÉE (Win OFF / X ON) — la casse tape inversée dans toute fenêtre WSLg jusqu'au reset.
- CP-MVP-007 S07g (directive owner: « placer dynamiquement le contenu de la note au centre horizontalement »). margin-inline:auto sur les DEUX consommateurs de --note-column (.markdown-body et .cm-content du mode live) — les marges auto absorbent l'espace libre du flex scroller, read ↔ live gardent une géométrie identique PAR CONSTRUCTION (le commentaire de 1347 reste vrai), les panneaux étroits remplissent comme avant (marges auto → 0 au max-width). Le mode source garde sa mise en page IDE (gouttières, aligné à gauche) — déviation enregistrée, à revoir au bench. VÉRIFIÉ PAR CAPTURE sur le vrai layout: colonne centrée dans un panneau de 963px, marges équilibrées (~113px de chaque côté). Typecheck/tests/build/smoke verts.
- CP-MVP-007 S07h (directive owner + clarification même session: « ouvrir le menu quick actions à d'autres actions — il n'est aujourd'hui que pour les sources »; format de ligne « pill note/source d'abord, puis titre du doc + libellé d'action »; « le libellé d'action ne change pas de couleur en surbrillance bleue, à peine lisible »; « notes ordonnées en hiérarchie d'arbre INVERSE, du dossier courant jusqu'au vault »). Le menu @ gagne le fournisseur NOTES (réservé depuis l'en-tête du module): linkableNotesOf — tout .md sauf la note éditée et les FICHIERS DE CONTRAT de bundle (règle S07e-e: source/index/extracted/transcript/reader.md — ils ont déjà leurs entrées de bundle; une note ordinaire VIVANT dans un bundle reste liable), tri par PROFONDEUR DE PRÉFIXE PARTAGÉ décroissante (dossier courant → parents → racine; à égalité: moins profond d'abord, puis nom); noteLinkEntry insère [titre](<chemin relatif>) en chevrons comme toute insertion. Format de ligne: type = kind (pill rendue par le slot d'icône de completion, icons:true + CSS ::after 'note'/'source'), displayLabel = TITRE seul, detail = libellé d'action; correctif contraste: le detail hérite la couleur de la ligne sélectionnée (opacity .8) au lieu du muted illisible. Déviation enregistrée: le rendu des pills et le feel du menu = bench owner (la suite node couvre ordre/format/exclusions, pas le geste). Tests 407→411/41; typecheck/build/smoke verts (ATOMIK_SMOKE_OK).
- CP-MVP-007 S07h-b (capture owner sur le vault réel: la pill peignait PAR-DESSUS les premières lettres du titre (« source » masquait « cur » de curlew-sandpiper-wikipedia), et la pill de la ligne sélectionnée était illisible). Deux corrections de spécificité/couleur: (1) le sélecteur des pills monte sur .cm-tooltip.cm-tooltip-autocomplete pour BATTRE la feuille injectée de CodeMirror (`.ͼ… .cm-completionIcon { width: .8em }` gagnait sur la classe nue — largeur fixe, la pill débordait sur le label); (2) la pill sélectionnée garde un texte SOMBRE (var(--fg)) sur son chip clair — color:inherit prenait la couleur claire de la ligne en surbrillance et disparaissait dans le chip. Constat au passage: le detail hérité (correctif S07h) est LISIBLE sur la capture — la moitié contraste du rapport est confirmée au bench. CSS seul; tests 411/41, typecheck, build+smoke verts (le HMR du dev server livre le fix sans relance).

## 2026-07-21

- COUCHE LEARNING EN PANNE + PORTE D'ALIGNEMENT VISION (unité docs-seule, deux directives owner). CONSTAT owner: docs/learning/ s'arrête à la note 11 (CP-MVP-002 S02; dernier commit learning le 2026-07-07) alors que CINQ chemins ont livré depuis. DIAGNOSTIC (git log --follow sur docs/learning + grep des DoD): la clause « learning notes » du definition-of-done a tenu en 001/002/004 mais est TOMBÉE du boilerplate à partir de 003/005/006/007/008 (les chemins se sèment du précédent; le template de 24 n'avait AUCUNE ligne pré-remplie) — et même 004, qui portait la clause, n'a rien produit: elle ne vivait pas dans la boucle par-étape que les agents exécutent (l'étape 9 de 22 disait « module notes / affected docs » sans nommer la couche learning; le Required de 008 annotait 17 « decision recording »). Personne n'a DÉCIDÉ d'arrêter — la ligne a glissé. CORRECTIFS: l'étape 9 du protocole 22 nomme désormais la note learning (règle first-use de 17), le template de chemin de 24 embarque la ligne pré-remplie, le DoD de CP-MVP-008 (draft) la retrouve, et docs/learning/index.md porte la DETTE visible — notes 12–15 dues (PDF/CP-003; seats speech+OCR/CP-004+005; web source/CP-006; file management/CP-007), backfill en unités docs-seules, séquencement posé à l'owner (Q6 de la revue). NOUVELLE PORTE (directive owner du jour): revue d'alignement vision AVANT tout nouveau chemin actif — parcourir les features principales attendues (milestones de 18 + registre) contre la vision COURANTE de l'owner, consigner en note de session; accepter un chemin sans revue enregistrée est INVALIDE. Codifiée: 22 §If no active path exists (étapes 1–3) + règle d'ouverture du registre. PREMIÈRE INSTANCE posée: sessions/2026-07-21-vision-alignment-before-cp-mvp-008.md (Part 1: M0–M5 + passes de friction livrés; Part 2: résumé de 008 proposé; Part 3: file M6–M13 + événements de vision du 07-16 + dette learning; SIX questions owner ouvertes — la revue ferme sur ses réponses). 18 (roadmap) NON touché — amendements owner-gated, la revue propose, n'applique jamais. L'activation de 008 est désormais TRIPLEMENT gardée (acceptation owner + clôture 007 S07 + revue close) — registre et checkpoint/blockers de 008 mis à jour. CP-MVP-007 reste le chemin actif (bench S07 owner inchangé). Aucun code touché.
- DETTE LEARNING REMBOURSÉE (unité docs-seule; directive owner = réponse à la Q6 de la revue: « populate the docs »). QUATRE notes écrites en parallèle par quatre agents, chacun REJOUANT le processus de développement de son chemin (session de décision datée → fichier de chemin → entrées log FR → code réel + tests) avant d'écrire, puis SONDAGE des ancres par l'orchestrateur — chaque nom cité retrouvé dans la source. 12-pdf-source-and-anchors.md (CP-003: décision pdfjs-dist 6.1.200 datée avec rejets; DEUX builds du même moteur dans DEUX process — fidélité rendue ≠ fidélité extraite; TEXTLESS_THRESHOLD=20 + rasterizer pdftoppm vers le siège OCR; ancres = lignes de tableau durables + registre pending-page pour le retour de citation). 13-local-speech-and-ocr-seats.md (CP-004/005: whisper.cpp v1.8.6 et llama-mtmd-cli en sidecars bornés SIGKILL/32 Mo/LD_LIBRARY_PATH; seam TranscriptionAdapter + routeByMedia; tiers mesurés à démotion collante; rung cloud épinglé mistral-ocr-4-0 / voxtral-mini-2602 sous AbortController 180 s; clé 0600 masquée côté main; leçons: -l auto, EXIF/652 s, Parakeet effondré au mémo réel). 14-web-source-tab-isolation-and-snapshots.md (CP-006 + vague S07e: invité WebContentsView SANS preload; partitions persist:web-sources et snapshot-preview éphémère; permissions deny-by-default {fullscreen, clipboard-sanitized-write}; MHTML = preuve hachée; extraction structure-first readability/linkedom/turndown en utilityProcess; frontière vérité/provider). 15-file-management-as-refactor.md (CP-007: seam TrashFn — un trash qui échoue REMONTE, jamais de repli rmSync, test cité; computeRelocate privé partagé preview/apply + rollback; garde re-scopée sur les FICHIERS DE CONTRAT; DnD = liaison d'entrée sur le flux Move; arbres = chrome de panneau migré; leçons S06b gate pipé / S07a stale-prop / S07e-e sur-garde). Chaque note: les 9 sections du template 24, 220 lignes, backfill daté honnêtement sous le H1. index.md: notes 12–15 promues dans la liste, §Coverage debt → §Coverage stall (repaid) avec la leçon durable (une obligation récurrente vit dans le protocole par-étape, pas dans une ligne de checklist que l'essaimage des chemins peut perdre). Revue d'alignement: Q6 consignée RÉPONDUE (backfill maintenant, une unité — exécutée le jour même), Q1–Q5 restent ouvertes — l'acceptation de 008 attend toujours (007 S07 + revue close + acceptation owner). Aucun code touché.
- CP-MVP-007 S07i (directive owner au bench: « improve the tab design — professional/modern, continuité de forme/lignes/couleurs, actif = l'onglet AFFICHÉ du panneau, pas le dernier cliqué »). Cause: le traitement surface-fusion était réservé au panneau FOCALISÉ (.pane.focused > .tabstrip .tab.active) — l'onglet affiché des autres panneaux restait une pilule chrome — et le border-bottom de la strip passait SOUS l'onglet actif, le coupant de son contenu. Refonte CSS pure: le séparateur devient une COUCHE ::after (le même 1px dans la même hauteur --bar-h) que l'onglet actif recouvre et INTERROMPT; l'onglet affiché de CHAQUE panneau est un onglet connecté (fond --surface, coins hauts arrondis, liseré 1px ouvert vers le bas — il pousse du contenu); le focus garde SON signal (la ligne accent en tête, inchangée); les onglets inactifs deviennent des segments pleine hauteur discrets (survol = pilule code-bg); ＋ recentré; le commentaire de doctrine two-surface (:root) mis à jour. Typecheck/tests (411/41)/build/smoke verts, codes de sortie regardés; CAPTURE de la vraie fenêtre: onglet fusionné au contenu, ligne interrompue dessous puis reprise à sa droite. Le cas SPLIT réel (panneau non focalisé) rejoint les points de bench S07.
- CP-MVP-007 S07j (rapport owner au bench: « le rendu est laggy, pas snappy comme Obsidian — déplacement de fenêtre, resize de panneaux, tout le visuel; moniteur 240 Hz »). ATTRIBUTION contre l'audit perf du 07-15 (addendum daté écrit dans la session): le déplacement de fenêtre et le feel global sur CETTE machine = la taxe WSLg MESURÉE (GL 100% logiciel présenté via RDP — Obsidian compare depuis Windows natif avec compositing matériel; aucun code ici ne voit jamais 240 Hz sous WSLg; le test honnête du « snappy » = un build natif packagé, rien à truquer sous WSLg). Le resize de panneaux avait SA moitié app (RS2 de l'audit): chaque pointermove dispatchait un re-render COMPLET du workspace — sous raster logiciel les événements dépassent les paints et chaque événement en file devenait un render. Correctif = la moitié drag de l'action 6: frameCoalesced (renderer/src/workspace/frame-coalesce.ts, ordonnanceur en SEAM, 5 tests) — le drag du divider mesure ET dispatche une fois par frame peinte (le getBoundingClientRect quitte aussi le par-événement), le drag de largeur d'arbre pareil; le report de bounds WebView dédupliquait déjà et suit le rythme des renders. Leviers différés consignés: restructure CSS-var commit-au-relâchement, mémoïsation RS1, caches RS3. Tests 411→416/42; typecheck/build/smoke verts, codes de sortie regardés.
- CP-MVP-007 S07k (deux décisions owner au bench, questionnaire: « where do you get that option D assumptions? I would prefer full conventions » + « I just want the index and log files to be automatically updated, either deterministically after manual file mgmt or later by agent if they do file mgmt »). L'option D (07-16) est RÉVISÉE en CONVENTIONS PLEINES — révision consignée dans l'addendum de décisions 07-16, doctrine posée en 04 §Vault and folder conventions. `folder-index.ts` (nouveau): chaque dossier porte index.md ET log.md (createFolder naît avec les deux; un dossier existant les ADOPTE à sa première opération); la RACINE du vault est semée UNE fois, au moment explicite du dialogue open-vault (adoptVaultRoot — la restauration au lancement n'écrit JAMAIS; ré-adopter un vault déjà semé n'écrit rien). L'index porte UN bloc Contents géré entre marqueurs <!-- atomik:contents --> (dossiers d'abord, bundles en UNE ligne d'unité vers source.md, puis notes; le texte de l'owner hors marqueurs n'est JAMAIS touché; un index sans marqueurs adopte le bloc à la prochaine opération; octets inchangés = zéro écriture, 27). Le log gagne une ligne datée par verbe — les DEUX parents sur un move inter-dossiers (départ + arrivée). Le sync vit DANS les verbes main-side (createNote — y compris la CHAÎNE de dossiers matérialisés par un chemin imbriqué, du plus profond au plus haut pour que les liens parents résolvent; createFolder/createProject — l'ensure n'enregistre que le NEUF; deletes/relocates — APRÈS succès seulement, un rollback ne laisse aucune comptabilité): tout appelant (menu, DnD, file management AI futur) produit les mêmes écritures; les atterrissages d'import gardent leurs conventions de dossier source. Le scanner relocate traite index/log comme des notes scannables — l'aperçu d'un rename compte AUSSI le lien du Contents parent (le rung smoke asserte totalLinks=2 et l'index re-dérivé liste le nouveau nom). Tests 416→432/43 (folder-index.test.ts: matrice bloc/entrées pure, idempotence d'adoption, zéro-écriture-cosmétique, aller-retours de verbes dont la chaîne imbriquée; leçon au passage: « # My folder » contient « old » — les sentinelles de test se choisissent); typecheck/build verts; E2E PROUVÉ sur vault scratch: vaultWrite=ok+folder+trash+reloc+fmove avec les conventions VIVANTES (Contents suit créations/renames/moves de bout en bout, log chronologique complet, corbeille OS réelle). Docs même unité: 04, note module, learning note 15 (règle first-use — la forme enseignée a changé le jour même), addendum. Point de bench: la racine du VRAI vault se sème au prochain dialogue open-vault explicite.
- CP-MVP-007 S07l (capture owner sur S07i: « more curve on the attachment of the tab on the page » + « the header of the page is a different color »). ÉPAULES COURBES sur l'onglet actif (pseudo-carrés hors onglet en radial-gradient: la strip au travers de l'arc, l'anneau de bordure, le remplissage surface; rayon 7→9px; le scroller gagne un padding d'épaule pour que le PREMIER onglet ne se fasse jamais rogner) et les barres d'en-tête de la COLONNE CONTENU passent en --surface (.note-bar, .web-nav — l'onglet fusionné atterrit sur SA couleur; commentaire de doctrine two-surface mis à jour). Parité même-rôle entamée sur l'exemple nommé par l'owner: .web-nav adopte --bar-h et le rythme du note-bar (« note header thinner than webpage header » — confirmé au code: web-nav était content-driven et plus haut; input compacté au gabarit). Typecheck/tests/build/smoke verts; CAPTURE: les épaules s'arquent dans la surface, la bordure suit la courbe. EN PARALLÈLE: audit UI PROFOND lancé (deux inspecteurs: styles.css — échelles/alignements/parité de barres/discipline couleurs/z-index/duplication; markup — texte-qui-devrait-être-icône/a11y/divergences même-rôle/styles inline) pour la vague de correctifs S07m puis le SYSTÈME DE DESIGN S07n; goûts owner consignés par questionnaire: identité macOS-glass (contenu opaque, chrome+overlays légèrement translucides), LES QUATRE directions de palette organic-future en famille de thèmes (« can we have all? »), densité balanced.
- CP-MVP-007 S07m (les deux audits rendus — ~175 findings, rapports en scratchpad — puis LA VAGUE DE CORRECTITUDE/PARITÉ exécutée). Icônes: 17 SVG ajoutées à icons.tsx (grille maison 16/1.3, aria-hidden) et CHAQUE glyphe de chrome remplacé (× ✕ + ◫ ⬓ ‹ › ⟲ ⟳ ⚓ ● ■ ↗ « Aa ») avec aria-labels; <HistoryNav> partagé remplace TROIS paires ‹ › identiques dupliquées. LE bug de scoping corrigé: .note-bar-button n'était stylé que sous .note-bar-actions — 18 boutons (web/pdf/nav/source) rendaient en boutons UA bruts, LA cause du feel « pas pro » des barres. L'indentation VESTIGIALE de 2.6rem (.no-tree .note-bar) SUPPRIMÉE — chaque barre de note était décalée vs la barre web depuis S07d (elle compensait un toggle mort). ~120 lignes de CSS mort supprimées (.theme-picker, .ai-settings*, .tree-show; une accolade orpheline attrapée à l'équilibre 360/360). Parité de barres FINIE: .pdf-nav dé-centré sur --bar-h/surface/gouttière, .ai-panel-bar et .editor-conflict sur --bar-h, .web-source-bar en surface. Tokens sémantiques (--error/-bg, --warn-fg/bg/border, --ok-*, --info-*) remplacent 20+ littéraux dérivés — dont le #c0392b PLAT illisible en sombre. Micro-passe d'alignement: rangées baseline→center, paddings symétrisés, lignes dossier/note de l'arbre sur UN bord gauche, ＋ sur la ligne de texte des onglets, input code-bg-sur-code-bg corrigé. Labels français traduits; 12 inputs sans nom accessible nommés; les tooltips d'onglets portent notePath/dossierPath; role=meter sur le niveau micro. Tests 432/43, typecheck/build/smoke verts, CAPTURE vérifiée. NOURRIT S07n (consigné au ledger): architecture de tokens (échelles fs/espace/rayons/hauteur de contrôle/gouttière, étages z), classes partagées .pill/.btn/.input/.popover, composants NoteBar/InlineCreateForm/ConfirmDialog, parité nav de la barre éditeur, enregistrement TreeMenu à la garde web-overlay, les QUATRE thèmes organic-future + identité macOS-glass + doc bedrock du système câblé dans AGENTS.
- CP-MVP-007 S07n — LE SYSTÈME DE DESIGN (directive owner: « design a very modern ui design system, that we will be referencing for the rest of the project as guidelines automatically followed »). BEDROCK 36 posé (36_36-ui-design-system.md): identité issue de l'interview de goûts (macOS-glass — contenu opaque, chrome+overlays translucides; zen/minimaliste; organic-future; densité balanced), vocabulaire de tokens, règles par composant (barres/contrôles/boutons-icônes/pilules/onglets/inputs/popovers/arbres), BUDGET de verre (chrome debout = teinte alpha SEULE, jamais de backdrop-filter — la taxe WSLg mesurée; overlays = vrai givre blur(14px) petite surface; repli opaque via @supports et prefers-reduced-transparency; planchers d'opacité ≥86% sous du texte), planchers a11y (noms accessibles, contrastes 4.5:1/3:1, Échap/Entrée/focus accent), et LE CONTRAT AUTOMATIQUE: AGENTS.md gagne la ligne « UI work obeys 36 » (parallèle à la ligne 13-sécurité) — chaque session future la voit avant de toucher au renderer. IMPLÉMENTATION VIVANTE dans styles.css: échelles en :root (--fs-xs/sm/md/lg, --space-1..5, --radius-sm/md/lg/pill, --tab-r, --control-h, --gutter, étages --z-bar/float/pop/menu, --shadow-pop, --glass-chrome/-pop en color-mix) et APPLIQUÉES (les cinq barres sur space-1/gutter; les TROIS genres de contrôles en-barre dans UNE boîte --control-h — la rangée aux trois hauteurs du rapport d'audit fermée par construction; géométrie d'onglet sur --tab-r; sites z sur les étages); les QUATRE thèmes organic-future (sage-stone, eucalyptus clairs; moss, biolum sombres — échelles de surfaces vérifiées dans les deux sens) rejoignent THEMES (validation modèle + picker gratuits), pastels pré-36 gardés en LEGACY jusqu'à un élagage owner au bench; le VERRE en place: lavis d'accent 6% sur body, teintes alpha du chrome debout (header/tabstrip/pane-tree), givre réel sur les overlays (app menu, tree menu). Note learning 16 écrite LE JOUR MÊME (règle first-use restaurée le matin — tokens/thèmes/verre = patron nouvellement mobilisé) + index; la couverture de CP-MVP-008 gagne 36 en Required (complétude 00–36 — ses surfaces AI naîtront conformes). Tests 432/43, typecheck/build/smoke verts (le corpus dev-docs passe AUTO à 93 fichiers = 36 servi dans l'app), CAPTURE vérifiée. Restent au feed de consolidation (ledger): classes partagées, extractions NoteBar/InlineCreateForm/ConfirmDialog, parité nav éditeur, garde overlay TreeMenu, migration littéraux→tokens complète.
- CP-MVP-007 S07o (bench owner sur S07n: « there no translucid effect nowhere, most of the buttons are still text (I need icons with label in hover), most of the icon/button are still not centered and aligned »). LES TROIS verdicts confirmés au code et corrigés. (1) VERRE rendu VISIBLE: teintes approfondies 88→72% (chrome) / 82→66% (popovers), blur 14→18px, lavis du body DOUBLÉ (deux taches d'accent 14%/9%), liseré 1px --glass-edge (inset highlight) sur header/arbre/popovers — la cause racine: 88% d'alpha sur un lavis de 6% ≈ imperceptible, et givrer un popover posé sur une surface de MÊME couleur ne montre rien; 36 §glass révisé (valeurs + règle de re-vérification de contraste à chaque baisse de teinte). (2) ICON-FIRST imposé partout (36 §buttons réécrit: le verbe est une icône, le LABEL vit dans le title au survol + aria-label; le texte ne survit que dans les menus/listes et les rares composés icône+état): 13 icônes de verbes de contenu ajoutées (Sparkle/Save/Autosave/Book/Pen/Code/Trash/ScanText/Mic/Cloud/Image/Import/Play); convertis: AI, auto/manual, Save, View original, le segmented read/live/source (ModeSwitch icônes + aria-pressed), TOUS les verbes de dossier source (extract reader/text, deletes, transcribe local, cloud OCR/transcribe, view scan/original — les libellés busy morphants disparaissent, l'état disabled signale), Import du web (icône+texte court), Run/Accept/Reject du panneau AI. (3) CENTRAGE: cause racine trouvée — plusieurs familles de boutons accueillant les nouveaux SVG n'étaient PAS des boîtes flex, l'icône chevauchait la baseline du texte; correction STRUCTURELLE: `button svg { display: block }` global + UNE règle flex centrée couvrant toutes les familles de boutons du chrome (tabstrip, tree-bar, vault-new, tree-menu-form, window-controls, app-menu, source-tools, ai-actions, conflict). Tests 432/43, typecheck/build/smoke verts; CAPTURE: le lavis d'accent traverse visiblement le verre de l'arbre. Le feel final (survols, tooltips, thèmes) = bench owner.
- CP-MVP-007 S07p (capture owner, thème sombre: « the curve of the tab is offset; a non-displayed hovered tab should be pill and not fully connected »). DEUX corrections. (1) Bug de GÉOMÉTRIE de l'épaule: l'anneau du radial-gradient était tracé au rayon ~8 dans une boîte d'épaule de 9px — l'arc finissait 1px avant le flanc de l'onglet ET avant la ligne (marche visible, pire au scaling d'affichage); l'anneau court maintenant de (tab-r − 1px) à tab-r EXACTEMENT — les extrémités de l'arc atterrissent SUR la bordure du flanc et SUR la ligne. (2) DEUX formes d'onglet désormais: les onglets inactifs sont des PILULES flottantes (rayon pill, décollées de 5px au-dessus de la ligne, survol = pilule code-bg) — seul l'onglet ACTIF descend sur la ligne en forme connectée à épaules; un survol d'inactif ne peut plus se lire comme connecté. Tests 432/43, typecheck/build/smoke verts, CAPTURE: épaules affleurantes.
- CP-MVP-007 S07q (rapport owner: « weird white band around the app window » + « classic old timey rectangular look with no box shadow and outline like Obsidian »). DEUX causes SÉPARÉES. La bande blanche = À NOUS: le backgroundColor natif de la BrowserWindow restait au BLANC par défaut d'Electron et encadrait l'app sombre aux bords (avant le premier paint + coutures de DPI fractionnaire sous WSLg); correctif: windowBackgroundFor(state, prefersDark) dans workspace-state.ts — la carte thème→--bg hex, couplée À LA MAIN aux valeurs de styles.css (commenté des deux côtés: un thème ajouté là-bas atterrit ici dans la même unité), buildMainWindowOptions porte la couleur (les DEUX sites de création), html gagne le plancher var(--bg) côté web. Tests 432→435/43 (carte des thèmes, repli system/inconnu, options avec/sans). Bord connu consigné: un changement de thème en cours de session recolore la bande au PROCHAIN lancement. Le look rectangulaire sans ombre = HORS-APP: la fenêtre X11 de WSLg passe par RDP et ne reçoit NI l'arrondi DWM de Windows 11 NI son ombre — Obsidian compare depuis Windows NATIF; le build natif packagé recevra coins arrondis + ombre GRATUITEMENT (même recommandation que le test 240 Hz); l'alternative fenêtre-transparente + arrondi CSS sous WSLg REJETÉE (coût GL logiciel + ARGB fragile). Typecheck/build/smoke verts.
- Décision owner (séquencement): le BUILD NATIF WINDOWS PACKAGÉ est RÉSERVÉ pour APRÈS le prochain chemin (CP-MVP-008) — « after the next coding path I think ». Consigné au registre en follow-up réservé: une unité bornée (config electron-builder, cible Windows, chemin de lancement) qui ferme d'un coup les TROIS limites WSLg nommées: présentation 240 Hz réelle (attribution S07j), coins arrondis + ombre DWM (S07q), profondeur de verre composée matériellement (36 §glass). D'ici là, l'app dev sous WSLg reste le quotidien et ces trois limites sont connues, consignées, et ne sont pas des défauts de l'app.
- CP-MVP-007 FERMÉ (owner: « I validate bench », 2026-07-21). Balayage machine dans sessions/2026-07-21-cp-mvp-007-acceptance.md: les quatre intentions 18 §M1 ✓ (la connaissance survit au restart; l'état workspace jetable — les migrations S07d/e DÉRIVENT; ouvrir n'écrit JAMAIS — le sync S07k ne vit que dans les verbes explicites, la racine ne se sème qu'au dialogue open-vault; une édition = un diff relisible — l'aperçu relocate compte TOUT ce qu'il touchera, bookkeeping compris), les quatre décisions de scope ✓ (D révisée en conventions PLEINES; corbeille OS prouvée; rename = refactor 27 sous aperçu; move + DnD sur le flux gardé). QUINZE sous-étapes de bench S07a–S07q validées en cumulé, état final 435/43 + rung e2e complet. Écarts honnêtes PORTÉS par écrit (destination tapée vs picker, pas de hover-expand DnD, modal de diff en attente de l'index de liens de 20, dividers pointer-only, bande native recolorée au prochain lancement sur changement de thème, pastels legacy à élaguer, garde overlay TreeMenu, feed de consolidation 36 au ledger; limites WSLg réservées au build packagé post-008). Statut done + registre + ACTIVE.md basculés: AUCUN chemin actif — l'activation de CP-MVP-008 attend la clôture de la revue d'alignement (Q1–Q5) et l'acceptation owner.
- LES DEUX CÉRÉMONIES + REVUE D'ALIGNEMENT CLOSE (clarification owner: « what I meant was a quick interaction with me on the features inside the coding path... a closing active path ceremony to manage roadmap backlog... followed by prompted exchange »). Le processus est REFONDU en deux cérémonies interactives (22 §Between paths réécrit; règle d'ouverture du registre alignée): CÉRÉMONIE DE CLÔTURE à la fermeture d'un chemin (rappel compact DÉRIVÉ des métadonnées du repo — registre/ledgers/acceptations/log, jamais de la mémoire de conversation — backlog, prochaines étapes, points à challenger, puis échange par questions à options) et CHECK D'OUVERTURE à l'activation (confirmation par feature DANS le chemin, les deltas amendent AVANT l'épinglage du base commit; l'acceptation owner explicite reste requise). Idée « ceremony tab » in-app consignée en brainstorm (2026-07-21 — projection 35 sur les TRANSITIONS du plan d'exécution). LES DEUX CÉRÉMONIES EXÉCUTÉES SÉANCE TENANTE: clôture de 007 — réponses owner promues dans la revue: post-008 = candidat WIKIDATA EN COLONNE VERTÉBRALE de la génération via RAG LOCAL (tranche M8 lexical-first 33 + tranche M10 Wikidata; Truth Lens M6 ensuite SUR sortie ancrée; à confirmer à la cérémonie de clôture de 008 — consigné au registre), M7 décidé à la clôture de M6, 18 inchangé (ADR + registre), Git status/diff view TIRÉ du deferred vers le backlog proche; check d'ouverture de 008 — QUATRE features confirmées (Mistral Small cloud-first + mock en repli; entrée = menu contextuel de sélection, le bouton AI quitte la note-bar; aperçu INLINE; chat latéral droit) avec UNE révision: LES CHATS PERSISTENT EN FICHIERS VAULT (remplace le défaut éphémère — DoD et pins S01 de 008 amendés: convention chats/ à épingler au S01). REVUE CLOSE — l'activation de CP-MVP-008 n'attend plus que le mot d'acceptation owner.
- CP-MVP-008 ACCEPTÉ et ACTIVÉ (owner: « ok lets go », 2026-07-21). Base commit épinglé 6cacfa2; 435/43 verts à l'activation; ACTIVE.md + registre basculés (un seul chemin parent actif — 007 fermé le même jour). Les deux cérémonies du gap sont consignées (clôture de 007 + check d'ouverture avec l'amendement chats-en-fichiers). Prochaine étape: S01 bootstrap — lectures pleines 06/13 §IPC/14 §ai-core/33/28/05/26, épinglage du contrat d'adaptateur (mapping requête/bundle, taxonomie d'erreurs, snapshot de prix daté depuis docs/research/model-research.md), de la forme des fichiers prompts et de la convention chats/.
- CP-MVP-008 S01 (bootstrap, unité docs-seule): réconcilié (base 6cacfa2, 435/43 verts, arbre de travail = seuls les fichiers owner préexistants); LECTURES PLEINES faites (06 pipeline AI — formes AiOperation/Bundle/Patch, étiquetage mécanique, budgets sous le renderer, un op accepté = un diff; 05 Selection/ContextScope; 28 claims/évidence — le modèle ne s'auto-note jamais; 33 politique d'exécution/budgets/traces + échelle de retrieval + ledger .atomik/usage/private/actions.jsonl; 26 context packets inspectables; 13 §IPC/clés/cloud/worker-local — clés jamais dans le preload, extraits minimisés, repli cloud jamais silencieux; 14 §ai-core — possède les adaptateurs, ne possède ni le ledger ni les écritures). PINS consignés au checkpoint du chemin: contrat GenerationAdapter (module main PURE COMPUTE comme ai-mock, identité dans la sortie de l'adaptateur répondant, mapping messages/bundle, labelClaims mécanique sur la vraie sortie), taxonomie d'erreurs typée (offline/timeout/auth/quota/4xx/5xx/annulé/budget — surfacée, JAMAIS de repli silencieux vers le mock), id de modèle EXACT épinglé (précédent mistral-ocr-4-0; S02 confirme l'id Mistral Small courant et le date), snapshot de prix daté (model-research.md: Small $0.10–0.15/$0.30–0.60 par MTok, 128K, résidence EU — billing basis 'estimated', tokens provider-reported préférés), budgets main-side (2k out, 60 s AbortController, constantes du mock en entrée), forme prompts/ (frontmatter kind: system|message, verbes vault existants, zéro nouvel IPC), convention chats/ (décision owner: chats/YYYY-MM-DD-<slug>.md, type: Atomik Chat, NÉ au premier message — jamais à l'ouverture, sections ## you/## atomik par le chemin d'écriture ordinaire, note éditable/liable), bande de claims inline (langage truth-chip existant), OUVERT au bench: moteur par défaut quand une clé existe (proposé: mistral). current_step → S02 (l'adaptateur Mistral de bout en bout).
- CP-MVP-008 S02 (l'adaptateur Mistral de bout en bout — LA GÉNÉRATION QUITTE LE MOCK). Le seam typé `GenerationAdapter` posé dans `electron-main/generation.ts` (siège ai-core de 14: engins PURE COMPUTE, l'identité voyage dans la sortie de l'adaptateur répondant — le contrat renderer est INCHANGÉ, la promesse de l'en-tête d'ai-mock tenue au caractère près) avec le mock S08 derrière (engin sélectionnable, chemin offline) et la taxonomie `GenerationError` à HUIT genres portée en `ai(<kind>): …` pour survivre à l'aplatissement IPC: offline/timeout/auth/rate-limit/provider-request/provider-server/cancelled/budget-exceeded — surfacée, JAMAIS de repli silencieux vers le mock (13). `mistral-generation-adapter.ts`: chat completions sur l'id ÉPINGLÉ EN DIRECT `mistral-small-2603` (docs.mistral.ai models overview, confirmé et daté 2026-07-21 — Mistral Small 4; précédent mistral-ocr-4-0/voxtral-mini-2602); budgets EN MAIN sous l'état renderer (2k tokens de sortie, mur 60 s, pré-check d'entrée AVANT que les octets voyagent) avec DEUX sources d'abort sur UN signal fetch (le timer de l'adaptateur + le cancel de l'appelant, un drapeau les distingue — timeout ≠ cancelled); candidats de claims DÉTERMINISTES sur la vraie sortie (phrases, fences exclues, plafond 32) → `labelClaims` INCHANGÉ (le containment exact reste la seule route vers source-backed; le prompt système exige la citation exacte — une paraphrase gagne honnêtement model-only); usage provider-reported PRÉFÉRÉ et étiqueté (repli chars/4 étiqueté estimated); coût USD estimé du snapshot daté docs/research/model-research.md@2026-07-20 (borne HAUTE $0.15/$0.60 par MTok — une estimation ne flatte pas), id du snapshot DANS la ligne de trace; retry-after surfacé sans boucle d'auto-retry; finish_reason=length → uncertainty « tronqué au budget ». Sélection d'ENGIN persistée à côté de la clé (`atomik:set-ai-engine`; résolution: choix explicite → 'mistral' si clé (défaut PROPOSÉ, l'owner confirme au bench S07) → 'mock') + `atomik:cancel-ai-operation` (abort en vol par id d'opération; registre main des AbortController); AppMenu gagne le picker mock|mistral (+ avertissement clé absente), AiPanel gagne Cancel pendant running; preload + DOCUMENTED_PRELOAD_SURFACE étendus (le test de surface tient l'égalité des deux côtés). Traces: draftFor/recordFailure portent l'identité d'engin — lignes cloud en location cloud-model + privacy.mode 'cloud', les DEUX paires de tokens gardées et étiquetées, contentRecorded toujours false. Tests 435→455/44 (generation-adapter.test.ts FIXTURES SEULES: construction des messages, mapping bundle par destination, extraction déterministe, taxonomie complète, timeout-vs-cancel, pré-check budget, mock derrière le seam; ai-settings + action-trace étendus); typecheck/build verts; smoke `ai=ok:2/1/4/1` avec les quatre labels À TRAVERS le nouveau handler async; rung LIVE `ATOMIK_SMOKE_AI_LIVE=1` câblé (bascule d'engin → vraie complétion → trace cloud → cancel en vol) — `skip:no-key` honnête aujourd'hui, la preuve live atterrit avec la clé owner au bench S07. Docs même unité: note learning 17 (règle first-use — premier adaptateur de génération réel) + index, note module (section boucle AI réécrite, inventaire de tests), ledger (S02 coché, checkpoint, current_step → S03), ACTIVE.md. Quirk consigné: la restauration d'engin du rung smoke écrit le résolu en explicite.
- CP-MVP-008 S02 addendum (pointeur owner: « u can use the one in .env.local no ? »): la PREUVE LIVE est FAITE. Le handler de génération honore désormais le même override dev `MISTRAL_API_KEY` que le rung de transcription cloud (précédent CP-MVP-005, même site index.ts — la clé du fichier settings GAGNE quand elle existe; l'override reste invisible de publicAiSettings, comme pour l'OCR: l'engin 'mistral' se choisit explicitement quand seul l'override existe), le check de présence du rung live passe MAIN-SIDE (runSmoke reçoit stateDir), et le rung a tourné avec la clé de .env.local sourcée pour la session seulement (le state dir smoke est un mkdtemp scratch — rien n'atterrit sur disque): `aiLive=ok:cloud-model/mistral-small/150+11tok/0.000029USD:cancel=cancelled` — vraie complétion mistral-small-2603, trace en identité cloud avec usage provider-reported (150+11 tokens) et coût estimé du snapshot ($0.000029), cancel EN VOL sur latence réelle rejetant en ai(cancelled). Tests 455/44, typecheck, build verts. L'item « preuve live au bench S07 » est levé — le bench S07 garde la clé posée via l'UI et le vault réel.
- CP-MVP-008 S02 validé owner (« test ok », 2026-07-21) — la génération réelle confirmée côté owner après la preuve live du rung. S02 clos sans réserve; prochaine action inchangée: S03 (dossier prompts/).
- CP-MVP-008 amendement prompts/ SCOPÉS (échange par options, owner: « what do you think to have a root prompt folder and ther folder and project specific prompt folder ? » → choix « Root + any folder + project »): le pin S01 root-only est SUPERSÉDÉ AVANT toute exécution de S03 — un dossier `prompts/` peut vivre à la racine du vault, dans N'IMPORTE QUEL dossier, et dans un bundle projet; résolution PLUS-PROCHE-GAGNE depuis la note vers l'extérieur (chaîne de dossiers de la note → bundle projet → racine → built-ins), un prompt de même nom plus proche OMBRAGE le lointain (un projet peut REDÉFINIR un prompt racine, pas seulement ajouter), le menu AI groupe par scope pour que l'ombrage reste VISIBLE; matérialisation des starters explicite PAR SCOPE (créer prompts/ dans un projet est en soi un acte explicite); frontmatter/verbes vault/zéro nouvel IPC inchangés. DoD, étape S03 et pin du checkpoint amendés dans le fichier de chemin. Cohérence doctrine: 04 (conventions par dossier, précédent S07k index/log), bundles projet. Aucun code touché — S03 n'a pas commencé.
- CP-MVP-008 S03 (dossiers prompts/ SCOPÉS — l'amendement owner du jour exécuté). `renderer/src/editor/prompts.ts`: la marche des scopes depuis la note vers l'extérieur — racine + N'IMPORTE QUEL dossier + bundle projet couverts par UNE règle (un projet EST un dossier de la chaîne), PLUS-PROCHE-GAGNE avec ombrage par nom (un projet REDÉFINIT un prompt racine; l'ombrage reste VISIBLE via les tags de scope: 'this folder' / chemin / 'vault'); un prompt = markdown + frontmatter `kind: system | message` (+title/description); une non-prompt dans prompts/ est SAUTÉE (jamais une erreur), index/log de convention exclus; scan par les verbes EXISTANTS injectés (`listVaultFiles`/`readNote` — zéro nouvel IPC, testable sans vault). AiPanel: les prompts message rejoignent la rangée de presets (tag de scope au survol, clic = corps chargé, preset `file:<nom>` dans la trace), les prompts system alimentent un sélecteur dont le corps voyage en `AiOperation.systemPrompt` — NOUVEAU champ optionnel du contrat, borné 8k dans `isValidAiOperation` côté main; il remplace la SEULE ligne d'identité du prompt système: les règles de citation exacte, le brief de destination et no-preamble sont composés CÔTÉ MAIN quoi qu'il arrive (aucun fichier prompt ne peut se soustraire au contrat mécanique, 28); rechargement sur vaultFilesChanged (éditer un prompt = éditer une note, le prochain run sert le corps édité). Starters (1 system + 2 message, auto-valides comme prompts) UNIQUEMENT via l'action explicite ☰ → « Create starter prompts » — idempotente, manquants-seulement (un starter supprimé se re-matérialise, un édité n'est JAMAIS touché; createNote fait naître prompts/ avec ses conventions S07k); zéro écriture à l'ouverture. Tests 455→467/45 (prompts.test.ts: chaîne, collection nearest-first + ombrage + exclusions, parse honnête, chargement injecté + aller-retour edit→use, labels, matérialisation; ai-mock: bornes systemPrompt; generation-adapter: la composition identité-remplacée/règles-conservées); typecheck/build/smoke verts (l'op smoke porte désormais un systemPrompt à travers le vrai canal). Pills et chat consomment S03 à leurs surfaces (S04/S06). Pas de note learning: convention + scanner sur patrons prouvés — aucune première mobilisation; consigné au ledger. current_step → S04 (menu contextuel de sélection).
- CP-MVP-008 S03b (deux directives owner: « when in a prompt folder, context menu should show create prompt with autofill frontmatter based on UI choices and input, also we should be able to insert nested message prompt and nested system prompt as buildable layers of one custom prompt »). (1) CRÉATION AU MENU: le menu contextuel de l'arbre montre « New prompt… » dans TOUT dossier prompts/ (`isPromptsFolder` — racine, dossier, projet pareil): un radio de kind (message | system) + le nom, et le frontmatter s'autofill (`buildPromptFileContent`: kind, nom→title façon « key-points » → « Key points », l'indice de layers dans la DESCRIPTION — jamais envoyé au modèle); même morphe TreeMenu, même verbe createNote, le fichier s'ouvre pour édition. (2) LAYERS COMPOSABLES: une ligne ENTIÈRE `{{prompt: name}}` dans le corps d'un prompt insère le prompt nommé comme couche (`expandPromptLayers`) — system et message se composent librement, le kind du prompt EXTÉRIEUR gouverne l'usage; les noms se résolvent par la MÊME résolution scopée plus-proche-gagne (un projet redéfinit une couche comme il redéfinit un prompt — testé dans les deux sens depuis la même paire de fichiers); profondeur max 8; inconnu, cycle et mention INLINE restent LITTÉRAUX — une référence cassée se VOIT, jamais avalée; l'expansion tourne au chargement, APRÈS l'ombrage, donc l'instruction et le systemPrompt partent déjà composés (les bornes 4k/8k s'appliquent au résultat composé et remontent honnêtement). Tests 467→473/45 (expansion imbriquée, littéraux, override scopé d'une couche, reconnaissance prompts/, autofill re-parsable dans les deux kinds); typecheck/build/smoke verts. current_step reste S04.
- CP-MVP-008 S03c (directive owner: « make a quick action menu @ in prompt accessing prompts »). Le `@` dans le champ d'instruction AI convoque le menu des prompts — le précédent du menu @ de citations de l'éditeur, refait à la main pour un textarea (zéro dépendance, patron TreeMenu/popover): un token s'ouvre sur un `@` en début de texte ou après un espace (les emails et @ en milieu de mot restent inertes), le filtre nom+titre suit la frappe, clavier complet (flèches/Entrée/Tab/Échap, roving aria-selected) + clic (mousedown avant le blur); choisir un prompt MESSAGE insère son corps COMPOSÉ (layers déjà expansés) au curseur en remplaçant le token, choisir un prompt SYSTEM règle le sélecteur système et efface le token; chaque ligne porte son tag de kind et son tag de scope (l'ombrage reste visible jusque dans ce menu); style popover 36 (verre --glass-pop + repli opaque @supports, étage --z-pop). Helpers purs `atPromptToken`/`applyAtInsertion`/`filterPrompts` testés (bords: email, espace clôturant, caret avant le @, remplacement vide du pick system, filtre insensible à la casse). Tests 473→477/45; typecheck/build/smoke verts. current_step reste S04 — la surface S04 (input custom au clic) réutilisera le même trio de helpers.
- CP-MVP-008 S03d (trois retours bench owner: « prompt doesnt show as prompt in pill, search is not ordered from folder to root inverted hierarchy, I dont follow how you integrated it with the layering possibilities »). (3) LA CORRECTION DE FOND — l'intégration @/layers était FAUSSE: le pick @ APLATISSAIT le corps du prompt dans l'instruction, détruisant la couche. Corrigé au modèle owner: le pick @ insère LA DIRECTIVE `{{prompt: name}}` elle-même (`insertDirectiveAt`, rembourrage pleine-ligne pour préserver la règle des mentions inertes) — l'instruction EST un prompt custom composable; elle se COMPOSE À L'ENVOI (`expandInstruction`) via la résolution scopée de la note (un override projet d'une couche joue jusque dans l'instruction), la boîte garde la forme en couches, une couche inconnue voyage LITTÉRALE et visible; les pilules AJOUTENT la directive au lieu d'écraser le texte tapé. (1) Les pilules adossées à un fichier se lisent PROMPT: anneau pointillé + glyphe @, kind/scope au survol — les built-ins restent pleins. (2) Ordre: le scan était bien plus-proche-d'abord (dossier → racine) et il est désormais ÉPINGLÉ par test à travers le filtre; cause probable du constat owner: un prompt édité côté Windows en CRLF échouait la clôture de frontmatter et DISPARAISSAIT en silence — `parsePromptFile` tolère CRLF désormais (testé). Tests 477→481/45; typecheck/build/smoke verts.
- CP-MVP-008 S03e (captures d'écran owner: « still proposing not prompt first and not labeled as prompts »). Le diagnostic change tout: le @ de l'owner était le menu de CITATIONS DE L'ÉDITEUR (il rédigeait prompts/tone.md dans l'éditeur), pas le panneau AI — les prompts n'y apparaissaient que comme entrées « note … link » génériques, noyées sous les sources. `promptLayerEntries` rejoint quick-actions.ts: quand la note ÉDITÉE vit dans un dossier prompts/, le menu @ OUVRE sur les prompts résolus de la note — pilule `prompt` (accent, à côté de note/source, règles sélectionnées incluses), kind + « insert layer » + scope dans le détail, ordre plus-proche-d'abord dossier→racine encodé dans le boost CodeMirror (l'ancre de tri survit au filtre de frappe), le pick insère LA DIRECTIVE `{{prompt: name}}` (jamais un lien), le fichier ne se propose jamais lui-même; hors dossier prompts/ le menu citations reste INCHANGÉ (une directive est inerte dans une note ordinaire — décision consignée). Tests 481→484/45 (entrées frères jamais soi, rien hors prompts/, boost décroissant du plus proche au plus lointain sur trois scopes); typecheck/build/smoke verts.
- CP-MVP-008 S03f (clarification owner: « nesting system prompts was choosing multiple prompt and order then to choose the nesting direction, exemple system prompts : personality > tone > objectives … pushing for modularity and/or interconnection … compose modular blocks (or not) on both message and system side to fine tune generation and in the future agent behavior, also as agent will have access to vault, it could also write its own sub agent prompts in this very modular way »; échange par options: drag-and-drop + save maintenant). Le côté system du panneau AI devient une PILE ordonnée de blocs: le sélecteur unique disparaît, remplacé par une rangée de pilules `personality › tone › objectives` — le picker @ ajoute un bloc (doublon = no-op; le pick system du menu @ de l'instruction AJOUTE aussi à la pile désormais), glisser-déposer réordonne (`reorderStack`, no-op hors bornes), × retire; au run les corps (déjà expansés en couches) se composent DANS L'ORDRE de la pile (`composeSystemStack`, un bloc supprimé du vault tombe en silence — le run ne se coince jamais) en UN systemPrompt — les règles mécaniques de citation se composent toujours côté main par-dessus. « save » persiste la pile comme FICHIER prompt de lignes de directives dans prompts/ racine (`stackFileContent`) — ALLER-RETOUR PROUVÉ par test: l'expansion du fichier sauvé reproduit la composition de la pile. L'UNIFICATION porte la vision agent: une pile sauvée EST un prompt modulaire ordinaire — un futur agent écrivant dans le vault authorera ses prompts de sous-agents exactement ainsi (pointeur consigné pour la conversation roadmap de la cérémonie de clôture). Le côté message garde sa modularité par directives visibles dans l'instruction. Tests 484→487/45; typecheck/build/smoke verts.
- CP-MVP-008 S04 (le menu AI de la sélection — l'entrée devient LA SÉLECTION, 05). `AiSelectionMenu.tsx` sur la machinerie TreeMenu (overlay, clamp à l'écran, morphe sur place, erreurs dans le popup, zéro dépendance): clic droit dans l'éditeur — ou Shift+F10 à la position du caret (précédent arbre) — ouvre le menu à l'endroit du clic, l'en-tête montre l'extrait de sélection (ou « whole note », le repli 05 existant). ACTIONS RAPIDES: les prompts MESSAGE résolus de la note, groupés par scope plus-proche-d'abord (`groupPromptsByScope` — l'ombrage reste lisible), puis les built-ins; un pick lance LA DIRECTIVE de couche (modèle S03d — la composition au run). « Custom… » morphe en saisie à l'endroit du clic avec les PILULES system: l'ORDRE DE CLIC construit la pile (`toggleStackBlock`, pilules numérotées 1·2·3 — la direction d'imbrication S03f au doigt), Ctrl+Entrée lance. « Open chat » ouvre le panneau ancré à droite — surface conversationnelle INTÉRIMAIRE jusqu'à la colonne latérale S06 (consigné). Remise menu → panneau par `AiPanelRequest` (id appliqué exactement une fois; l'auto-run DIFFÉRÉ d'un rendu pour que l'état préchargé atterrisse — pas de fermeture obsolète). LE BOUTON SPARKLE QUITTE LA NOTE-BAR (directive owner: la rangée du haut = préoccupations d'édition seulement). Écarts honnêtes au ledger: pas de menu @ dans la saisie du popup (le panneau l'a — l'itération continue là), visuels du menu au bench owner. Tests 487→489/45 (ordre de groupage, bascule des pilules); typecheck/build/smoke verts (le rung AI traverse la remise S04 inchangé). current_step → S05 (aperçu inline vivant).
- CP-MVP-008 S04b (rapport owner: « There is problem with whats generated as It doesnt correspond to prompt sent, can you verify how you build the request and expose it to me »). CAUSE RACINE trouvée en traçant la chaîne: l'auto-run S04 tirait sur L'INSTANTANÉ D'ÉTAT des prompts — vide juste après le montage du panneau (le chargement est async) — donc `{{prompt: name}}` ne résolvait rien, restait LITTÉRAL et voyageait tel quel jusqu'au modèle (l'instruction reçue était la directive brute — d'où la génération sans rapport); la pile system se vidait par la même course. CORRECTIF: run() attend LA PROMESSE de chargement (`promptsReady` ref) et compose contre les prompts réels — auto-run ou manuel, la course disparaît structurellement. EXPOSITION (26, demande owner): un inspecteur « sent request » repliable dans le panneau — l'instruction COMPOSÉE et la pile system EXACTEMENT comme parties (couches expansées), le preset, la sélection (bornes/chars/note entière), la destination; posé AVANT l'await pour qu'un run échoué reste inspectable; la note précise que main ajoute par-dessus les règles de citation et le brief de destination. Limite consignée: la course est du niveau hooks, non testable unitairement dans ce harnais (pas de tests DOM React); la littéralité d'une expansion à liste vide est déjà épinglée par test. Tests 489/45 inchangés; typecheck/build/smoke verts.

## 2026-07-22

- CP-MVP-008 S04c (redesign owner du menu contextuel: « we can't fast choose system prompt with message prompt, you idea of 1,2,3 is good so expend it to message prompt as well … first and only contextual display: display limits …, a search bar if we reach the maximum displayed, orderable message prompts section, orderable system prompts section, built in section, and an input section but not mandatory and a send/run button and enter key also launch and close context menu »). Le menu devient UN SEUL écran composeur — plus de morphe menu→custom: section MESSAGE ordonnables et section BUILT-IN partagent UNE séquence de clics numérotée (1·2·3 traverse les deux — `composeMenuInstruction`: directives de couche pour les fichiers, lignes brutes pour les built-ins, la saisie libre OPTIONNELLE en dernier; entrées inconnues tombent; rien de composé = Run désactivé), section SYSTEM numérotée à part (la pile S03f), saisie facultative, UN bouton Run; ENTRÉE lance et ferme (Shift+Entrée = retour ligne; Entrée marche aussi depuis la barre de recherche). Plafond d'affichage par section (`visibleMenuPrompts`, 6) avec barre de recherche au-delà de 10 prompts au total — un pill CHOISI ne sort jamais de l'affichage (son numéro doit rester visible), testé y compris quand la recherche ne le matche pas. Le preset de trace n'est posé que pour un pick unique sans saisie. « Open chat » survit en pied de menu (intérim dock-droit jusqu'à S06). Tests 489→491/45; typecheck/build/smoke verts.
- CP-MVP-008 S04d (rapport owner + capture: « i am highlighting ethymology and it respond on philosophy … Didn't you make something too complicated ? »). VÉRIFIÉ d'abord: la bonne variable voyageait — l'inspecteur de la capture montre selection 110–120 · 10 chars = le mot surligné. La fuite était la FORME DU MESSAGE, pas la plomberie: l'en-tête du bloc de sélection OUVRAIT sur le chemin du fichier (« Selection 1 — from `philosophy/philosophy.md` ») — un sujet de 10 caractères sous un « philosophy » bien visible, et le modèle a répondu sur l'étymologie de philosophy. Correctif simple, deux lignes: (1) bloc SUJET-D'ABORD dans buildMessages — `### Subject selection N`, contenu fencé, le chemin RÉTROGRADÉ en ligne de provenance APRÈS le contenu; (2) règle système ajoutée: « le texte sélectionné EST le sujet de la requête; ne jamais inférer le sujet d'un chemin ou d'un nom de fichier ». Épinglé par test: sujet avant provenance, plus de chemin dans l'en-tête, la règle présente. L'inspecteur « sent request » montre désormais le TEXTE capturé de la sélection (extrait 200 chars) à côté des bornes — voir bat faire confiance aux offsets. Tests 491→492/45; typecheck/build/smoke verts.
- CP-MVP-008 S04e (capture owner: « input field is off and also we cant chose the way it will integrate replace, append, new note »). (1) Le popup prend une LARGEUR FIXE (330px, box-sizing) et la recherche + la saisie passent en pleine largeur — le textarea ne déborde plus du menu. (2) Section DESTINATION dans le composeur: pilules radio replace / append / new note (replace désactivée sans sélection, défaut append), le choix voyage AiMenuRequest → AiPanelRequest et atterrit dans le panneau AVANT le tir de l'auto-run — le run du menu intègre désormais là où l'owner l'a dit. Tests 492/45 inchangés (câblage UI); typecheck/build/smoke verts.
- CP-MVP-008 S04f (rapport owner: « the answers are very short lol … something is off, can you reconstruct the final request sent to the model ? »). RECONSTRUCTION VERBATIM depuis le vault réel (/home/toure/vault-juju): system = le corps d'evergreen.md (« Answer using evergreen notes style for atomic knowledge managment system ») + les règles mécaniques + le brief new-note; user = « Instruction: Explain this simply. » + sujet de 10 caractères « ethymology »; max_tokens 2000, temp 0.2. VERDICT: pas un bug — chaque couche empilée demandait COURT (style evergreen atomique × « explain simply » × sujet d'un mot × règle selection-only; 22 tokens de sortie, aucun drapeau de troncature) — le modèle a OBÉI à la composition; la richesse se règle dans le CORPS du fichier prompt (la pile la portera). DEUX vrais correctifs sortis de la capture: (1) l'en-tête du panneau affichait « AI · mock provider » EN DUR depuis S08 alors que la trace disait cloud-model — il affiche désormais l'engin RÉSOLU (28: identité honnête); (2) le brief new-note disait « starting with a # title » et invitait le titre « # # Etymology » à double dièse — reformulé (« level-1 heading line…, do not repeat the # character »). Tests 492/45; typecheck/build/smoke verts.
- CP-MVP-008 S04g (directive owner: « store request content sent and make it displayable on hover somewhere »). Le contenu de la requête envoyée est CONSERVÉ par run — en mémoire renderer SEULEMENT: le ledger de traces reste sans contenu, contentRecorded=false intouché (33) — et l'inspecteur clic-pour-déplier devient une PUCE + POPOVER au survol ET au focus clavier (plancher a11y 36; idiome popover: verre --glass-pop, repli opaque @supports, étage --z-pop): la puce porte le résumé (destination · bornes · chars), le popover porte le système composé, l'instruction composée et la sélection capturée tels qu'envoyés (26). Tests 492/45 inchangés (présentation); typecheck/build/smoke verts.
- CP-MVP-008 S04h+i (owner: « you didn't expose the main side default and grounding rules » puis « I want to be able to copy the prompt to test it somewhere else, so I need the full request copiable »). La composition déménage dans shared/prompt-composition.ts — UNE source de vérité pour l'adaptateur (main) ET l'inspecteur (renderer): identité built-in, les quatre règles d'ancrage, les trois briefs de destination, la règle de clôture, le message user sujet-d'abord. Le popover montre désormais le PROMPT SYSTÈME FINAL VERBATIM (pile ou built-in + règles + brief + clôture — plus jamais un libellé qui les résume) et la forme exacte du message user. Bouton « copy full request » dans le popover (focus-within garde le popover ouvert; feedback « copied ✓ ») → texte portable `=== SYSTEM === / === USER ===` avec le CONTENU COMPLET de la sélection (conservé en mémoire renderer seulement — le ledger de traces reste sans contenu). ÉPINGLÉ PAR TEST: les deux moitiés de buildMessages === les sorties du composeur partagé (l'affichage/copie égale le fil PAR CONSTRUCTION), le texte portable porte les deux verbatim. Tests 492→493/45; typecheck/build/smoke verts.
- CP-MVP-008 S04j (comparaison playground de l'owner: « I dont have the same result in the mistral playground »). DIAGNOSTIC en trois volets. (1) ÉCHANTILLONNAGE, pas composition: le playground tournait mistral-small-latest à température 0.7 / max_tokens 2048; l'app épingle mistral-small-2603 à 0.2 / 2000 — deux échantillons uniques à températures différentes sur des versions possiblement différentes ne se comparent pas. (2) STRUCTUREL: dans le run de l'app, evergreen voyageait comme INSTRUCTION (frontmatter passé kind: message → section message → preset file:evergreen; system = built-in) — une instruction QUI PARLE du style evergreen + un sujet de 10 caractères = deux topics en compétition, le modèle a pris le plus bavard; guidance owner: un prompt de COMPORTEMENT se déclare kind: system et se choisit dans la section SYSTEM (il apparaît alors dans le bloc system de l'inspecteur, au-dessus des règles). (3) MON AFFICHAGE a invité la reconstruction erronée: le popover montrait des blocs étiquetés (instruction / selection) au lieu de la forme du fil — il montre désormais LES DEUX MESSAGES API VERBATIM (vue composeUserMessage — l'affichage EST le fil, comme la copie) + une ligne de params (temp 0.2 · max_tokens 2000 · modèle épinglé) pour des tests externes équitables. Tests 493/45; typecheck/build/smoke verts.
- CP-MVP-008 S04k (contre-poussée owner — à raison: evergreen était dans le MESSAGE dans LES DEUX runs, le point structurel de S04j ne pouvait pas expliquer l'écart). BENCH A/B sur le VRAI fil, modèle épinglé mistral-small-2603 via l'API (clé owner, ~21 appels, centimes): le fil exact de l'app → « # Evergreen » 3/3 à t=0.2; le collage playground de l'owner → « # Etymology » 2/2 à t=0.2 — L'ÉCART ÉTAIT LE CADRAGE DU FIL, ni la température ni message-vs-system. MÉCANISME: le corps du prompt ouvre sur un H1 (« # evergreen ») juste derrière « Instruction: », et le brief new-note demande « ouvre avec un titre H1 » — le modèle ADOPTAIT le H1 tout prêt de l'instruction comme titre de la note. CORRECTIF BENCHÉ AVANT LIVRAISON: règle d'ancrage « les titres dans l'instruction sont du guidage STYLE/COMPORTEMENT — le sujet et le titre viennent de la sélection » (3/4 seule) + l'instruction voyage EN BLOCKQUOTE dans le message user (4/4 avec les deux). Livré dans le composeur PARTAGÉ — fil, popover et copie bougent ensemble par construction; test mis à jour à la forme citée. Tests 493/45; typecheck/build/smoke verts.
- CP-MVP-008 S04l (owner: « The copy prompt option was not working » + « I am not sure you are using prompting best practice … the final request sent should be clearly layered as # title 1, ## title 2 … your implementation should respect that when injecting blocks and layers … if I append or replace, you should give as context the current state of the note and show where the generation is gonna land … step by step instructions »). REFONTE du composeur partagé en GABARIT MARKDOWN FIXE à injection déterministe — SYSTEM: `# Role` (identité pile ou built-in) / `# Rules` (`## Grounding` en puces, `## Output` = brief de destination + clôture); USER: `# Request` / `## Instruction — style and behavior guidance (quoted)` (blockquotée, leçon S04k) / `## Subject` (`### Selection N`, fencé, provenance en trait final) / `## Note context — read-only` / `## Steps` (l'ordre d'intégration explicite: sujet → style → contexte → sortie). NOUVEAU `AiOperation.noteContext` (validé main-side, 8k par partie): APPEND embarque la FIN COURANTE de la note (3000 chars) + « appended right after the ending shown — do NOT duplicate »; REPLACE embarque les extraits AVANT/APRÈS (1500 chars chacun) + « must read seamlessly »; capturé côté renderer au run, il voyage identiquement dans le fil, le popover et la copie (le composeur partagé garantit l'égalité). BENCH LIVE avant livraison (modèle épinglé, t=0.2): le cas etymology 3/3 sur le bon sujet avec le nouveau gabarit; la sonde append a produit une section SŒUR distincte au bon niveau de titre sans dupliquer le contenu existant. COPIE RÉPARÉE: le rejet silencieux de navigator.clipboard (avalé par mon catch) tombe sur un repli textarea+execCommand, avec état visible « copy failed » à défaut. Tests 493→496/45 (gabarit des deux messages, sections note-context append/replace + steps conditionnels, bornes noteContext); typecheck/build/smoke verts.
