# Brainstorm

Explicitly provisional thinking. Nothing here is a decision; promotion goes through reviewed patches into docs/bedrock or docs/adr.

**Persistence rule (owner directive, 2026-08-03 sessions B/C):** a brainstorm session is a dialogue, persisted ONCE at its end — as **three artifacts**: (1) the session file here; (2) its line in this index (discoverability only); (3) a **recorded-input note in [the coding-path register](../coding-paths/index.md)** pointing at the session file and naming, per concerned path/milestone opening, what that opening reads it for — phrased "pointer, not a decision; confirmed or overridden at the ceremony." The register note is the load-bearing one: the bootstrap chain (AGENTS.md → 22 → ACTIVE.md → register) forces every path-opening through the register, so it is the only mechanical guarantee a future agent inherits the session ("no milestone silently unassigned" extends to "no recorded input silently orphaned"). Re-read this index AND the register immediately before editing — concurrent owner sessions touch both. Pattern to follow: the 2026-08-03 session B and C notes.

- [2026-07-06 — Panes, tabs, and where navigation lives](./2026-07-06-panes-tabs-navigation.md) — keep-signal: tab groups per pane are a loved differentiator; open question: workspace-level tree vs per-view tree (decide ≥ S05).
- [2026-07-06 — Creation flows and drag-and-drop](./2026-07-06-creation-flows-and-dnd.md) — folder-first mental model vs note-first folders (leaning: pending folders in UI state); DnD split into tabs / tree-moves-with-link-repair / OS-file import with three timelines; pills kept for large projects.
- [2026-07-06 — Evidence scope and the AI panel](./2026-07-06-evidence-scope-and-ai-panel.md) — owner instinct: evidence should recognize other notes (= reuse loop / M8, keep it mechanical); AI panel gains resize + right dock, tab-kind evolution reserved.
- [2026-07-06 — Does read mode survive live preview?](./2026-07-06-read-mode-vs-live.md) — owner question; lean: keep read cheaply (faithful render + plain-click links + no accidental edits), re-decide at M5 with usage evidence.
- [2026-07-07 — One vault per pane/tab?](./2026-07-07-multi-vault.md) — owner question; lean: plumbing is easy, semantics are the permanent tax ("which vault?" everywhere); ladder = repo-root vault now → window-per-vault if needed → per-tab last resort after M8 defines cross-vault evidence.
- [2026-08-03 — Brainstorm session: definitive-vault timing · semantic graph relations](./2026-08-03-brainstorm-session.md) — vault: start at 008's S07 acceptance (content safe since M1; one possible meta migration before M6). Graph (owner frame): notes ARE concepts, the link graph IS the semantic graph; claims = optional overlay, never birth requirement (bedrock 20 patch needed); spec sketch `[[target]]{label}` inline decoration (collision-tested against markdown-it/Lezer), own-language autocomplete, typed backlinks; fits as the FRONT half of the post-008 M8 path, before the Wikidata slice; feeds 008's closing ceremony.
- [2026-08-03 — Brainstorm session B: claims epistemology — from mechanical labeling to the LLM judge](./2026-08-03-brainstorm-session-b.md) — parallel session; today's containment check = honest floor, the judge LLM is bedrock 29's planned engine; implementable via retrieve-then-judge (index-bounded candidates, batched fast-model calls — never all-pairs); owner rulings: review = transient panel (no file), claim-vs-claim edges deferred; decided (provisional, freezes at M6): 4-verdict vocabulary (`supports`/`partially-supports`/`contradicts`/`unrelated`, mandatory qualifiers, no confidence floats), event schema + RECEIPTS (justification survives IF claim accepted; raw trace ephemeral); labels `model-verified`/`disputed`/`challenged` land with M6; M8 gains claim-verification as named retrieval consumer; recorded as M6/M7/M8 opening input in the register.
- [2026-08-03 — Brainstorm session C: websearch tool — contract, backends, Wikimedia-first grounding](./2026-08-03-brainstorm-session-c.md) — parallel session; SOTA converged on the 07-08 charter (own the tool contract: app-side `search`/`fetch` via function calling, swappable backends; tool LOOP greenfield in the handler); core rule: **search transiently, fetch your own evidence** (search-API storage ToS forbid persistence; stored quotes come from pages the app fetched itself — existing WebContentsView/reader machinery); backend ladder Tavily → SearXNG → loud in-app scrape fallback (Bing-API retirement = the seam's justification); **owner lean: Wikimedia family first (Wikidata structure + Wikipedia prose, claim-nature → sister-corpus routing), Tavily only on receipt-recorded fall-through**; serving ladder rung 0 live APIs → rung 1 few-GB SQLite entity subset (= M10 shape) → rung 2 Kiwix ZIM offline packs (opt-in, lifecycle rule); Commons P18 hover-cards/import as a fourth consumer of the entity index; addendum: entity reference links (statement refs + external IDs + Wikipedia citations) = curated subject search rung between layer B and Tavily (anti-circular, editor-chosen sources); recorded as M6/M7/M8 + post-008 opening input in the register.
