---
type: Atomik Brainstorm
title: Generation, grounding, and the agent harness — owner strategy resume (provisional)
timestamp: 2026-07-08T15:40:00Z
---

# Provisional — nothing here is a decision (promotion goes through patches)

Owner resume at CP-MVP-005 close, confirmed/corrected in session:

- **Cloud-as-reference first** — the proven house method (Voxtral for
  speech, Mistral OCR 4-0 for OCR): elect a ceiling, bench local
  candidates against it on the owner's real use (34), correction effort
  as THE metric.
- **Grounding is two rungs, not one choice**: M6 = embedded-source
  verification (claim vs imported quote/page — local-friendly; M4's PDF
  anchors feed it). M7 = live-web verification via provider — cloud as
  the durable seat, not merely a reference (live web needs search
  access).
- **Websearch: own the TOOL CONTRACT, not a search engine** — typed
  seam behind main (search → typed results; fetch → reader text) with
  swappable backends, same pattern as the sidecar/cloud-rung seams;
  no home crawling (standing prohibition + thinness rule). This is
  what "prepares for local models": they call the tool through the
  harness.
- **Agent harness (M8)**: okf context assembly (26) outside the model +
  the EXISTING patch pipeline law (06: proposed patches with preview,
  never silent writes) + a chat surface for reflection (the new part).
  File creation/edition for brainstorming/research rides patches.
  **Single loop first; subagents only when a dated eval shows the
  single loop saturating.**
- **SOTA honesty (2026-07)**: local 4–14B = seat-worthy for BOUNDED
  operations (summarize/extract/rewrite/GBNF-constrained); multi-step
  agentic reasoning degrades sharply at these sizes — frontier cloud
  stays the harness-grade reference. The house bet: file-first context
  + patch preview + typed tools + constrained decoding move
  intelligence into the harness — strong harness + small model beats
  naked strong model FOR BOUNDED OPS; open-ended research stays on the
  cloud rung meanwhile. Fastest-moving gap in the field (Qwen3.5-4B:
  natively multimodal, 262k ctx) → re-bench per 34, never assume.
- Sequence unchanged: M4 (active) → M5 → M6 → M7 → M8 → M9; the owner
  may re-sequence by decision in the register (M9 pull-forward is
  cheap post-005 if generation matters sooner).
