---
type: Atomik Coding Path Register
title: Coding path register — vision to execution mapping
timestamp: 2026-07-05T00:00:00Z
---

# Coding path register

The bedrock carries the whole vision; the roadmap (18) sequences it; a coding path executes one bounded slice. This register keeps the mapping complete: every roadmap milestone is accounted for below — active, reserved, or not yet opened. No milestone is silently unassigned. This is the portfolio-level form of the no-hidden-areas rule in 35.

## Opening rule

Paths are opened just-in-time, when their milestone becomes next:

- the TWO ceremonies gate every gap (22 §Between paths, owner directives 2026-07-21): a CLOSING ceremony at path close (metadata recall + backlog management + prompted exchange) and an OPENING check at activation (feature-by-feature prompted confirmation inside the new path, deltas amending it before the base commit pins); activating a path with no recorded ceremonies is invalid
- seed from the milestone section of 18 and the coding path template in 24
- pass the coverage audit: every bedrock page accounted for at least once
- SEVERAL paths run at once (revised 2026-08-14, owner: *"every workstation or dev should be able to merge into master"*) — one worktree and one branch each, every path merging ITSELF after its own closing ceremony. No integration parent, no integrator. Numbered paths (`CP-MVP-010`) come from the roadmap and hold a row below; labelled paths (`CP-SETTINGS`) are named for their subject and hold none. Convention: [paths.md](./paths.md)
- never widen a closing path to absorb the next milestone — open a new one (thinness rule)

Late paths are better paths: they inherit module notes, friction records from the daily-use gate, and the architecture as actually built — none of which exist yet.

## Register

| milestone | scope | path | status |
|---|---|---|---|
| M0–M2 | shell · vault · AI patch loop | CP-MVP-001 | done (2026-07-06) |
| M3 | capture sources + local speech baseline | CP-MVP-002 | done (2026-07-07) |
| M3 (completion) | local speech runtime — 34 evaluation + seating | CP-MVP-004 | done (2026-07-08) |
| M3 (seats) | CUDA tiers + OCR seat + opt-in cloud rung | CP-MVP-005 | done (2026-07-08) |
| M4 | PDF source + strong anchors | CP-MVP-003 | done (2026-07-13) |
| M5 | web source tab + explicit imports | CP-MVP-006 | done (2026-07-16) |
| M1 (friction pass) | tree file management — create/rename/move/delete + DnD | CP-MVP-007 | done (2026-07-21) |
| M2 (completion) | real generation (Mistral Small) + AI interaction pass | [CP-MVP-008](./CP-MVP-008.md) | done (2026-08-04, [acceptance](../sessions/2026-08-04-cp-mvp-008-acceptance.md)) |
| M6 | minimal Truth Lens + challenge/repair | — | not opened |
| M7 | live verification provider | — | not opened |
| M8 (front half) | semantic graph foundation — ADR-011 grammar, pills everywhere, nodes/edges index, typed backlinks | [CP-MVP-009](./CP-MVP-009.md) | done (2026-08-13, [acceptance](../sessions/2026-08-13-cp-mvp-009-acceptance.md) · [closing](../sessions/2026-08-13-cp-mvp-009-closing-ceremony.md)) |
| M8 (retrieval) | graph retrieval — lexical BM25 baseline + link expansion, context packets, retrieval traces, vault-grounded chat | [CP-MVP-010](./CP-MVP-010.md) | **running** (accepted 2026-08-16, [opening check](../sessions/2026-08-16-cp-mvp-010-opening-check.md)) on `path/cp-mvp-010` |
| M8/M10 (augmentation) | Wikimedia augmentation — Wikipedia extract · Wikidata entity into the same nodes/edges tables · Commons P18 · Wiktionary etymology · the model-driven tool loop · web citations · save-as-source | CP-MVP-011 | reserved; opens immediately after CP-MVP-010 merges, short opening check (rulings already recorded in the 010 [opening check](../sessions/2026-08-16-cp-mvp-010-opening-check.md)) |
| — (process) | Cairn — parallel paths, self-merge, and the CI that enforces it | [CP-OPS-001](./CP-OPS-001.md) | **running** (accepted 2026-08-14, [opening check](../sessions/2026-08-14-cp-ops-001-opening-check.md)) |
| — (labelled) | provider expansion + settings menu — OpenRouter gateway, direct adapters (OpenAI, Anthropic, DeepSeek, Google Gemini, Mistral), multi-provider settings panel | [CP-PROVIDERS](./CP-PROVIDERS.md) | done (2026-08-16, [acceptance](../sessions/2026-08-16-cp-providers-acceptance.md) · [closing](../sessions/2026-08-16-cp-providers-closing-ceremony.md)) |
| M9 | measured local assistance + autocomplete | — | not opened |
| M10 | public knowledge + dictionary MVP | — | not opened |
| M11 | truth maintenance + cost dashboard | — | not opened |
| M12 | Atomik DSL | — | not opened |
| M13 | canvas | — | not opened |

Recorded input for CP-OPS-001 and for EVERY path opened while lanes run (owner brainstorm 2026-08-14, session note: [parallel agent execution with one active roadmap parent](../brainstorm/2026-08-14-parallel-agent-execution.md)) — the session that produced the lane convention. Unlike the pointers below it, this one is no longer purely provisional: eleven of its questions were ruled on by the owner the same day ([opening check](../sessions/2026-08-14-cp-ops-001-opening-check.md)) and the surviving convention is binding while it stands ([paths.md](./paths.md)). **Every path opening from now on reads it for**: which files are GENERATED rather than written by hand (`ACTIVE.md` and the running-paths view), that the journal is one file per entry under `../log/`, that declared write surfaces are advisory rather than exclusive, and that reproduced owner feedback preempts roadmap work. Superseded by the same day's revision: the integrator role and the lane layer are gone — every path merges itself. What is NOT yet decided: the bedrock/ADR ratification, which waits for the CP-OPS-001 S05 pilot and is owner-gated at S06.

Recorded input for any path touching NOTE RENDERING, the studio, or generated explanations (owner exploration 2026-08-15, session note: [the layered depth reader](../brainstorm/2026-08-15-layered-depth-reader.md)) — pointer, not a decision; confirmed or overridden at the concerned opening ceremony. A working prototype exists (`../../docs/cairn/workflow.html`). **What an opening reads it for**: the SYNTHESIS-LAYERING rule (each level complete at its own grain, so a reader may stop anywhere and be correct — as opposed to detail layering, where each level continues the last), the finding that colour should encode WHO ACTS rather than how deep you are, and eight recorded interaction gaps that any real implementation must answer — chief among them that a reader cannot see how deep a phrase goes before opening it. **Studio opening** additionally reads it as a sibling projection of the same gesture; **any generation path** reads it as a candidate output constraint (emit synthesis layers, a far stronger quality lever than "be concise").

Reserved follow-up (not a row yet, 33/34 discipline): a LOCAL llama.cpp generation seat opens as its own evaluated tier after CP-MVP-008's cloud engine lands — capability-gated like speech was, dated evidence in 34, never a release-wide promise. Decision trail: [../sessions/2026-07-20-generation-swap-decisions.md](../sessions/2026-07-20-generation-swap-decisions.md).

Leading candidate for the post-008 path (owner vision at the 2026-07-21 closing ceremony; **CONFIRMED at the 008 closing ceremony, 2026-08-04** — session note [../sessions/2026-08-04-cp-mvp-008-closing-ceremony.md](../sessions/2026-08-04-cp-mvp-008-closing-ceremony.md), with the semantic-graph front half sequenced first per the 2026-08-03 session-A amendment below): **Wikidata-backed generation grounding** — an M8 retrieval slice (lexical-first, 33's ladder) + an M10 Wikidata slice serving as LOCAL RAG for generation ("strengthen the generation through local rag architecture"); Truth Lens (M6) then runs over grounded output; M7's slot is decided at the M6 close. Pulled from deferred into the near-term backlog: Git status/diff view (M8 optional item).

Recorded opening input for M6, M7, and M8 (owner directive at the 2026-08-03 brainstorm session B — "link this session into next concerned paths"): [claims epistemology — from mechanical labeling to the LLM judge](../brainstorm/2026-08-03-brainstorm-session-b.md). **M6 opening** reads it for the format-freeze agenda: VerificationEvent schema (4-verdict vocabulary with mandatory qualifiers, bi-temporal), receipt shape (rationale + consulted/searched/not-found + judge + cost, durable-if-accepted, atomic with acceptance), label extension (`model-verified`/`disputed`/`challenged`), drawer = events + receipts + trace tree pre-acceptance, review = transient panel (NO dossier file); promotion targets at that ceremony: bedrock 28 + 31 patches, one ADR for the verdict vocabulary. **M8 opening** reads it for a named consumer: the candidate generator must serve claim verification (claim index, top-k passages, link expansion); vault-wide note review is a post-M8 composition unit. **M7**: web checks emit the same event schema (no scope change). **008 closing-ceremony backlog pass**: two small candidates — normalized containment (deterministic paraphrase-gap shrink) and a shallow judge pass over an operation's own selections (events as session meta only; no durable-format risk) — **ceremony ruling 2026-08-04: folded into M6's opening agenda** (land with the Truth Lens path, where the verdict vocabulary freezes).

Recorded opening input for M7 (primary), M6, M8, and the post-008 path (owner directive at the 2026-08-03 brainstorm session C — same three-artifact rule as session B): [websearch tool — contract, backends, Wikimedia-first grounding](../brainstorm/2026-08-03-brainstorm-session-c.md). Pointer, not a decision; every lean is confirmed or overridden at the opening ceremonies. **M7 opening** reads it as the path's design sketch: app-side `search`/`fetch` tool contract in main beside the adapters (adapters stay pure-compute), backend ladder (Tavily default → SearXNG → loud in-app scrape fallback), the **transient-search / fetch-your-own-evidence rule** (search-API storage ToS vs the durable evidence store), trace extensions (`location: 'web'`, `parentId`), budget/cancel reuse, `grounding-rules` prompt-block amendment, and the **owner lean: Wikimedia family first (layer B), Tavily only on receipt-recorded fall-through** — plus a dated search-API pricing/ToS research record before any backend commit. **M6 opening**: receipt carries "searches run + not-found"; verdicts layer-annotated (a layer-B miss with web disabled = unchecked, never evidence of absence); tertiary chains share one `independenceGroup`. **M8 opening**: the function-calling tool LOOP in the harness (chat search consumer); local rerank/query-rewrite roles (model-research §1.3); `search_web` mirrors the `searchVault` contract shape. **Post-008 path**: the Wikidata candidate is STRENGTHENED — its entity/nodes index doubles as the verification router's layer B (rung-1 few-GB SQLite subset = M10's concrete shape; Commons P18 hover cards a cheap fourth consumer once the semantic-graph front half lands). Nothing lands in 008 itself (web grounding explicitly excluded — M7 territory).

Recorded research input for any future cloud-generation/provider-expansion path and, secondarily, M6/M7 model/tool selection: [OpenRouter versus direct provider integrations](../../docs/research/openrouter-vs-direct-providers.md). Pointer, not a decision; its hybrid architecture is only the leading hypothesis and must be confirmed or overridden at the concerned opening ceremony. The opening reads it for the provider-neutral secondary seams, private-vault categorization/license gate, pinned-versus-resilient routing policies, route receipt fields, direct escape hatch, and synthetic-fixture A/B/failure matrix. **No CP-MVP-009 scope change.**

Recorded input for the 008 CLOSING ceremony and the post-008 path OPENING (brainstorm 2026-08-03, session note: [../brainstorm/2026-08-03-brainstorm-session.md](../brainstorm/2026-08-03-brainstorm-session.md)) — pointer, not a decision; every lean below is confirmed or overridden at the ceremonies:

- **Amends the leading candidate above**: the post-008 path gains a FRONT half — semantic graph foundation (wikilink grammar + inline `{label}` edge decoration + type pills on all rendered links + rebuildable nodes/edges index + typed backlinks) — sequenced BEFORE the Wikidata slice, which then joins the same nodes/edges tables. Rationale: the definitive vault starts accumulating `[[links]]` at 008's close; the agent-harness vision (owner: manage/curate/expand knowledge) needs map + edges + scoped retrieval first.
- **Two owner-gated patch candidates**: (a) bedrock 20 doctrine — notes are concepts, the link graph IS the semantic graph; typed edges are light structure, claims/evidence an OPTIONAL overlay, never a birth requirement; (b) ADR for the edge grammar — `[[target]]{label}` / `{^label}` reverse, kebab labels, adjacency rule; collision-tested against the app's markdown-it config and Lezer (evidence in the session note).
- **Q1 record**: definitive vault starts at 008's S07 acceptance (content layer safe since M1; one possible meta migration before M6; `.atomik/` never at risk) — feeds the experiential-gate start.

Recorded opening input for the DSL-integration path (main-repo M12/M13 = atomik-dsl D4) and, secondarily, the CP-MVP-009 opening (owner brainstorm 2026-08-04, cross-repo session — same three-artifact rule; session note: [../brainstorm/2026-08-04-brainstorm-studio.md](../brainstorm/2026-08-04-brainstorm-studio.md)). Pointer, not a decision; every lean is confirmed or overridden at the concerned ceremonies. **The studio**: the evolved articulation of ADR-010's one-surface-two-layers — a live-interactive block inline in a note's rendering, backed by its own studio file, hosting three entity kinds that each edit through their canonical door (bound scenes → DSL line patches; vault-graph nodes/edges → note diffs `[[target]]{label}` and real note creation, layout canvas-owned; ink → own format inside the studio file — Excalidraw dropped, ADR-010 amendment candidate); Illustrator-style layer/group hierarchy owned by the studio file, orthogonal to entity kind, never required to carry semantics; human edits per-gesture immediate with undo, AI iterations through the existing patch-preview lane; ONE custom engine — dimension-agnostic dependency-free core in atomik-dsl (benched in the local demo during its D5–D8 wave), SVG renderer v0, three.js reserved as the later 3D target. **D4 reframed as studio v0** (still a transplantation — the engine core matures in the atomik-dsl wave first). **M13's canvas is subsumed by the studio** — resolved at the roadmap amendment, not by this note. **CP-MVP-009 opening** reads it only for: the studio recorded as a later consumer of the nodes/edges index (graph layer = a projection of those tables; canvas edge-drawing reuses the pill edge-lifecycle discipline and write path); no scope change. **Owner-gated patch candidates carried**: ADR-010 amendment (custom engine, ink absorbed, SVG/3D targets) + bedrock 19/21 (unified studio supersedes the two-surface framing). Counterpart register note in atomik-dsl records the wave-side consequences (engine-core placement, D7/T3 ink swap).

Owner-gated patch candidates from the 2026-08-03 memory audit (session C; owner rule: project guidelines persist agent-agnostic in the repo, never only in an agent's private memory) — pointers, promoted at the next ceremony or a docs unit, not by this note:

- **Bedrock 03 (disposability/lifecycle)**: the house lifecycle rule, owner-stated twice in one day (2026-07-08, transcripts then PDF extractions): every feature that lands a derived artifact ships its full day-to-day lifecycle — create AND delete/re-run (confirmed, state-restoring, tree-refreshing) — in the SAME work unit, with a round-trip test, even at MVP. Already cited in commits as "disposable per 03"; the doctrine text should say it explicitly.
- **Bedrock 22 (session protocol)**: (a) staging discipline — the owner dogfoods the live repo during agent sessions, so untracked owner files appear mid-session: review `git status --porcelain`, stage explicit paths per work unit, never blind `git add -A`, surface unexpected files to the owner; (b) status/next-step reports to the owner = short themed sections, each with its OWN `- [ ]` checklist of owner-side tasks (never one global list — owner directive 2026-08-03, "continuity in my focus workflow"); (c) owner-reported external-tool experiments are first-class bench inputs — pin the exact artifact/version/config before comparing.
- **Bedrock 24 (coding-path template)**: gate discipline line — gates run BARE, exit code is the verdict, no `| grep`/`| head` on gate output (incident + rule recorded in the module note's Agent checklist, 2026-07-16 white-screen).

Reserved follow-up (owner decision 2026-07-21, "after the next coding path"; **at the 008 closing ceremony, 2026-08-04, the owner chose the semantic-graph path for this gap — the build stays reserved and is re-offered at a later ceremony**): the PACKAGED NATIVE WINDOWS BUILD opens after CP-MVP-008 — one bounded unit (electron-builder config, Windows target, launch path) that closes the three named WSLg gaps at once: real 240 Hz presentation (S07j attribution), DWM rounded corners + shadow (S07q), and hardware-composited glass depth (36 §glass budget). Until then the dev app under WSLg is the daily driver and those three limits are known, recorded, and not app defects.
