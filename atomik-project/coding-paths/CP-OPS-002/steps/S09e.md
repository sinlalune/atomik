---
type: Atomik Coding Path Step
title: 'CP-OPS-002 S09e — The integration compared against the trunk, and found the path's first step'
timestamp: 2026-09-02T00:00:00Z
atomik:
  path: CP-OPS-002
  step: S09e
---

# CP-OPS-002 S09e

### S09e — The integration compared against the trunk, and found the path's first step — **COMPLETE**

```cairn-unit
step: S09e
unit: 48
type: repair
verified: cairn-check, cairn-check with --previous at the trunk tip, cairn-active
```

The trunk now requires a pull request with both checks green — a host change
since the binding was written, and the transport ruling 5 asked for. The first
pull request (#3) carried the tested integration commit of candidate `041c713`
and its protocol check failed twice over.

**Twenty-six immutable records edited at S00.** The very first unit of this
path added `path:` and `ceremony:` to twenty-six existing session records so
the closing gate could read declarations. `record-integrity` was written at
S07i. Every push-triggered CI run compared with the previous push, so the edit
was never judged; the pull request compared with the trunk and judged it. The
failure mode this path was opened for, at its own door. The files are restored
to their trunk blobs and the
[correction record](../../../sessions/2026-09-02-cp-ops-002-record-integrity-correction.md)
lists each with its blob id before and after and says why restoring is safe.

**Three index files in the integration commit.** The register row, the audit
index and the sessions index are OKF courtesies, not closure surfaces; the
`acceptance` rule admits only the path record, the view and the journal at the
`done` transition. They move to this unit, on the branch, where they belong.

**Two verdicts that were not what they looked like.** The local gate on the
merge commit reported `OK` with no advisories because `resolveBase` treats any
non-path branch as trunk work and compares nothing; the pull request run passed
`--base` explicitly and judged 295 files. And the local branch gate never
reported the twenty-six mutations because its default comparison ref is the
push before, not the trunk — the same narrowing S08a repaired for `base`, still
present for `previous`. Both are genesis debts, named in the plan; a merge
candidate is now gated with CI's exact flags before it is pushed.

## Ledger rows

| Field | Value |
| :-- | :-- |
| Deliverable | Twenty-six records restored; the correction record; the register, audit and session index lines; the record returned to `running` for the third candidate |
| Measured | PR #3 protocol check: 26 `record-integrity` findings and 3 files under `acceptance`; local branch gate with `--previous <trunk tip>`: same 26, invisible without the flag |
| Verification | `cairn-check` OK; `cairn-check --previous dfcd09d` OK; `cairn-active --check` current |

## Continuation, same day

Two more things belong to this step, and the way they landed is itself a
record.

**The workflow no longer passes `--previous`.** Its push runs handed the
checker the event's `before` sha, so each record was compared with the push
before it while a laptop compares with the merge-base of the trunk — the
narrowing S08a repaired for `base`, left open for `previous`, and the reason no
push run ever judged the S00 edit. Without the flag, CI derives the same
comparison ref a laptop does. The paragraph above that called this a genesis
debt is superseded here: it is fixed.

**The working-tree gate reads a restoration as an edit.** Before the commit,
`record-integrity` reported all twenty-six restored files as modified: it reads
Git status, not content against the trunk, so a file put back to its trunk
blob looks like a file changed. On the committed tree the same rule compares
content and finds nothing. The unit was therefore gated after committing and
before pushing, in the default form, with `--previous` at the trunk tip, and in
the form a pull request from a non-path branch runs — all green.

**The commit split in two.** A script error staged part of the unit: commit
`ac3827e` carries the restorations, the workflow and this record's first
version with the path record still `ready`, so its push run is red under
`acceptance` — implementation after an acceptance that this very step voids.
The continuation commit carries the correction record, the path record at
`running`, the brief, the logs and the index lines, and it is the candidate.
