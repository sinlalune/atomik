---
type: Atomik Coding Path Step
title: 'CP-OPS-003 S05 — the fork on the record: ADR-023, and an attribution that was wrong in the engine'
timestamp: 2026-09-22T00:00:00Z
atomik:
  path: CP-OPS-003
  step: S05
---

# CP-OPS-003 S05

### S05 — the fork on the record — **COMPLETE**

#### Plan

Write the decision record for the one kit file this repository holds ahead of
its release, so that the fork is a declared position rather than an
undocumented edit. Advances outcome 6.

#### Work

```cairn-unit
step: S05
unit: 05
type: decision
verified: cairn-check, the diff against the release measured
```

- [`ADR-023`](../../../../docs/adr/ADR-023-the-checker-is-held-one-patch-ahead.md):
  `tools/cairn-check.mjs` is held one patch ahead of release 1.1.0 to carry two
  link exemptions the release dropped and ruled the adopter's own — while
  giving the adopter no field to declare them in. It records the five findings,
  why each of the two classes cannot be repaired, the three routes rejected,
  and the exact sequence that retires the ADR when a release carries the
  configuration field cairn PR #23 asks for.
- indexed in `docs/adr/index.md`.
- **the patched region's own attribution was wrong, and is corrected.** It
  said the exemptions left in `e18bbe4 (CP-CAIRN-006 S02)`. They left in
  Cairn's **CP-CAIRN-001** S02: `e18bbe4` reached Cairn's `main` through
  `path/cp-cairn-001` (PR #2, then the integration PR #3), and that same path's
  `S06` carries the ruling this ADR quotes. The comment now says so, names
  ADR-023. The same wrong id was written into the first draft of Atomik's
  adoption feedback note upstream and corrected there before it merged; it
  survived here because this comment was written from the same memory rather
  than traced to a commit.

#### The measurement

Every factual claim in ADR-023 was measured rather than recalled, and the ADR
carries the figures. What this record adds is the method: the release's file
was read from Cairn's `bff7e2a` and diffed against the worktree's; the FROZEN
header was read from `atomik-project/log.md`; the lock's digest was recomputed;
and the five `links` findings were reproduced by swapping the release's checker
in and running the gate.

#### Self-review

- `yagni:` no mechanism, no flag, no config shim to "manage the fork". The
  lock already reports the file as edited and `update` already refuses to
  rewrite it; the ADR names the condition and the command that undo it.
- `refused:` syncing the lock digest so the file reads pristine. An edited kit
  file that reads pristine is a lock that has stopped being evidence, and the
  ADR says so as a standing rule rather than a one-off.
- net: +1 decision record, +1 index bullet and its description, and the engine
  comment at `5 3` on `git diff --numstat` — the figure the tool prints, not
  one reasoned from it.

#### Review

First read — reader: a fresh context, a subagent of this session, given the
diff and the two criteria and nothing else. It re-derived every claim from the
Cairn repository and reproduced the five findings by swapping the release's
checker in:

- **`docs/adr/index.md` was broken by the new bullet** — it landed between
  ADR-022's bullet and ADR-022's description, so ADR-022 lost its description
  and the retention paragraph read as ADR-023's. **Fixed:** the bullet sits
  after ADR-022's description and carries its own.
- **the wrong path id survived in this record's own definition of done and in
  `plan.md`** — the unit whose headline is that attribution being wrong.
  `plan.md` is **fixed**. The definition of done is **not**, and that is the
  finding worth reading: correcting it moved the accepted text, `scope-digest`
  refused the run, and the acceptance in force was invalidated. The text is
  restored byte-for-byte — the digest is `sha256:fce14b6e…` again — and
  outcome 6 still carries `CP-CAIRN-006`. A citation inside bound text cannot
  be corrected by the writer alone; it needs a second acceptance block, and
  that is the owner's signature, not this unit's. Raised for a ruling.
- the parenthetical recording the earlier wrong attribution was a third of what
  the engine hunk added — in the one file ADR-023 declares every forked line a
  standing cost of — **fixed**, deleted. `numstat` on the engine went from
  `7 3` to `5 3`. `git blame` and the ADR hold that history; the corrected
  attribution alone is the whole fix.
- an eight-line section on why the attribution mattered — **fixed**, reduced to
  one clause on the work bullet.
- the measurement restated ADR-023's Decision verbatim — **fixed**, this record
  now gives the method and points at the ADR for the figures.
- "the engine comment grows by eight lines" was wrong — eight was the comment's
  length, not its growth — **fixed**, and the figure is now whatever `numstat`
  prints: `5 3`. Two arithmetic slips in this bullet were caught by the second
  read, in the one bullet whose point is that a figure must be measured.
- the reader notes ADR-023's Context reproduces much of the upstream feedback
  note merged in PR #23 — **refused.** The note is evidence filed with Cairn;
  the ADR is this repository's decision, and a decision record that cannot be
  read without fetching another repository's merged PR is not a decision
  record.

Second read — reader: a fresh context, a subagent of this session, given only
the diff of those fixes and the same two criteria. It counted the index's
bullets and descriptions, resolved every link, recomputed the digest and
re-derived `e18bbe4`'s path from Cairn:

- two arithmetic slips in the bullets that claim to have measured — *net +1
  after the trim* against `numstat`'s `5 3`, and *three of the seven lines*
  which no arithmetic produces — **fixed**, and both now quote `numstat`
  rather than reason from it. The lesson repeats itself inside one unit: the
  bullet whose subject is an unmeasured number was itself unmeasured.
- the definition of done still requires the ADR to name `CP-CAIRN-006`, so the
  delivered artifact does not literally satisfy the checkbox in force — the
  reader calls the refusal the honest call and says it needs the owner's second
  acceptance block rather than a silent pass. **Agreed, and it stays open.**
  It is raised in the first-read bullet above and in the path's resume section;
  it is not this unit's to sign.
- two new lines run past the file's ~78-column wrap — **fixed.**
- `docs/adr/index.md` verified: 23 bullets, 23 descriptions, one each, no
  orphan and no stolen paragraph; all 23 targets exist.
- `tools/cairn-check.mjs:1246` also says CP-CAIRN-006 and is **correct**: that
  is upstream text, byte-identical on Cairn's `main`, naming a real Cairn path.
  Recorded so a later sweep does not "fix" it.
- ladder: no complaint. This is the shortest step record of the path.

#### Verification

```text
cairn-check : pass — OK, 1 advisory (concept-growth, inherited from S02)
diff        : 1 hunk against the release at bff7e2a, measured above
tests       : not run locally; no local dependencies in this tree. CI runs them.
build       : same.
user review : not required — the fork already existed; this unit records it.
remote      : origin/path/cp-ops-003 — the commit carrying this record
```
