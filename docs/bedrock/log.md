---
type: Atomik Folder Log
title: Log — docs/bedrock
description: Recent meaningful changes to the constitution, per the OKF folder convention.
tags: [log, okf, bedrock]
timestamp: 2026-08-24T00:00:00Z
---

# Log — `docs/bedrock`

Recent meaningful changes in this scope: the constitution. The companion map is
[index.md](./index.md). An agent reads the index before opening many files, and
this log when recency matters or when re-entering after time away
([bedrock 26](../../docs/bedrock/26_26-okf-agent-context.md)).

Seeded 2026-08-24 (CP-OPS-002 S05c) from Git history: the 15 most recent of 16 commits that touched this folder, newest first, merges omitted because a merge names the path rather than the change.

**Append newest-first, at the top, in the same work unit as the change.** Git
remains the complete record; this is the readable one. If two paths ever start
colliding on this file, it takes the amendment the journal already took — one file
per entry in a `log/` subfolder — which was a concurrency fix, never a size one.

## 2026-08-24

- `3df9073` CP-OPS-002 S05: backfill OKF — five indexes, ADR frontmatter, schema over the decision plane
- `c4a9670` CP-OPS-002 S01: pin the ceremony schema, unify the registration doctrine
- `382ba30` CP-WORKTREE-CLEANUP S01: retire worktree after verified merge
- `9ea45db` CP-OPS-001 S09: push every step as a session boundary

## 2026-08-20

- `6deb103` CP-OPS-001 S08: register paths before branching

## 2026-08-15

- `4ff81af` CP-OPS-001 S06 — ratification: bedrock 22/24/35 amended, ADR-012 accepted

## 2026-08-04

- `d2759cc` docs unit (the four owner-gated patches approved at the 008 closing ceremony): bedrock 03 + lifecycle doctrine (derived artifacts ship create AND delete/re-run round-trip tested in the same unit) and invariant; bedrock 22 + working-alongside-the-owner protocol (explicit-path staging, themed owner checklists, external-tool experiments as bench inputs) and staging invariant; bedrock 24 + gate discipline (gates run bare, exit code is the verdict — 2026-07-16 white-screen incident promoted from the module note); bedrock 20 recast on the corrected doctrine (notes ARE concepts, the link graph IS the semantic graph; untyped links are full edges; claims/evidence an optional overlay never a birth requirement; block relation DSL retired for inline decoration); ADR-011 NEW — inline typed edge grammar [[target]]{label} / {^label}, kebab labels, immediate adjacency, same decoration after standard md links, session-A collision evidence recorded

## 2026-07-21

- `6cacfa2` the two ceremonies (owner refinement): closing ceremony (metadata recall + backlog prompted exchange) + opening check (feature-by-feature confirmation inside the path) replace the single review — both exercised live: post-008 candidate = Wikidata-backed local RAG grounding; Git diff view pulled from deferred; 008 confirmed with one amendment (chats persist as vault files); review CLOSED — 008 awaits the acceptance word
- `940aed7` CP-MVP-007 S07o: the owner's three verdicts fixed — glass made VISIBLE (72%/66% tints, doubled wash, glass-edge highlight; frosting same-color surfaces showed nothing), icon-first enforced everywhere with labels in hover titles (13 new content-verb icons; 36 §buttons rewritten), centering root-caused (SVGs rode text baselines in non-flex button families — block svg + one flex rule)
- `9c11033` CP-MVP-007 S07n: the design system — bedrock 36 (organic-future identity, token vocabulary, glass budget, a11y floors) wired into AGENTS; tokens live and applied (fs/space/radius/control-h/gutter/z scales); four organic themes (sage-stone, eucalyptus, moss, biolum); glass: alpha chrome tints + frosted overlays with opaque fallbacks; learning note 16 same-day (first-use rule)
- `65252ae` CP-MVP-007 S07k: full conventions + deterministic sync — every folder owns index.md+log.md, the vault root seeds at explicit adoption, and the management verbs keep the parent's managed Contents block and dated log lines current (owner decisions revising option D; sync lives in the verbs so future AI file management records identically)
- `45c65ad` learning layer repaid + vision-alignment gate (docs-only unit): notes 12-15 backfilled against the real code (PDF engines/anchors, speech+OCR seats, web isolation/snapshots, file-management refactor); the dropped learning-notes DoD clause restored in 22 step 9 / 24 template / 008 draft; the owner's pre-path alignment review instituted with its first instance before CP-MVP-008 (Q6 answered by this unit, Q1-Q5 open)

## 2026-07-16

- `c643c0f` ADR-010 accepted: one surface, two layers — bound scenes and free ink (doc-17 docs-only unit)

## 2026-07-13

- `c5de26a` CP-MVP-003 CLOSED (owner validated) — M4 done; bedrock 10 planned→mvp; no active path

## 2026-07-06

- `6b1e598` docs: beginner learning layer (owner feedback) — own what agents build
